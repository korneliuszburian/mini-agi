# Ticket

- id: TICKET-11
- title: Fix capability gap: codex-exp-003 scores below the loop target
- goal (one sentence): Bring codex-exp-003 composite above 0.5 by fixing the failing run.
- domain: eval

## Closure evidence (2026-08-10, goal session) — ABANDONED

- Same disposition as TICKET-10: the verifier target
  `/tmp/opencode/exp003-work` is gone (checked 2026-08-10);
  `codex-exp-003-rerun` is `unverified` with `target_missing=true`.
- No verifier can run, no score can move; the ticket's goal sentence is
  physically impossible with the current repository state.

Status: CLOSED (abandoned — evidence above).
