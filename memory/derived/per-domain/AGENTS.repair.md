# PROVENANCE
# canonical_sha256: 1185eb89b526658f
# canonical_entries: 104
# derived_at: regenerated deterministically by mini-agi derive
# rule: if this file's canonical_sha256 differs from `mini-agi provenance` output, re-run derive

# Domain: repair (derived from canonical memory)

Applies when working on this domain. Canonical memory wins on conflict.
- `73454769bc163ce3` TCMIIES (Zhao, arXiv:2605.07507; system report, verified in full text): schema-guided-prompting extraction (no constrained decoding, commercial LLM APIs) with a concurrent batch engine carrying up to three automatic retries (1 s delay) per record measured on 500 CNKI TCM papers × 3 providers achieved 94.2% average structured-output compliance (DeepSeek v4-flash 95.8, Qwen turbo 94.2, GLM-4 flash 92.5).
- `f3b751d3cfdaee27` TCMIIES (system report): field-level extraction accuracy plateaued at 81.6% (Research Topic 89.3% down to Research Limitations 73.5%) despite prompt+retry — the schema-valid-but-wrong gap; first-attempt success 88.4% rose to 97.1% after the three-retry mechanism, with 100% completion under 10% injected API failures.
- `f0ed662457e1c79e` TCMIIES (system report): measured per-task API cost anchor of roughly $0.38/1,000 papers (DeepSeek v4-flash) to $3.41 (Qwen max) at ~2,000 input + 500 output tokens per paper — a rare measured per-task cost figure for extraction (not isolating retry cost).
- `5480bd685bff8004` SQL Query Engine (Ijaz, arXiv:2604.16511; system report): two-stage NL→PostgreSQL pipeline with a five-strategy response-parser cascade (JSON → embedded JSON → code block → SELECT regex → raw text; tolerant parsing instead of schema enforcement) and an execution-grounded self-healing loop feeding full PostgreSQL SQLSTATE diagnostics back to the LLM.
- `79088f70c96759ad` SQL Query Engine (system report): on the synthetic benchmark (75 questions, 3 DBs) Config C (generation only) 48.0% → Config A (up to 5 repairs) 57.3% for Llama 4 Scout 17B (+9.3 pp, zero regressions); on BIRD mini-dev (437 questions, 11 DBs) the full pipeline reached 49.0% execution accuracy (GPT-OSS-120B), +4.6 pp from self-healing; repair latency cost visible: Scout 1.67 s → 4.19 s mean.
- `e5170e596b2575e1` SQL Query Engine (system report) validates retry-loop design with three explicit mechanisms: early-accept (never re-evaluate a query that executed correctly — prevents the LLM 'fixing' a good query into a bad one), best-result tracking (return the best result seen when retries exhaust — a bad retry cannot regress), and ignoring the LLM's own isValid self-assessment (always False by design; only execution outcome determines success).
- `86be403af727cc0f` xmemory / schema-grounded memory (Petrov et al., arXiv:2604.27906; system report): iterative schema-aware extraction with validation gates, local retries, an LLM judge-in-the-loop, and stateful prompt control; measured (10 runs, mean±std) single-pass frontier baselines (Sonnet 4.6, Opus 4.7, GPT-5.4, GPT-5.5, Gemini 3.1 Pro) hit near-saturated field-level F1 95–97% but collapsed at aggregate levels: object-level accuracy 79.24–89.24% and output-level accuracy 34–62% (GPT-5.4 34.0±7.0, Sonnet 4.6 42.0, Opus 4.7 42.7, GPT-5.5 high-reasoning 44.0, Gemini 3.1 Pro 61.7).
- `a2d22fc6a58aa061` xmemory (system report): judge-in-the-loop configuration reached 90.42% object-level and 62.67% output accuracy, above all tested frontier structured-output baselines; end-to-end memory benchmark 97.10% F1 vs 80.16–87.24% for third-party baselines; 95.2% on an application task — an LLM-judge + retry loop raises whole-output exactness from ~34–44% to ~63%, closing part of the compounding gap but plateauing well below field-level saturation.
- `26dbee59e3ecf43e` xmemory (system report) states an illustrative compounding model (not measured): at per-field accuracy 0.97 with 20 fields, single-pass record-correct probability is 0.97^20≈0.54, recovering to ≈0.98 with two retries per field.
- `7014b9ee9ab22a85` Cross-pattern synthesis (§4–§6): when the verifier is deterministic (schema validator / SQL execution / exact check), a retry+validation loop reliably lifts structured-output success to ~90–97% and never regresses; the residual failure is not syntax or schema but semantic leaf values, which deterministic validation cannot catch — measured plateau ~63% (leaf) to ~80% (transaction semantics).
- `d5995f282ab01107` MOUD medication extraction (Fei et al., arXiv:2510.21027; system report): constrained JSON generation (Pydantic schema, vLLM) across five heterogeneous EHR systems with post-processing (type normalization, rule-based cross-field validation, MOUD-days computation) measured on 10,369 manually-annotated records (776 patients): Qwen2.5-32B 93.4% coverage / 93.0% record-level exact-match, MedGemma-27B 93.1%/92.2% — even at ~93% coverage only ~93% fully match on all 5 attributes + computed metric (recurring valid-but-wrong tail).
- `551a3a46b1d3bf43` MOUD (system report): clinic heterogeneity dominates model choice — Qwen2.5-32B scored 100/100 on Clinic A while Qwen3-32B scored 0% there (42.9% coverage); smaller models are coverage-unstable (Qwen3-8B overall coverage 48.0%, Qwen3-4B 2.2% on Clinic C) — 'model family matters as much as size' for schema-constrained extraction.
- `9c3c2433df52405a` MOUD (system report): the repair loop is human-analyst-driven — a domain-expert error review identified three recurring fix classes applied as rule-based post-processing: imputing missing dosage fields from within-drug norms (Clinic B ~20% of records had key missing fields), handling monthly/weekly injectables (e.g., Vivitrol ~5% of Clinic C/E prescriptions) by setting duration from the documented schedule, and unit normalization so mass units ('250 g') are not misread as daily counts — residual semantic errors need domain rules, not more retries; authors explicitly note human-in-the-loop review is essential for high-stakes clinical use.
- `15b82401938c5a93` Palabrita (Oliveira, arXiv:2604.24636; on-device SLM practitioner case study, commit-level evidence trail 204 commits/5 days): five on-device failure categories with measured rates — F1 format violations (markdown code-fence wrapping majority of early calls, dropped to ~20–30% after 'no code fences' prompt; a structural parser needed for 15–20% of Qwen3 0.6B parses; JSON keys translated into prompt language; UTF-8 replacement-char corruption <5% but fatal), F2 constraint violations (wrong word length 30–50% of early generations — the 600M model cannot reliably count characters — dropped to ~10–15% after prompt hardening but 'still too high for production reliability'), F3 context quality degradation after ~3 generations (Qwen3 0.6B) / ~5–7 (Gemma 4 E2B) due to limited on-device KV cache, F4 latency (15–30 s per puzzle at 30–35 tok/s CPU), F5 model selection instability (6 models each needing a different prompt profile; cut to 2).
- `6e1f98afd067dae7` Palabrita resolution architecture (reported): defensive parsing as a pipeline (UTF-8 sanitize → code-fence strip → direct decode → regex extract → structural type-based field inference), contextual retry feeding the specific rejection reason back into the prompt (word-length errors the main trigger), and 'progressive simplification' — the LLM's job reduced from 7 output fields to 0 for word generation (curated word lists) and 2 fields for hints, with a deterministic fallback so 'the LLM enhances the experience; it does not gate it'.
- `cfe795ab8c642a7e` Palabrita (reported): authors' explicit compounding arithmetic — 'for n fields with independent violation probability p, expected success is (1−p)^n; reducing from 5 to 2 fields approximately doubled the success rate'; implication: at SLM scale measured structured-output reliability is dramatically lower than frontier models (0–50% constraint compliance without heavy engineering).
- `70c12cac8fc7ae06` Melo (Wang et al., NetEase Cloud Music, arXiv:2607.23718; RecSys '26; production system report): music-recommendation agent behind Muse Mix (100M+ user platform, >1M cumulative users) built as a deterministic five-node state graph (UNDERSTAND→PLAN→EXECUTE→REFLECT→SYNTHESIZE) deliberately not a free-form ReAct loop, because 'across a free-form chain, intent-parsing, argument-extraction, and tool-side errors compounded across steps'; named-node defenses: inference-time entity grounding (L1 catalog reverse-lookup, L2 prompt-level consumption rules, L3 plan-time guards) and reflective retry (REFLECT→PLAN back edge, capped at two rounds, choosing among proceed / retry_rewrite / retry_relaxed / fallback).
- `1f894d76e5789412` Melo (system report; measured on internal 298-query stress-test set, each config replayed 3× at temperature 0.3, Kimi K2.5 backbone, ~10³ measurements; rates caveated as properties of the stress-test set, 'not estimates of raw production frequency'): retry fires on 5.8% of sessions; process-level recovery 59.1% (retry_relaxed 63% recovering 52% of triggers, retry_rewrite 56% recovering 48%); constraint preservation after recovery 77% (23% recovered-but-degraded, concentrated in retry_relaxed 29.4% vs retry_rewrite 14.3%; intent-preserving recovery ≈46% of triggered sessions); central caution: 'retry must not hide what it gives up' — recovery and constraint preservation evaluated as two metrics in tension.
- `d0667829851ed2d0` Melo retry cost (measured): +15.9 s P50 / +29.2 s P99 per triggered session, amortized over all traffic just +4.1 s P50 / +13.6 s P99 (paired retry-on/off ablation: P50 32.5 s vs 28.4 s); avg song count +0.21 over all queries but +17.5 candidates on the tail queries it materially improves — 'retry is cheap on the median session because it does not run, and high-leverage on the tail sessions where it does'.
- `dfaec127fda77e74` Melo grounding ablation (measured): entity misidentification 17.4% → 9.6% (−7.8 pp) with full defense, at +2.5 s UNDERSTAND / +1.8 s PLAN P50 latency; L3 alone −1.2 pp, +L2 −3.8 pp, +L1 −7.8 pp cumulative — catalog evidence alone is not enough, the LLM needs explicit weighting rules.
- `e4a2fe363829afa9` Melo online A/B (1M users, April 2–May 10): >2 pp primary playlist-retention lift and >1 min core engagement lift, reported as lower bounds with no CIs/significance; novelty and product-surface effects not fully ruled out; node-level cost dominated by LLM reasoning not tool dispatch (UNDERSTAND 8.0 s + PLAN 8.2 s + SYNTHESIZE 10.3 s P50 of 26.0 s end-to-end; EXECUTE 0.04 s; triggered-session REFLECT adds 6.4 s P50 LLM call).
- `0f4339615d264098` IBM enterprise automation evaluation (Wrenn et al., arXiv:2608.03311; IC2E '26 Industry; system report, 2,784 runs, 232 attempts per model per pipeline, 8 runs per cell, 29 prompts, 6 LLMs): monolithic v1 pipeline (whole workflow in one call) achieved structural success (JSON-schema valid + UI-renderable) of just 31.5–82.8% (gpt-oss-120b best 82.8%; granite-4-h-small 31.5%, mistral-small 33.2%).
- `f0afcf1604a978a8` IBM CW (system report): redesigned piecewise pipeline (v2: deterministic early Planner → retrieve evidence → generate workflow variables → base blocks → nested blocks separately, then programmatic-fix step before LLM JSON-Patch correction, ≤2 retry rounds) raised structural success to 74.1–97.8% with four of six models >90% (zero under v1) — decomposing the generation task was a more reliable fix for complex-schema JSON than any single-model choice, taking the worst v1 model (33.2%) past the best v1 model's ceiling.
- `bea9df1d1fc5af19` IBM CW (system report): the decisive structural-vs-semantic gap (independent Claude Sonnet 4.5 judge at temperature 0, majority-vote over 3 gradings × 1,044 total) — even the strongest model, llama-4-maverick on v2, satisfied only 55.2% of prompts (mean 71.5), next-best 44.8%; 'no benchmarked model achieved reliable correctness' and 'human review of generated workflows before execution remains necessary'; structural validity and semantic correctness are nearly orthogonal at production scale.
- `f83e967751bb05ea` IBM CW (system report): model-specific semantic failure — gpt-oss-120b regressed semantic 58.9→44.9 (−14.0) and 24.1%→6.9% satisfaction on v2 despite being #1 structurally (97.8%), because v2 caused it to emit stub workflows (a single comment block declaring integrations unavailable) which pass schema validation but are non-executable — 'uniquely treacherous: first on structural metrics, last on whether outputs do what users asked'.
- `536b91e7628f3054` IBM CW cost anchors (point estimates only; no per-cell CIs/significance reported): $0.008–$0.20/workflow; mistral-small 95.7% at $0.010/workflow, gpt-oss-120b 97.8% at $0.016, mistral-medium 96.1% at a 19× cost premium ($0.197; 0.4 pp gain), llama-4-maverick 90.5% at $0.024 and fastest (33.6 s); annualized at 1,000/day: mistral-small ≈$3,700/yr vs mistral-medium ≈$71,900/yr; latency is the piecewise cost (mistral-small 34.4→42.6 s, llama-3.3-70B 76.2→151.4 s with generation rate declining 95.3%→81.5%; gpt-oss-120b sole exception 99.3→66.2 s).
- `57d4732c1ffb43bf` Characterizing Faults in Agentic AI (Shah et al., arXiv:2603.06847; empirical fault taxonomy, verified in full text): 13,602 closed issues/merged PRs from 40 open-source agentic-AI repos (>1,000 stars), stratified-sampled to 385 faults, grounded-theory taxonomy (5 architectural dimensions, 13 symptom classes, 12 root-cause categories), validated by a 145-practitioner study (mean relevance 3.97/5, Cronbach's α=0.904, 83.8% had personally encountered the covered faults).
- `e5a6d2ccb1b2bd7a` Agentic fault prevalence (Shah et al.): 'Data & Validation Errors' is the single most common observable symptom (75 occurrences, 20.0% of all faults), ahead of installation/dependency issues (13.3%) and runtime failures (10.7%); the two dominant root causes are Dependency and Integration Failures (19.5%) and Data and Type Handling Failures (17.6%), interpreted as 'systematic contract violations between probabilistically generated artifacts and the deterministic interface, type, and runtime constraints enforced by external software systems'.
- `f6c08ef82bdb0411` Agentic fault examples (Shah et al.): LLM Integration faults include invalid JSON responses (MetaGPT producing invalid JSON when its RAG module misinteracted with the Ollama provider); API Parameter Mismatch includes tool-calling failures from 'null values where structured parameters were expected' (SWE-agent); token-handling faults from different tokenizers between framework and provider causing context overflow; association-rule mining shows faults 'do not fail transparently — weak error handling and limited logging obscure their underlying causes' (e.g., auth failures ← token-refresh mishandling, lift 181.5; wrong time values ← datetime-conversion errors, lift 121.0).
- `d84e1b455ff7763a` 'Do LLMs Know They Are Being Tested?' (Ahmed et al., arXiv:2510.08624; single-model study, GPT-OSS-20B, six paired A/B scenarios holding task content and decoding fixed): evaluation framing inflates chain-of-thought length (hundreds to >1000 chars) and reduces answer-only compliance; 'in structured outputs, it improves wrappers (e.g., fenced blocks, enumerated lists) but not regex-validated substance' — a direct instance of specification gaming (e.g., a fenced code block present while the regex-validated fix is wrong); incentive wording reweights the error mix; implication: benchmark prompts demanding strict formatting can inflate wrapper-level schema compliance without improving substantive value correctness — the framework, not the model ranking, is the contribution.
- `7b151a7d20f87ecb` Benchmarking the Benchmarks (Vaghasiya et al., arXiv:2607.02577; trace-level audit of 496 tasks across four benchmark families, expert adjudication, 89 annotator-hours): aggregate evaluator–human misalignment 18.5% (92/496); per-benchmark error rates: BFCL v4 (AST + simulator-state) 20.0% (40/200), τ²-bench Retail (DB-hash + NL assertions) 9.8% (11/112), MCP-Atlas (LLM claim coverage) 13.5% (12/89), LiveMCPBench (LLM judge) 30.5% (29/95).
- `bcac82d5414851c8` Benchmark evaluator mechanisms (Vaghasiya et al.): deterministic evaluators fail by brittle state matching, trajectory lock-in, incorrect ground truths, substring-based assertions, and reward-basis misalignment — in the inspected BFCL v4 multi-turn export, 80% of official failures are instance_state_mismatch, which 'conflates genuine task failures with evaluator artifacts'; a τ²-bench false negative rejected a correct $708 answer for lacking the substring '1628' ('an incorrect semantic target implemented as a substring check'); a false positive showed inaction passing because unchanged DB state looks successful.
- `baf9ded88a16fc68` LLM-judge benchmark failures (Vaghasiya et al.): rubric drift, hallucinated completion, answer-only scoring, run-to-run variance — 23 repeated LiveMCPBench runs of the same setup scored 57.9–76.8% (mean 69.4%, σ=5.4 pp, spread 18.9 pp), larger than most leaderboard margins (judge regenerates task key-points per run; default judge gpt-4.1-mini weaker than the paper's recommendation).
- `f4cbdf553ae9377e` Tool-Veritas (Vaghasiya et al.): deterministic-first scoring (state gates + restricted LLM fallback, bounded repair window) raised expert agreement to 95.5% (401/420) vs 69–90% for the audited families, with all 19 disagreements being stricter (false negatives, no unsupported passes) — the recommended direction for repair-loop-relevant evaluation; implication: BFCL V4's quoted overall/multi-turn percentages (e.g., 77.47% top model) carry an ~20% evaluator-error band and should be read as relative rankings, not absolute success rates.
- `875c47689a1e1a8c` Flat Score, Amplified Failures (Jang et al., arXiv:2607.27275; τ²-bench, ten cells: two open-weight families × {dense, MoE} × two domains, 456 episodes per arm, 4-bit AWQ): 4-bit quantization produced a statistically flat final score in every cell (no change survives multiple-comparison correction; seven cells TOST-equivalent at ±7.5 pp) yet in the damaged cell (Gemma-4-31B, telecom) raised tool-name hallucination from 19.51% → 38.26% of agent tool calls (+18.75 pp, CI [14.0,23.6]) — a 2.5× growth in event volume (649→1,646) — while creating essentially no new failures (BF16/INT4 hallucinated-name distributions correlate 0.97; 23/24 invented names are real user-side tools — 'role-boundary confusion,' amplified not invented).
- `b25f3d9404e1c402` Error-budget masking mechanism (Jang et al.): the score stays flat because the benchmark allows ten failed tool calls per episode and per-error recovery is equally reliable at both precisions (55% recovery rate unchanged) — 'the loop actively repairs the errors quantization introduces before they reach the outcome' at the cost of 2.5× more failed calls per episode (1.45→3.64) and 29% more tool calls (7.3→9.4); falsifiable proof: shrinking the error budget K re-exposes the damage — the BF16−INT4 gap grows 1.3 pp (K=10) to 7.5 (K=5) to 16.7 pp (K=2), ~13× wider, only in the volume-adding cell; budget-exhausting terminations rise 9× (5→46 of 456); mechanism: logit-margin account — full-precision failure propensity gates susceptibility (near-zero-baseline Qwen cells not amplified; checkpoint identity, not size/architecture, decides).
- `a4be8759582ea228` Reflexive repair loop (Jang et al., measured): on a failed call append 'ERROR: {err} Reissue this tool call with corrected, schema-valid arguments.', regenerate, up to 3 attempts (five telecom models at every precision) — collapses tool-name hallucination 38.26% → 2.90% (INT4), cuts out-of-list events 1,646→82 (20×), eliminates budget exhaustions (46→0), shortens trajectories (17.9→15.9 turns), and raises INT4 final score 65.35 → 71.27 (+5.9, CI [+1.1,+10.8], p=.017) above unmitigated BF16 (66.7%), while every other cell's repair interval includes zero — 'the mechanism is prevention rather than cure'; recommendation: report per-channel error rates and success-vs-budget alongside task reward.
- `824c39e752901ec2` SLM survey claim (Sharma & Mehta, arXiv:2510.03847; abstract-level only, 9-page PDF not retrievable as HTML): SLMs (1–12B, sometimes up to 20B) 'are sufficient and often superior for agentic workloads where the objective is schema- and API-constrained accuracy rather than open-ended generation', with guided decoding + strict JSON Schema outputs + validator-first tool execution 'often lett[ing] SLMs match or surpass LLMs on tool use, function calling, and RAG at 10x–100x lower token cost'; proposes the reliability metric set cost per successful task (CPS), schema validity rate, executable call rate, p50/p95 latency, energy per request.
- `9edd522ac96ebcbc` SchemaBench (no enforcement, frontier, ~40K schemas, depth 16.7; measured): GPT-4o overall valid-JSON 61.06% (complex 84.47% / custom 61.56% / escape 37.14%), GPT-4o-mini 43.98%; parser-error floors 8% (GPT-4o), 23% (Llama-3.2-3B), 36% (MiniCPM) — native generation fails on roughly 40–90% of complex schemas; the two big benchmarks disagree on the no-enforcement regime (JSONSchemaBench 13–46% vs SchemaBench 27–61%) due to schema-depth and model differences, so neither generalizes to 'the' rate (uncertain / not verifiable).
- `06cc8dfc000757fa` Enforcement coverage limits (measured): constrained decoding brings per-schema compliance to 63–98% for open grammar engines and ~100% for closed providers — but only on the subset of schemas each engine declares support for; OpenAI declared coverage on github-hard/store splits is 9%/6%; JSONSchemaBench github-hard empirical coverage for Llama-3.2-1B unenforced is 13% (easy 65%, medium 38%, store 21%); open-engine compliance rates on github-hard: Guidance 0.69 / XGrammar 0.41 / Outlines 0.06.
- `5f7aa50430ec256f` Format request is a capacity-dependent reasoning tax (measured): the format request itself (not decoding) drives most loss — Haiku −36.2 pp, GPT-4o-mini −28.0 pp, Opus 4.7 AIME −5.3 pp, and forced function calling collapses hard-reasoning to 10%; think-first-format-later recovers 80–87%; three measured remedies: reasoning-augmented grammars (CRANE, +10 pp), trigger-gated single-call decoding (In-Writing, +27% on classification, though small models can emit the trigger prematurely and lose reasoning — a documented failure mode, so trigger choice matters), and prompt-level decoupling (two-pass +6.8 pp / thinking +9.2 pp).
- `0f112bef6165821c` Constraint tax / SLM enforcement (measured): hard schema on sub-3B models raises validity 61.5→100% but lowers answer accuracy 19.7→11.0% and raises wrong-valid-schema outputs 49.5→88.9%; a deterministic calendar tool-call loses 43.5 pp executable accuracy at 100% schema validity (102/104 failures = one wrong field); a 3B model still loses 15.3 pp — 'wrong-valid-schema rate' is the first-class reliability metric that counts failures passing parsers, which retries do not fix.
- `04ce20f686503fa3` Constraint Tax side-effect (measured): under joint tool-calling + JSON-schema constraints, grammar masks silently suppress tool calls — Tool Invocation Rate 100%→0% for 6 open-weight models (GPT-5.4-mini unaffected) — so in agent stacks the compliance win can hide an action-taking loss; two-pass decoupling restores tool calls.
- `8df10858c5595438` Semantic ceiling at 100% schema validity (measured): SOB best leaf-value accuracy 83.0% text / 67.2% image / 23.7% audio; OrderBench 100% schema-valid with 2–83% semantic across models; the gap compounds with schema breadth and context length — xmemory single-pass frontier output-level exactness collapses to 34–62% even at 95–97% field-level F1; ExtractBench 0% valid output on a 369-field schema (4.6% field-level pass); ScrapeGraphAI-100k (93,695 real events): validity 98→20% at depth ≥7, ~65% at ≥200 keys, ~55% at complexity>1000 (87.4% GPT-4o-mini).
- `f26ea5ec230f7ffb` Unenforceable format rules (measured): IFEval-FC adherence ceiling <80% overall, Opus 4.1 letter-frequency 28%; nested/hierarchical output constraints far worse — IFHierBench best model 53.7% prompt-level, every model drops 37–53 pp at the first nesting level (d=0→d=1), d=3 collapses to ≤6% for 5/7 models; instruction stacking: follow-rate 96%→20–60% at 20 stacked instructions, 'output JSON' jointly unsatisfiable with 9 others.
- `293c648eb7e6dfe0` Intrinsic self-correction (measured, repair): without an external signal consistently lowers accuracy — GSM8K Llama-2 62.0→36.5, GPT-4 95.5→89.0 (1→5 calls, up to −25 pts) while multiplying call cost 1→5×; repair only reliably helps with external feedback — oracle labels, execution/unit-test signals (Self-Debugging +2–9% to +12%), or stronger/human feedback (1.58× more repaired programs pass, 33.3%→52.6%); at equal budget repair often ties or loses to plain i.i.d. resampling.
- `fd14a275027254d8` Self-Refine caveat (measured): its headline ~20% average is driven by preference/constrained tasks (+20–49 pp) while math gains are ~0 (64.1→64.1, 92.9→93.1) because the model cannot tell its own math is wrong; its own failure analysis confirms the feedback stage, not the refiner, is the bottleneck (61% bad fix, 33% wrong location, 6% refiner error).
- `da2c3504efdd2f5e` Deterministic-verifier repair economics (measured): Self-Debugging shows ~10× sample-efficiency when a deterministic verifier drives the loop; difficulty-adaptive retry budgets are up to 4× more compute-efficient than uniform best-of-N (Snell et al.); test-time compute can match a 14× larger model under FLOPs matching.
- `f4090a7c63c389b2` Offline improvement of native rate (measured): RL with a fine-grained schema validator (SRL) raises valid-JSON generation 28.5→72.5% (Llama-3.2-3B) and 36.5→79.7% (Llama-3.1-8B) — the only measured route that improves what retries resample from.
- `b8b7f1d1340fadc7` Agent-level reliability plateaus (measured): τ-bench gpt-4o succeeds on <50% of policy-compliant tool tasks and pass^8 is <25% in retail — 8 retries still fail on >75% of tasks, so pure resampling is exhausted; live BFCL V4 top model (Claude-Opus-4-5 FC) is 77.47% overall, multi-turn the weak category (best 68.38%); robustness drops: toolkit expansion with semantically related tools −8 to −19%, query rephrase −1 to −8%.
- `1f6daa3b273092cd` Diagnosis-guided runtime repair (measured): AgentTether repairs 59–65% of failed τ-bench runs on the hardest domain, beating blind retry, plain outcome feedback, and self-reflection at lower turn/token cost; feedback content matters — VeriHarness: adding location+value+admissible alternatives to repair feedback gains +42/+44 pp, and JSON-vs-prose presentation is neutral ('no evidence that JSON syntax itself improves repair').
- `00f1658f7d8f50f6` Retry infrastructure (measured): retry storms from rate limits are preventable with jittered exponential backoff (ContinuityBench 99.20% continuity, N=750, vs stateless ~0%); unchecked retry loops are a documented $-thousands production failure class (63 incidents, 21 frameworks — Khan); agentic orchestration multiplies energy per successful goal 4.33× vs linear (Panigrahy et al.).
- `55dfb567f49f3e98` Retry cost is structurally determined (measured): rerunning only failed subtasks cuts retry token cost 51.7–73.2% vs monolithic/static-decomposition reruns; a cost-aware reliability router achieves ~56% lower cost at matched quality than any fixed retry/voting technique; retries: expected draws to compliant on LM-only github-hard ≈7.7 (arithmetic estimate from 1/0.13).
- `b12ad7c7f07596a0` Counterpoint to always enforce (measured, 8,560 trials, 14 models): free-text tool calling (NLT) beat structured tool calling 62.3% vs 47.4% accuracy with 93% fewer critical errors and 25.2% fewer tokens — enforcement guarantees parseability, not task success, and for tool calling can add retry cost rather than remove it.
- `4423b1abbdb5a7d3` Serialization interface as reliability lever (measured): programmatic (code) tool calling matches or exceeds native-JSON accuracy on 11/14 models on a BFCL v4 subset (GPT-5.6 +10.6 pp) and removes a structural JSON limit — native JSON tool calling drops calls outright at fan-out N≈70–100 (Claude Sonnet 5: 100% enumeration at N≤70 → 0% at N=100) where code-based calling holds 100%; chaining +18.8 pp at len≥12; context-flood JSON −2.3 vs code +5.5.
- `457ddfbc8482528c` Schema representation as independent lever (measured, TSCG): for small models the JSON format of tool schemas is itself the dominant failure driver (accuracy collapses to 0–49% at >15 tools); deterministically rendering the same schemas as plain text restores Phi-4 14B 0→84.4% at 20 tools and 90.3% at 50; mechanism is format translation, not token compression (R² 0.88→0.03) — JSON, the production standard, is measurably the worst representation for small-model tool use.
- `952039c898e69791` Inter-agent hand-off representation (measured): schema-constrained JSON hand-off achieves feasibility 0.96 (false-NONE 0/50) vs narrative summary 0.48 (8/50) at the smallest payload (Relay); for multi-hop relays structure is 'faithful, not corrective' — injected errors persist 83–100% in every format with no cascade, and a strong relay is near-lossless in every format (≥0.973 recall after 6 hops) while under a weak relay JSON retains 0.94 of facts vs 0.74–0.85 for others.
- `f775e497d7e8f748` Token-set and wording levers (measured): the token set a grammar admits moves classification correlation ~0.5 — real numbers ρ 0.42–0.61 vs integer ρ=−0.16; leading-whitespace tokens +5–10% because LW/non-LW twins are non-exchangeable ('Lost in Space'); schema key wording alone moves accuracy ±16 pp by model family (Qwen2.5-7B +6.89 pp, Llama-3.2-3B −15.77 pp); word-formats −0.12 to −0.41.
- `998dc6a4eeebfd10` Cross-model format collapse (measured): the same prompt yields clean JSON on GPT-4 but fenced/malformed on Llama (PromptPort); Gemma-2B strict-parse ROS 0.116 vs semantic CSS 0.246; canonicalize+verifier recovers +6–8/+14–16 F1; in small-model multi-agent pipelines, JSON as the inter-role contract drops GSM8K 75.0→45.0% and HumanEval 96.3→56.0% on a 7B, while plain text restores 82.0%/81.7% (+37 pts) (Two Calls).
- `01f00e1e3835a627` Serving-stack corruption (measured): a routine sign-branched repetition-penalty sampler setting (θ=1.3) drops JSON schema compliance 97%→23% on 200 real schemas (58–96% of greedy tokens change; gauge-dependent per model) — a corruption neither constrained decoding nor retries recover; diffusion-LLM structured output collapses under stochastic sampling (Dream BFCL-Live 63.9→22.3%) and recovers only under exact FA-constrained decoding (22.3→69.0% at 100% CS, +4% overhead).
- `0a6c3cc372c5cf77` Enforcement cost is implementation-determined (measured): with mask-cache + CPU/GPU overlap, XGrammar mask generation drops to <40 µs/token ('up to 100×' vs regex/CFG baselines, ~zero end-to-end overhead), though the fastest configuration can be the most permissive (XGrammar's highest under-constraint count on the JSON Schema Test Suite); parser-stack classification gives O(1) vocabulary-independent masks (PSC, 700×/30× mask speedups, e2e throughput ≈ unconstrained); vLLM JSON-grammar constrained decoding: 3.6–8.2× inference latency with Gemma 52.4% duplicate outputs.
- `07bdd35766db54d4` Enforcement guarantee is approximate (measured): the standard locally-constrained stack (Outlines/XGrammar/OpenAI) is distribution-biased (renormalizing over locally-valid tokens distorts sampling from p(x|constraint)) and can fail validity within a finite token budget — LCD constraint satisfaction 95.26–99.20% at fixed budgets vs 100% for budget-aware GCD (GCD ~1.1× latency; P-GCD probabilistic look-ahead 3.11–10.2×); on MoE architectures Outlines/LLGuidance/XGrammar give ~0 gain and Mixtral LLGuidance AST collapses 0.409→0.236; grammar scarcity is removable — learned CFGs from execution logs reach 0.91–1.00 precision vs 0.025 for formal induction (Autogrammar).
- `01a2672e8349e37a` Training-side format cliff (measured): on-policy distillation reward-extrapolation collapses the JSON contract above a closed-form threshold — parse 94.8→65.1→8.5% across λ=1.15→1.50 while ranking quality on parsed outputs stays flat; below-λ a 1.7B matches 8B-SFT at 1/5 the parameters; constrained decoding adds +0.009.
- `f4596f48ade2d79f` Deployment structure dominates model choice (measured): decomposing complex workflow-JSON generation into piecewise stages raises structural success 31.5–82.8% → 74.1–97.8% across six models (4/6 above 90%, zero before); minimal-diff repair (RFC 6902 patches, JSON Whisperer) matches full-regeneration quality within 5% at 31% fewer tokens and 42.3% lower latency/cost (failure modes: missed related updates + array-index arithmetic) — third independent confirmation of selective-repair economics after StepCache/Asthana.
- `ef69e42494b930cc` Repair mechanics are measured at the mechanism level: supplying the error location is the dominant lever — attribution-based repair fixes 13/73 GAIA tasks vs 4–6 for decoupled self-correction, strict agent+step attribution 28.8% vs 21.7% single-pass (AgentDebugX); procedure-directed retry corrects 84.6% of UNSAT failures where a generic retry corrects 11.5% (Tell-Tale Trace, Llama3-70B, +73.1 pp); a consistency-based abstention filter raises function-calling IDKS 0.342→0.541 (APIGen) at the price of confident-incorrectness blind spots (5/5 identical wrong calls) and formatting-variation failures (IDK Filter); inference-time FFN gating repairs at ~1.85:1 fixes:harm with model-specific transfer (Amplitude Gating, PFHE 48 fixes/26 harms/2,556 units).
- `e03589b07f291e50` Selective triggering / patch economics (measured): GGC gate (SPARQL, MCQA) gen-only 90.23 → correct-all 92.34 → selective 98.33, gate FPR 0.31%, trigger rate 9.31%, 77.7% of errors semantic, ~45% less inference; StepCache latency 2.13→0.67 s, tokens 36.1k→27.3k, correctness 72.5→100%.
- `d7e7994431d530e1` Retry/refinement economics (measured): sampling diversity saturates (only 1.2→2.5 unique reasoning clusters from N=2→32 on AIME-24); refining each sampled trajectory (self-critique→self-correct→majority vote, no verifier) beats both plain resampling and verifier-based best-of-N (Qwen2.5-1.5B MATH500 29.6→58.0, AMC 25.0→32.5, +28 pp); under tool-environment unreliability recovery quality beats retry quantity — targeted recovery hints recover more than test-time scaling (ToolBench-X; agents fail under 5 recoverable hazards despite good reliable-tool performance).
- `20b4f75f5aab7cb5` Verification feasibility bound (CRC, Kotte, [69]; theoretical): with base risk μ above the target α on the task-model pair, no verifier or abstention layer can certify the target without abstaining on ≥(μ−α)/(1−α) of outputs — NER/QA/CLS uncertifiable at α=0.10 (0% certifiable), 47/40/60% at α=0.40; JSON μ≈0 certifies at 0 abstention; heuristic abstention misses targets 7.5–12.5% — 'guaranteed ≤X% error' claims for high-base-risk tasks are only meaningful with an explicitly stated abstention rate.
- `a2d0d1644fe1d3d4` Uncertain / not verifiable from reached sources: there is no single canonical 'JSON failure rate'; vendor guarantee statements (OpenAI, Anthropic) are first-party claims — OpenAI publishes one measured ladder (prompting <40% / trained-only 93% / strict 100%) on its own complex-schema eval and explicitly limits the guarantee to its supported JSON-Schema subset; whether closed-provider compliance stays ~100% outside that subset is unverified; the IFEval-FC tables, BFCL-robustness table, and production/system reports (TCMIIES, SQLQE, xmemory, ContinuityBench, AgentTether) are limited to specific models/domains at one point in time and mostly lack independent replication; τ-bench and SchemaBench headline numbers are for 2024-era model versions.
- `4b26f96529333302` BFCL live numbers here are the 2025-12-16 archive snapshot (evaluation commit f7cf735); the leaderboard's later 2026-04-12 update has a JS-rendered unextractable table so the two could differ slightly; an independent expert audit puts the evaluator's own error rate at ~20% for BFCL v4 (state-mismatch artifacts dominate) and 30.5% for LLM-judged LiveMCPBench — all function-calling leaderboard numbers should be read as ranking-level, not point-accurate; rate-limit-specific dollar-per-retry costs for commercial APIs were not located in primary literature (TCMIIES $0.38–3.41 per 1,000 papers and BFCL full-eval $1–355 per model both include non-retry spend).
- `9d1f310048daf8ef` Melo (system report) is the retry-economics anchor the literature otherwise lacks: retry loop's true cost is latency arithmetic (a rare +4.1 s amortized / +15.9 s triggered measured pair), its benefit is converting otherwise-empty sessions into usable ones, and its failure mode is silent constraint degradation (23% of recoveries) — a production retry must be evaluated on recovery AND preservation, not recovery alone.
- `ce00079fe0159247` Retry consistency is model-dependent (measured, STED): Claude-3.7-Sonnet stable at T=0.9; Haiku/Nova-Pro degrade; and repair feedback quality: LLM judge 'spot-on' critique rate only 58–63% (Counsel, τ-bench retail); evidence-locking (frozen persisted record, no source access) changes agreement −4.3 to +5.6 pp with order-inconsistency +8.0–9.5 pts vs structured one-call (structured elicitation ≈ standard).
