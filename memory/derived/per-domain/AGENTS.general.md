# PROVENANCE
# canonical_sha256: 454d486990964198
# canonical_entries: 73
# derived_at: regenerated deterministically by mini-agi derive
# rule: if this file's canonical_sha256 differs from `mini-agi provenance` output, re-run derive

# Domain: general (derived from canonical memory)

Applies when working on this domain. Canonical memory wins on conflict.
- `38e05948dad83b29` docs/MEMORY-RESEARCH.md (deep memory+skills pass): Anthropic context
- `7cd074b862efb583` docs/SKILLS-RESEARCH.md: 7 skill-design defects, routing gaps, dogfood
- `4ae6e387c68760c2` User direction: FULL AGI with a brain/memory layer so good it all ties
- `786416899b0bd2c2` Method: wayfinder skill (map + decision tickets). .scratch/wayfinder-
- `eb88bee62f23ab41` Research tracks: TRACK 1 DONE (brain/memory: CoALA, MemGPT/Letta,
- `7dfd93402ddc4aaf` Prime Agent (primeintellect.ai/blog/prime-agent, Aug 05 2026,
- `b9b79e0f2cdaacf4` Mastra Observational Memory (mastra.ai/docs/memory/observational-
- `0fc5cf16dc6d0efd` REJECTED: web dashboard for solo kernel (AFK-SUPERVISOR v2 doc) —
- `da7617f1e8f01004` REJECTED: parallel-planner "needs a second consumer" deferral —
- `11e6d3fb133e330e` DECIDED: reviewers = devils-advocate (roast, evidence-first);
- `d406cf06c1a1df90` DECIDED: per-domain human-review gate — frontend = mandatory HITL
- `4efccafe428586c9` DECIDED: memory consolidation = fact merge/supersede + selective
- `e40bb0673ce72f70` DECIDED: skills = contracts with hooks in the gate (built);
- `6ae0cbe9df28755d` DECIDED: opencode as the worker harness (deepseek v4 flash economics)
- `497abf139d49369e` Source integration (2026-08-06): prime-agent (RLM+Continual Harness) and Mastra Observational Memory (observer+reflector, dense observation log) — both researched, dispositions in wayfinder D8
- `98478c8b1daccdeb` ADOPTED: prototype rule 6 — capture prototype as primary source on a THROWAWAY BRANCH with a context pointer on the implementation issue; main keeps only the validated decision. Adds provenance.
- `8ffcf6eceee36cf2` ADOPTED: writing-great-skills renamed to writing-for-agents, scope widened to anything agents read (skills, AGENTS.md, CLAUDE.md, docs); ported doc-writing machinery (context pointers, two loads context/cognitive, information hierarchy, leading words, negation, no-ops test) as reference while KEEPING our kernel-seam machinery (contract, one-owner, hooks, versioning — which Matt lacks).
- `da7d898b4b623136` ADOPTED: to-questionnaire — new repo-local, hook-bound skill for decisions a THIRD PARTY holds: decision the user can't answer becomes a questionnaire DOC (async), grill only the SEND (who + what you need back), most-important-first, one idea per question, disable-model-invocation. Composes with domain-modeling.
- `3cb84b83835fb03b` ADOPTED: wait-what — new global mode skill for session clarity when a message did not land: re-pitch using context + ASD-STE100 simplified English + ubiquitous language from CONTEXT.md. Complements caveman; no hook needed.
- `1177064e1ab14cee` LAB-TEST: prototype LOGIC branch — build one shareable HTML prototype (free-play + tabbed walkthroughs, drivable by a non-developer) vs our TUI on the next logic-prototype task; no observed local TUI failure so no forced change.
- `e38ecd5bb62762ce` DEFERRED: wizard — trigger is the next multi-step HUMAN-ONLY provisioning (GH runner setup, domain/credential provisioning, CI secrets); boundary rule never-for-steps-the-agent-can-perform is its transferable mechanism.
- `4d375d777705576e` DEFERRED: research skill — trigger is an auto-researcher worker (AGI Phase 2); wayfinder track pattern covers research ad-hoc today.
- `3358e0f2e6ca0096` REJECTED: grilling rounds (our domain-modeling is already a superset, only missing non-blocking fact dispatch), and catalog/marketplace/distribution (popularity is not a mechanism; our enforced kernel registry with hooks/one-owner/versioning is deliberately stronger).
- `282a35001a555f06` Pipeline map (ORIENT/PLAN, DECIDE, RESEARCH, BUILD, VERIFY, KNOWLEDGE, ORCHESTRATE, MODE) shows the real gap is RESEARCH; name-overlaps were not where gaps were. to-questionnaire closed the only chain-break in DECIDE.
- `ccc01d856b49501a` Skill-layer changes are USER-governed (HITL): consumer and sole writer is the USER; the falsifier is gate-bound — mini-agi skill verify --all must stay 14+ PASS and one-owner lint must pass after any skill edit; to-questionnaire hook must verify the template + grill-the-send rule.
- `54652132a3129b58` D8 dispositions (2026-08-06) live per-decision in entry 2026-08-06-003 (F-001..F-010); this meta-summary is superseded by them
- `c93ad27aa8c7bb3a` deepseek-v4-flash API pricing (USD per 1M tokens, official DeepSeek pricing page, fetched 2026-08-06): input cache-hit $0.0028, input cache-miss $0.14, output $0.28. Source: https://api-docs.deepseek.com/quick_start/pricing
- `b9a0c323a1475dc2` The deepseek-v4-flash API alias currently points at model version DeepSeek-V4-Flash-0731 (confirmed on DeepSeek's pricing and 'Your First API Call' docs pages).
- `d5b02e7e2030b2b9` DeepSeek officially warns it plans to raise overall API pricing significantly in the near future, subject to official notice — so deepseek-v4-flash rates are time-sensitive and only valid until the pricing page changes.
- `e9e5cfc1d0b1386a` deepseek-v4-flash pricing has exactly three cells per model (cache-hit input, cache-miss input, output) — cache-hit vs cache-miss is the only split; there is no separate standard-vs-thinking input tier, though thinking mode is billed at the same input/output rates.
- `3f23602cd01322dc` Authority collapse is measured in agent-memory consolidation: 48 of 49 evaluated configurations across seven consolidators and seven LLM backbones erode source constraints governing a claim's authorized use when they consolidate; persisting authority labels reduced mean unauthorized-action rate from 16.9% to 0.0% in end-to-end evaluation. Source: AuthMem-Bench, arXiv 2608.01679.
- `faedc0f0a4b0184b` Consolidation rewrites hedged/casual statements into confident dated assertions the agent treats as verified facts; the agent responds to phrasing confidence, not source; a passive 'unverified' tag is ignored, while keeping tentative phrasing rather than upgrading it is the store-level fix; a single redundant source restores correct decisions. Source: 'Manufactured Confidence', arXiv 2606.29279.
- `77e29c7c6707642d` TrustMem verifies each memory-update transition on coverage, preservation, and faithfulness via a Memory Transition Verifier, then uses preference-pair RL over candidate updates; it reduced transition-level omission, corruption, and hallucination by 40.1%, 79.1%, and 50.0% respectively versus strongest per-error baselines. Source: arXiv 2606.25161.
- `4a16c7ba303c62e7` RecMem keeps incoming interactions in a 'subconscious' embedding layer and invokes the LLM to extract episodic/semantic memory only on sustained recurrence of semantically similar interactions; it reduced memory-construction token cost of three SOTA memory systems by up to 87% while exceeding their accuracy. Source: ACL 2026 Findings, arXiv 2605.16045.
- `9a057f6afaa72539` D-MEM decouples short-term interaction from cognitive restructuring via Fast/Slow routing on Reward Prediction Error: routine low-RPE inputs bypass or cache in an O(1) buffer, high-RPE inputs (factual contradictions, preference shifts) trigger the O(N) memory-evolution pipeline; reduces token consumption over 80% and removes the O(N^2) write-latency bottleneck of append-and-evolve systems like A-MEM. Source: arXiv 2603.14597.
- `ccecea1af23feed5` Mem0's automatic extraction is additive by default: on changed facts it stores the new fact without silently rewriting the old one; correction requires explicit update/delete operations; the store separates SQL (facts/metadata as source of truth), vector (embeddings), and entity stores, fusing semantic, keyword, entity, and temporal retrieval signals. Source: Mem0 docs 'How Mem0 Works'.
- `5e22ecc2aa426efa` Mem0's architecture dynamically extracts, consolidates, and retrieves salient information; on LOCOMO it beat all baselines across single-hop/temporal/multi-hop/open-domain questions with 26% relative LLM-as-judge gain over OpenAI, ~91% lower p95 latency, and >90% token savings; the graph variant adds ~2% overall. Source: 'Mem0: Building Production-Ready AI Agents', arXiv 2504.19413.
- `92823ba871a2eaae` Graphiti (Zep's open-source engine) treats each fact as having a validity window: changed information is invalidated, not deleted; every entity/relationship traces back to the raw episodes that produced it; new data integrates incrementally without batch recomputation; contradictions are handled by automatic fact invalidation with temporal history preserved (bi-temporal tracking). Source: getzep/graphiti README.
- `b6fef0270ed8d28d` Graphiti outperformed MemGPT on Deep Memory Retrieval (94.8% vs 93.4%) and achieved up to 18.5% accuracy gains with 90% lower latency on LongMemEval, especially on cross-session synthesis. Source: 'Zep: A Temporal Knowledge Graph Architecture for Agent Memory', arXiv 2501.13956.
- `e794ca1248d1924c` HippoRAG 2 frames memory as non-parametric continual learning, integrating new passages online into a knowledge graph with Personalized PageRank, staying cost/latency efficient and exceeding standard RAG on factual, sense-making, and associative memory tasks (+7% associative over a SOTA embedding model). Source: ICML 2025, arXiv 2502.14802.
- `8014b8b82d793b73` A-MEM (Zettelkasten-style agentic memory) creates structured notes with links and enables memory evolution: integrating a new memory can trigger updates to contextual representations/attributes of existing memories. Source: NeurIPS 2025, arXiv 2502.12110.
- `075b797922584daa` GAM explicitly decouples memory encoding from consolidation: ongoing dialogue lives in an event-progression graph and is integrated into a topic-associative network only on semantic shifts, minimizing interference while preserving long-term consistency (SOTA on LoCoMo and LongDialQA). Source: arXiv 2604.12285.
- `7087735a64f76c95` TiMem consolidates raw conversational observations through a Temporal Memory Tree into progressively abstracted persona representations with semantic-guided consolidation (no fine-tuning), reaching 75.30% on LoCoMo / 76.88% on LongMemEval-S while cutting recalled memory length 52.20%. Source: ACL 2026 Findings, arXiv 2601.02845.
- `40e1e6b577d42b61` MemoryOS uses a hierarchical short/mid/long-term store: short-to-mid updates via a dialogue-chain FIFO principle, mid-to-long via segmented-page organization (+49.11% F1, +46.18% BLEU-1 over baselines on LoCoMo). Source: arXiv 2506.06326.
- `0b44c39d153c71e2` A biologically-grounded agent memory architecture combines sleep-phase consolidation, interference-based forgetting, engram maturation, reconsolidation upon retrieval, entity knowledge graphs, and hybrid multi-cue retrieval; deduplication-based consolidation achieved 97.2% retention precision with 58% store reduction (+21.8 pp over baseline). Source: 'Human-Inspired Memory Architecture for LLM Agents', arXiv 2605.08538.
- `8742eae87151033d` A position paper argues current agent memory (vector stores, RAG, scratchpads) implements lookup, not memory: drawing on Complementary Learning Systems theory, biological intelligence pairs fast hippocampal exemplar storage with slow neocortical weight consolidation, and current AI agents implement only the first half. Source: 'Contextual Agentic Memory is a Memo, Not True Memory', arXiv 2604.27707.
- `a668572194f1c30e` The TMLR survey 'A Survey of Agent Memory in the Second Half' reports hundreds of agent-memory papers in 2025 and frames memory as the substrate of self-evolution: short-term memory gates which experiences are abstracted, long-term memory consolidates them into reusable knowledge and skills, and memory management itself is becoming a trainable capability. Source: arXiv 2602.06052.
- `cf99d7577e330864` Reported consolidation numbers across the research are self-reported on benchmark-specific setups (LoCoMo, LongMemEval, DMR, MemAgentBench, HaluMem); there is no cross-system same-harness comparison of consolidation correctness, and authority-preservation survival in a graph-based store (vs flat facts) is only tested in the flat-fact setting.
- `866588d8e2efd4e0` SQLite allows multiple processes to do SELECT at the same time, but only one process can make changes to the database at any moment; it uses reader/writer locks and writers lock the whole database file during their update; on lock conflict the default is SQLITE_BUSY, adjustable via a busy handler/timeout.
- `601ae8b56f00daf0` SQLite supports an unlimited number of simultaneous readers but only one writer at any instant; the explicit guidance for many concurrent writers is to choose a client/server DBMS.
- `681c9467e108c168` SQLite defines five lock states (UNLOCKED/SHARED/RESERVED/PENDING/EXCLUSIVE) for rollback mode, including writer-starvation avoidance via PENDING locks.
- `ec84de5da723d4a8` SQLite WAL mode lets readers and writers proceed concurrently, but since there is only one WAL file there can only be one writer at a time.
- `510db6dc2a1720ad` SQLite WAL does not work over a network filesystem; all processes using a database must be on the same host computer because the wal-index lives in a shared-memory mmap.
- `d86f43007ae0e974` The SQLite wal-index is backed by a file mmapped in the same directory as the database; processes with a different root directory (via chroot) will see different files and use different shared memory areas, leading to database corruption — a chroot'd sandbox worker cannot reliably share a WAL-mode store on the same filesystem path.
- `4ab516db7ccfc98e` Even in WAL mode SQLite queries can return SQLITE_BUSY: when another connection holds exclusive locking mode, while the last connection does close/cleanup, or while the first connection after a crash holds the recovery lock.
- `e39035055344f0fc` SQLite checkpoint starvation: if concurrent overlapping readers mean there is always at least one active reader, checkpoints never complete and the WAL grows without bound, causing excess disk use and slow queries.
- `3c51acc3d4b42092` The SQLite WAL-reset corruption bug only affects WAL-mode databases with two or more connections open on the same file in separate threads or processes that attempt to write or checkpoint at the same instant; it is present in all versions 3.7.0–3.51.2, fixed in 3.51.3 (2026-03-13), and rare with low probability.
- `8529de285f71e224` SQLite rollback mode gives atomic commit via a rollback journal; a 'hot journal' left by a crash is automatically rolled back by the next process to open the file, and the commit point is journal deletion.
- `87458eb593e72446` If an application, OS, or power failure occurs mid-transaction, SQLite's partially written transaction should be automatically rolled back the next time the database file is accessed; SQLite is 'not immune' to corruption.
- `22b21011428f4c33` SQLite recovery breaks if journals are interfered with: if hot journal files are moved, deleted, or renamed after a crash or power failure, automatic recovery will not work and the database may go corrupt.
- `f0576e80a8acf6c5` PRAGMA synchronous=OFF can corrupt an SQLite database on OS crash/power loss; SQLite should always be run with its default synchronous setting of FULL; WAL is more forgiving of out-of-order writes than rollback mode (only checkpoint needs a correct sync).
- `3758f98022d19fbb` SQLite database files are ordinary disk files; any process can open the file and overwrite it with garbage, there is nothing the SQLite library can do to defend against this, and there is no built-in access control — protection is whatever the OS filesystem permissions provide.
- `5b306c225ec47f04` POSIX advisory locking is buggy/unreliable on NFS; two or more clients might try to modify the same part of the same SQLite database at the same time, resulting in corruption.
- `344882f912fc587f` A close() on any file descriptor for a SQLite file in one thread cancels the POSIX advisory locks held by all threads of the process, which can lead to two or more threads or processes writing to the database at the same time and corrupting it; only partially fixed defensively in 3.51.0.
- `029d332977786104` Linking two copies of SQLite into one application, using two different locking protocols, unlinking/renaming the file while open, and multiple hard/symlinks to the file are all documented sources of SQLite corruption.
- `a7f8334d85e65d51` Do not open an SQLite database connection then fork() then use that connection in the child process — locking problems result and the database can easily end up corrupt; under Unix an open SQLite database should not be carried across a fork() into the child process.
- `221140d4511ffebf` A read-only mount of a WAL-mode SQLite database fails unless the -shm/-wal files already exist and are readable, or the directory is writable, or the database is opened immutable.
- `40a6f5c1783caef7` OpenAI Agents SDK session guide assigns: SQLiteSession to local development and simple apps, AsyncSQLiteSession to async dev, RedisSession to shared session memory across multiple workers or services, SQLAlchemySession to production apps with existing databases (PostgreSQL, MySQL, SQLite, etc.), MongoDBSession to multi-process horizontally-scalable session storage, and DaprSession to production cloud-native deployments; the documented production path for concurrent workers is a shared-server DB or a single long-running app server that serializes access, not a shared SQLite file.
- `08460d217f2ef14e` SQLAlchemy sessions use SQLAlchemy to provide a production-ready session implementation, allowing use of any database supported by SQLAlchemy (PostgreSQL, MySQL, SQLite, etc.).
- `f273b2d3255aaefa` OpenAI Agents SDK handles concurrency by identifying sessions by session_id (a row key), not by file ownership; multiple sessions share one conversations.db and different agents can share the same session.
- `7fb535112327a3fb` LangGraph makes the same split explicitly: SqliteSaver is 'ideal for experimentation and local workflows' while PostgresSaver (used in LangSmith) is 'ideal for use in production'.
- `8fa553364e85a282` Estimate/opinion: neither OpenAI Agents SDK nor LangGraph documents 'many sandboxed workers sharing a single SQLite file' as a supported pattern; their documented answers are (a) serialize through one server, or (b) move to Redis/Postgres/MongoDB/Dapr; the single-file SQLite store is documented as the local/dev topology.
- `d8bd2c0e4ce8fe9c` LangGraph writes a checkpoint at each super-step boundary and persists per-node writes to a checkpoint_writes table so that if one node fails, successful nodes' writes are durable ('pending writes') and are not re-run on resume; resume restarts from the last successful step.
- `5ff5c00c83663882` LangGraph durability modes trade recovery vs. performance: 'exit' persists only on exit (no mid-execution recovery after a process crash), 'async' persists during the next step (small loss window on crash), 'sync' persists every checkpoint before the next step (high durability, performance cost).
- `4a7619de535ac362` OpenAI sandbox agents recover sandbox work via serialized session_state and workspace snapshots (SnapshotSpec); in the SDK-owned lifecycle the runner persists snapshot-backed workspace state on shutdown, and stop() explicitly checkpoints the live workspace mid-lifecycle.
- `5f26c9330e251481` At the SQLite layer crash recovery is the automatic hot-journal / WAL replay; framework-layer checkpoint/resume sits on top of that file-level guarantee.
- `3912dd0a304826cb` OpenAI Agents SDK automatically tracks token usage for every run: requests, input_tokens, output_tokens, total_tokens, per-request entries, and input_tokens_details.cached_tokens / output_tokens_details.reasoning_tokens; aggregated across tool calls and handoffs; usage is per-run and independent, and previous messages may be re-fed as input to each run which affects the input token count in subsequent turns.
- `2d020d15e3ee5a95` Persisting cost telemetry into the store is application-driven, not automatic: OpenAI AdvancedSQLiteSession provides detailed usage analytics by storing token usage per conversation turn, entirely dependent on the store_run_usage method being called after each agent run, into a turn_usage table keyed by (session_id, branch_id, user_turn_number).
- `b1b91935c25b467a` Anthropic returns a usage object on every Messages response described as 'Billing and rate-limit usage': input_tokens, cache_creation_input_tokens, cache_read_input_tokens, output_tokens; total input tokens is the summation of input_tokens, cache_creation_input_tokens, and cache_read_input_tokens, and output_tokens is the inclusive, authoritative total used for billing; token counts will not match one-to-one with the exact visible content.
- `c4900ca5b5630d45` Anthropic token counting (/v1/messages/count_tokens) is explicitly an estimate ('The token count is an estimate') and free, and you are not billed for system-added tokens.
- `8941b4abb47a94d2` Anthropic telemetry pitfall: models since Opus 4.7 use a newer tokenizer producing approximately 30 percent more tokens for the same text, so token counts are not portable across models — don't reuse token counts measured on a model before Claude Opus 4.7 to estimate costs.
- `59054a5c043328ad` OpenAI Agents SDK tracing records generation/function/tool spans with usage by default; in long-running workers the batch exporter flushes every few seconds, so for per-job delivery guarantees call flush_traces() after the trace context exits.
- `0f5173c2ca304d27` OpenAI SDK architecture keeps state out of the sandbox by design: the outer runtime owns approvals, tracing, handoffs, and resume bookkeeping, while the sandbox session owns commands, file changes, and environment isolation; the conversational Session is explicitly distinct from a sandbox session.
- `df2131dec381bc98` Sandbox manifests treat path grants as trusted config: treat manifests that contain extra_path_grants as trusted configuration, do not load grants from model output or other untrusted payloads unless the application has already approved those host paths, and read_only=True is recommended for host data the sandbox should not modify.
- `cc40e2b691349c43` Both OpenAI Agents SDK and LangGraph document encrypting persisted session state at rest: LangGraph EncryptedSerializer for checkpoints, OpenAI EncryptedSession wrapper (encryption + TTL on top of another session).
- `0ed192e5705ef919` Unverified/opinion: whether a single SQLite file is a viable store for many concurrent sandboxed workers is not documented by any vendor as a blessed topology; production frameworks instead document sharing via Redis/Postgres/MongoDB/Dapr or a single server, with SQLite reserved for dev/local.
- `690fc27f845ea9eb` SQLite labels the WAL-reset bug's real-world occurrence rate as 'less than or equal to the expected occurrence rate of SSD malfunctions'; no reproducible stress evidence surfaced from primary sources.
- `a1cb6948c6bd72ed` No system in the surveyed literature uses a literal "nightly cron"; "nightly" is approximated by daily-frequency batch reflection (Generative Agents) and idle-time scheduled consolidation (Letta sleep-time compute), while continuous background consolidation is approximated by per-event incremental ingestion (Zep/Graphiti, Mem0, A-MEM).
- `e7eb9557db61d4e7` Generative Agents (Stanford 2023) batch reflections are generated when the sum of importance scores for the latest perceived events exceeds a threshold of 150; in practice the agents reflected roughly two or three times a day [S4] §4.2.
- `3fbb3ac04aaf7727` Generative Agents ablation (TrueSkill): no-reflection μ=26.88 (σ=0.69), no-reflection-and-no-planning μ=25.64 (σ=0.68), crowdworker baseline μ=22.95, no-memory/no-reflection/no-planning μ=21.21; full architecture vs no-memory baseline gives Cohen's d=8.16; authors conclude "each of these components is critical to strong performance" [S4] §5.
- `6a1beb5a9e4c7be0` MemGPT uses "timed events that are run on a regular schedule (allowing MemGPT to run 'unprompted' without user intervention)" [S1] §2.4.
- `3997ff446a6ec077` MemGPT consolidation is the queue manager's recursive summarization: on memory pressure (warning at ~70% of context) the agent self-edits working/archival memory; at flush (~100%) it evicts ~50% of the window and "generates a new recursive summary using the existing recursive summary and evicted messages" [S1] §2.2.
- `dfa6bac8d91fe7f9` MemGPT DMR accuracy 93.4% (GPT-4 Turbo + MemGPT) vs 35.3% (recursive summarization alone) [S1] §3.1.1; MemGPT reports no separate consolidation cost figures.
- `75149e672ee23e31` Letta sleep-time compute (2025) is idle-time batch consolidation: two agents, the sleep-time agent owns the memory-edit tools and rewrites the primary agent's in-context memory blocks; it is "the closest published analogue to 'nightly'" [S3].
- `c73782bb8c584f4f` Letta sleep-time memory formation is explicitly contrasted with MemGPT's incremental form: "Sleep-time agents on the other hand can continuously improve their learned context to generate clean, concise, and detailed memories" [S3].
- `8935e088219cd985` Letta sleep-time frequency is configurable: "The higher the frequency setting, the more tokens your agent will use" [S3].
- `31f481abaa8a4578` Letta measured effect (paper): "sleep-time compute can reduce the amount of test-time compute needed to achieve the same accuracy by ~5x on Stateful GSM-Symbolic and Stateful AIME and ... scaling sleep-time compute we can further increase accuracy by up to 13% on Stateful GSM-Symbolic and 18% on Stateful AIME"; on Multi-Query GSM-Symbolic "we can decrease the average cost per query by 2.5x" [S2] abstract.
- `56ffb2187d17cbe9` Zep/Graphiti dynamic community extension "gradually diverge[s]" from a full label-propagation run, so "periodic community refreshes remain necessary"; the dynamic path exists because it "significantly reduces latency and LLM inference costs" [S5] §2.3; design rationale is continuous incremental updates for cost/latency, periodic batch recompute for quality.
- `ca509f76052d6818` Zep/Graphiti per-message ingestion: each episode triggers entity extraction (with prior n=4 messages for context), entity resolution, fact extraction, deduplication, temporal extraction, and LLM-based edge invalidation on contradictions; "Graphiti consistently prioritizes new information" [S5] §2.1–2.2.
- `cba421bad5c5a34c` Zep vs full-context baseline on LongMemEval (~115k token conversations): accuracy +15.2% (gpt-4o-mini) and +18.5% (gpt-4o); latency 31.3s→3.20s and 28.9s→2.58s ("reducing response times by approximately 90%"); context tokens 115k→1.6k [S5] §4.3.2.
- `30d49ce68046cd2f` Zep caveats the paper itself reports: on DMR Zep scores 94.8% vs MemGPT's 93.4% but the full-conversation baseline scored 94.4% (gpt-4-turbo) and 98.0% (gpt-4o-mini), so the DMR gap is within noise; Zep declined on single-session-assistant questions (−17.7% gpt-4o, −9.06% gpt-4o-mini) [S5] §4.2, §4.3.2.
- `c804819854579d9d` Mem0 does incremental consolidation at write time plus an async background summarizer: extraction runs per message pair; the update phase runs an LLM tool call per candidate fact choosing ADD / UPDATE / DELETE / NOOP; the "asynchronous summary generation module that periodically refreshes the conversation summary ... operates independently of the main processing pipeline" [S6] §2.1.
- `391ec99299379c6c` Mem0 cost vs full-context on LOCOMO: full-context p95 latency ≈17.1s (p50 9.87s); Mem0 p50 0.708s / p95 1.440s; graph variant p50 1.091s / p95 2.590s; paper claims "91% lower p95 latency and saves more than 90% token cost" vs full-context [S6] abstract, §4.4.
- `e6486c4c6d13d9fe` Mem0^g reaches the highest LLM-judge score among memory systems (J≈68.4 overall); "26% relative improvements in the LLM-as-a-Judge metric over OpenAI" [S6] abstract, §4.4.
- `86e0523aa7a21428` On LOCOMO open-domain questions Zep beat Mem0 (J 76.60 vs 72.93) — a cross-vendor result, not a batch-vs-continuous one [S6] §4.1.3.
- `de06970103940658` A-MEM (2025) does per-interaction continuous evolution: each new interaction triggers note construction (LLM keywords/tags/context), top-k link generation, and "memory evolution" that rewrites the contextual representation of existing memories [S7] §3.1–3.3.
- `a6c8b00124bf07c1` A-MEM cost: "approximately 1,200 tokens per memory operation, achieving an 85–93% reduction in token usage compared to baseline methods (LoCoMo and MemGPT with 16,900 tokens)"; "each memory operation costing less than $0.0003"; processing "5.4 seconds using GPT-4o-mini and only 1.1 seconds with locally-hosted Llama 3.2 1B" [S7] §4.3.
- `e9488afde6379a28` A-MEM retrieval scales to 1M memories at 3.70 µs with linear storage [S7] §4.6; on DialSim F1 3.45 vs LoCoMo 2.55 and MemGPT 1.18 [S7] §4.3.
- `ecda50a3261745b3` No published batch-vs-continuous head-to-head exists: all measured quality/cost differences in [S1]–[S7] are within-system comparisons against full-context or RAG baselines; none of the sources reports the same system under both nightly-batch and continuous consolidation; the closest evidence for the trade-off is Zep's design claim that continuous dynamic extension trades graph quality (periodic refresh needed) for latency/LLM cost [S5] §2.3.
- `576acfc982bfd532` Cost accounting is incomparable across papers (tokens vs latency vs USD, different benchmarks/models).
- `5993a2bf47e80f46` "Nightly" as such is not used by any source; the closest are "~2–3 times a day" ([S4]) and configurable idle-time ([S3]).
- `8718ee8dce4540b4` A controlled ablation running one system under per-event vs daily-batch consolidation on a fixed benchmark with identical token/latency metering, scored on a staleness-sensitive benchmark (e.g., LongMemEval knowledge-update/temporal categories), does not yet exist in the sources reached.
- `fcf2eb13f45ee48a` The full-architecture μ value in the Generative Agents technical evaluation fell in a section the fetch truncated; only ordering and the Cohen's d are quoted for it [S4].
- `1d998782a2b7e1ae` LangSmith annotation queues group runs or whole threads, prescribe a rubric ("Instructions" + feedback keys), and track reviewer progress; queues present one item at a time ("single-run") or two runs side-by-side ("pairwise"), with item states Needs Review → Needs Others' Review → Completed.
- `7f9242706e36c133` LangSmith annotation-queue collaborator settings let an item be held until every workspace member marks it Done, or require a set number of reviewers per run, and can name assigned reviewers explicitly; reservations lock an item to a reviewer for a set time and expire/release it if the reviewer leaves without marking Done, so the number of items visible to each reviewer can differ from the total queue size.
- `8fb8a22a126c8ff7` Langfuse annotation queues require a Score Config defining scoring dimensions, can be assigned to users, accept traces/observations/sessions singly or in bulk, and are processed fully keyboard-driven (navigation + scoring keys).
- `693010a524f0a9fb` Braintrust human review defines review scores (categorical, continuous, free-form) that appear in logs/experiments/datasets, and manages review work via a dedicated Review page with views (Awaiting review, Assigned to me, Completed), SQL filtering, and a kanban layout (Backlog / Pending / Complete); rows can be assigned to team members who get email (and optional Slack DM) notifications.
- `49b7fb8236915e10` Braintrust stores each reviewer's scores independently; continuous/categorical scores are averaged on the parent span, free-form scores require a reviewer (or admin) to pick a value via "Set as base span value", and an "N total reviews" indicator shows agreement vs. divergence; Braintrust explicitly does not support blind review.
- `2d77bf15f0089c2f` LangSmith's per-run reviewer thresholds and reservation locking prevent two reviewers from grading the same comparison simultaneously in pairwise queues.
- `fe7f8b67b4c17d62` LangSmith automation rules are defined as filter + sampling rate (0–1) + action, with actions including Add to annotation queue, Add to dataset, Trigger webhook, Extend data retention, run online evaluator, and Trigger alert, in a fixed ordering (queue → dataset → webhook → evaluator → alert); documented examples include sending all traces with negative feedback or 10% of all traces to an annotation queue for spot checks.
- `df8b4bc391d54d28` LangSmith docs suggest routing runs that already carry user feedback (e.g., thumbs-down) into an annotation queue for triage.
- `fe8d202885bb2379` Braintrust Topics auto-classifies production logs by task, sentiment, issue type, and custom patterns; logs can be filtered by topic classification (SQL on classifications.Task.label, etc.), matching logs assigned to reviewers, and topic-matching scorers run online with a sampling rate via automations.
- `df988dfd099e2766` Datadog Agent Observability "Patterns" performs automated hierarchical topic clustering of production traffic to surface user intents and coverage gaps; "Insights" runs outlier detection across span name, workflow type, and Patterns topics, surfacing anomalies in the UI for the selected time window.
- `ecb39c6174cac241` LangSmith Engine automatically detects recurring issues in traces, diagnoses root cause, and proposes resolution, as an alternative to manually triaging every failed run.
- `1b415f6ca1c96c6c` LangGraph implements HITL via dynamic interrupts: the graph pauses at interrupt(...), returns an interrupt payload to the caller, and resumes by posting a Command(resume=...) with the human's input; static interrupts (interrupt_before/after) exist but the docs recommend against them for HITL, calling them debug/test tools.
- `304e41006f05da56` LangGraph time travel lets an operator list checkpoints (get_history), mutate state (update_state), and resume from the edited checkpoint.
- `cb86a545057edd22` LangSmith alerts are threshold-based on aggregated metrics (run count, cost, errors count or %, average feedback score, latency) over a configurable 5 or 15 minute window, are project-scoped, can be further scoped by a filter builder (status/run type/tag/error), and route to Slack, PagerDuty, Dynatrace, or any webhook.
- `7cfe9661cd15e22a` LangSmith alert noise controls documented in-source are PagerDuty dedup (to receive the same alert again within an hour you must resolve the active incident) and a webhook payload alert_rule_id documented as a de-duplication key in the webhook service; best practices listed are starting with broader thresholds, adjusting sensitivity to criticality, and routing to appropriate on-call.
- `b3d72fc2c65ef3e3` Langfuse monitors are a severity state machine (OK / WARNING / ALERT / NO_DATA / PAUSED); alerts fire only on severity transitions, Renotify is off by default (re-firing every N minutes only if enabled), and no-data handling can suppress alerts (keep previous severity, show NO_DATA with no alert, or delayed sustained-NO_DATA alert); notifications go through Automations to Slack, HMAC-signed webhook, or GitHub Actions.
- `fe7a1f40036e0381` Braintrust's anti-noise mechanism is a beta daily "Topics digest" posted once a day to Slack covering the previous 24h — summary of activity shift, biggest topic mover, highlight traces, and one takeaway — rather than per-event alerts.
- `e5e1072ffa4930c8` Braintrust score visibility can restrict scores to specific members/groups or show them conditionally (e.g., only when a triage score indicates deeper review is needed), and is documented as a display filter, not an access control.
- `50d864bb40f726a7` OpenAI Agents SDK reduces captured volume rather than notification volume: tracing is on by default but can be disabled per-run, sensitive-data capture (trace_include_sensitive_data) can be turned off, and custom/sampled trace processors can be installed in place of the default exporter.
- `54f9fe2c40ea09b0` OpenAI Agents SDK supervises concurrent agents via grouping: traces carry workflow_name and an optional group_id to link multiple traces from one conversation (e.g., a chat thread id), giving a conversation-scoped view rather than per-call noise.
- `7007a70aaca3b4a3` Langfuse sessions group traces/observations into one session for replay of the full interaction.
- `c94749b55ef6938d` HumanLayer's original HITL-API product is deprecated — the repo README states "the code here is pretty much all deprecated"; the current product (humanlayer.com) is an AI-coding IDE whose marketing positions task/session grouping, real-time session visibility, and cloud daemons as the way to supervise many parallel agent sessions. Only the marketing homepage and repo README were verified; no detailed supervision-docs were reachable (docs.humanlayer.dev transport-error), and its efficacy as a supervision console is an unverified opinion.
- `090f7beeffd3f929` No vendor documentation was verified that quantifies how much notification noise these controls actually reduce — no measured alert-fatigue data was found in primary docs, and no claim about effectiveness is made.
- `d2696f42c303dce8` Arize Phoenix was reached only at its overview (tracing → evals → datasets → experiments); no HITL review-queue or alerting page was found, so Phoenix is not claimed as a review-queue tool.
- `4d21686cc85b19ee` The HITL review-queue surface is standard across LangSmith, Langfuse, and Braintrust: rubric-driven queues over traces/sessions, reservation/locking, reviewer-count or assigned-reviewer completion, and multi-reviewer consensus handling.
- `1686d0bb9b2d028f` Attention routing into HITL queues is overwhelmingly pre-filtering rather than pushing every trace at a human: rule + sampling-rate automations route a fraction of filtered runs into queues (LangSmith), and topic/issue classification feeds assignment and online scoring (Braintrust Topics, Datadog Patterns/Insights, LangSmith Engine).
- `daf69938aa31c832` Notification-noise reduction is documented as threshold aggregation + severity state machines + transition-only firing + dedup keys + daily digest (LangSmith alerts, Langfuse monitors, Braintrust Topics digest), not as per-trace paging.
- `8e916f9507714e83` For supervising parallel agents, grouping surfaces (OpenAI group_id/workflow_name, Langfuse sessions, HumanLayer task/session views) are the documented answer; effectiveness of these mechanisms is unmeasured inference, not a primary-source claim.
- `7412039e83dcc5e5` DPR's dense retriever outperforms a strong Lucene-BM25 system by 9-19% absolute in top-20 passage retrieval accuracy for open-domain QA (Karpukhin et al., EMNLP 2020). Source: arXiv:2004.04906 abstract.
- `59735e32b612df1a` BM25 underperforms dense retrievers on agent-memory fact recall: LongMemEval retriever ablation (Value=Session, K=V) gives BM25 Recall@5=0.634 / NDCG@5=0.516 vs Contriever 0.723/0.634 vs Stella V5 1.5B 0.720/0.594 (Wu et al., ICLR 2025). Source: arXiv:2410.10813v2, Table 9, Appendix E.2.
- `3264c807ce55f96c` BM25 is a robust retrieval baseline; re-ranking and late-interaction models on average achieve the best zero-shot BEIR performances but at high computational cost, while dense and sparse-retrieval models often underperform. Exact per-dataset BEIR nDCG@10 values were not extractable (paper HTML render 404'd). Source: arXiv:2104.08663 abstract.
- `0fb38c56ff147e30` AgentIR (2026 preprint) reports on LoCoMo (n=1,982) that BM25 alone is already the strongest single system and a hybrid cascade can skip the dense channel with no accuracy loss — this is an estimate from a single 2026 preprint, not independently verified. Source: arXiv:2605.25092 abstract.
- `955b38582633ba0a` Dense-only retrieval roughly doubles lexical accuracy on an 18-language benchmark: MIRACL dev nDCG@10 average BM25 31.9 → M3-Embedding Dense 69.2. Source: arXiv:2402.03216, Table 1.
- `50662f026de1acfe` Embedding-based retrieval alone does not close the gap to human fact recall in long conversations: on LongMemEval even commercial memory chatbots with dense memory (ChatGPT GPT-4o 57.7%, Coze 33.0%) drop 37% and 64% vs offline reading of full context (91.8%). Source: arXiv:2410.10813v2, Figure 3a.
- `2c6fb8ddf6a41794` Time-aware memory retrieval measurably improves temporal-reasoning recall: LongMemEval time-aware query expansion improves recall by an average of 11.3% using rounds as the value and 6.8% using sessions (6.8%-11.3% 'when a strong LLM is employed for query expansion'). Source: arXiv:2410.10813v2, Section 5.4/Table 4 and abstract.
- `2372968ceac568ce` Temporal reasoning is the single hardest fact-recall category across agent-memory benchmarks: LoCoMo human temporal F1=92.6 vs best base model GPT-4-turbo=10.4; long-context LLMs lag human levels especially in temporal reasoning by 73%. Source: arXiv:2402.17753, Table 2 + Section 6.
- `abb91cc1c7c944cc` A temporal knowledge-graph memory improves temporal-reasoning QA by a large relative margin over full-context reading: Zep (Graphiti temporal KG) on LongMemEvalS temporal-reasoning, gpt-4o full-context 45.1% → Zep 62.4% (+38.4% rel); gpt-4o-mini 36.5% → 54.1% (+48.2% rel). Source: arXiv:2501.13956, Table 3.
- `1412c255c7aa37da` TiMem (2026 preprint) claims 75.30% on LoCoMo and 76.88% on LongMemEval-S as 'state-of-the-art' — an estimate from a recent preprint (ACL 2026 Findings claim), unverified and not independently corroborated. Source: arXiv:2601.02845 abstract.
- `e18bd137f36455e4` Entity/graph retrieval gives the biggest measured gains on multi-hop fact integration: HippoRAG R@5 on 2WikiMultiHopQA BM25 61.9 / ColBERTv2 68.2 / HippoRAG 89.1 (+20 points); on MuSiQue 41.2/49.2/51.9 (~+3); QA F1 2Wiki 43.3→59.5 (+16.2), MuSiQue 26.4→29.8. Single-step HippoRAG is 10-30x cheaper and 6-13x faster than IRCoT with comparable/better performance. Source: arXiv:2405.14831, Tables 2-4, abstract.
- `229361857af8028c` Graph memory beats summarization-based and flat memory baselines on conversational fact recall: Zep on LongMemEvalS overall accuracy gpt-4o full-context 60.2% → Zep 71.2% (+11.0 points, ~18% rel; abstract 'up to 18.5%'); gpt-4o-mini 55.4% → 63.8%. On DMR (500 multi-session conversations): Zep 94.8% vs MemGPT 93.4% vs full-context 94.4% vs session summaries 78.6%. Caveat: Zep is vendor-authored and the DMR gap to full-context is small (0.4). Source: arXiv:2501.13956, Tables 1-3.
- `87f0150ad5e164b8` Graphs do not help every category: structured fact extraction can hurt single-session detail recall — Zep regressed on single-session-assistant questions (gpt-4o 94.6% → 80.4%, −17.7% rel). Source: arXiv:2501.13956, Table 3.
- `ebc840082d1b992d` Hybrid (dense + sparse) and fused (dense + sparse + multi-vector) retrieval consistently beat each single signal: M3-Embedding MIRACL nDCG@10 Dense 69.2 / Sparse 53.9 / Multi-vec 70.5 / Dense+Sparse 70.4 / All 71.5; long-doc MLDR Dense 52.5 / Sparse 62.2 / Dense+Sparse 64.8 / All 65.0; NarrativeQA Dense 48.7 / Sparse 57.5 / Multi-vec 55.4 / Dense+Sparse 60.1 / All 61.7. Source: arXiv:2402.03216, Tables 1, 3, 4.
- `999b1d884e08eb37` Hybrid indexing (multiple keys per memory item) improves agent-memory recall: LongMemEval key expansion (K = value + extracted user facts) over K = value gives +9.4% avg recall@k and +5.4% end-to-end QA accuracy; key merging ≫ post-retrieval rank merging (Table 10). Source: arXiv:2410.10813v2, Section 5.3, Tables 3, 10.
- `eab86d0eeedfb0ca` Production agent-memory systems combine all three retrieval signals: Zep runs Okapi BM25, cosine embedding similarity, and breadth-first graph search in parallel, then reranks (incl. Reciprocal Rank Fusion and cross-encoder rerankers). Source: arXiv:2501.13956, Sections 3.1-3.2.
- `c607d1ffd2eb99af` RRF's superiority over other fusion methods is a referenced method claim (Cormack et al., SIGIR 2009, cited via Zep Section 3.2) — not independently fetched this pass; treat as referenced, not verified. Source: Zep, arXiv:2501.13956, Section 3.2.
- `65c5ee1c3d7613bb` Conversation-store granularity interacts with fusion — what you retrieve matters as much as how: LoCoMo RAG with GPT-3.5-turbo-16k overall QA F1 none 22.4 / dialog top-5 31.7 / summary top-5 32.5 / observations (facts) top-5 41.4; 'a noticeable 5% improvement … when the input is top 5 relevant observations instead of pure conversation logs'. Source: arXiv:2402.17753, Table 3 + Section 6.1.
- `dd3d81d3e6c5124a` Long-context/retrieval gains are large relative to base LLMs but far from human: LoCoMo improvements of 22-66% from long-context/RAG yet still lag human by 56%; human overall F1 87.9 vs best long-context 37.8 (GPT-3.5-turbo-16k @16k tokens) vs best RAG 41.4. Source: arXiv:2402.17753, Tables 2-3 + Section 6.
- `0b6bcd41fb0c4fef` Retrieval reading strategy is a separate measurable lever: LongMemEval, even with perfect retrieval, Chain-of-Note + structured data format improves QA accuracy by as much as 10 absolute points across three LLMs; on LongMemEvalS long-context LLMs drop 30-60% vs oracle retrieval. Source: arXiv:2410.10813v2, Section 5.5 + Figure 3b.
- `68944cdb3db0a2fc` Retrieval techniques are complements, not alternatives — the biggest gains come from stacking them: Zep = temporal KG + hybrid lexical/dense/graph retrieval; HippoRAG = KG + embeddings + PPR; LongMemEval's best config = session decomposition + fact-augmented keys + time-aware query expansion. Sources: arXiv:2501.13956; arXiv:2405.14831; arXiv:2410.10813v2.
- `832a7c538944c10a` Cross-benchmark retrieval numbers are not comparable; each row's comparison is only valid within its own benchmark/model. A single head-to-head benchmark running all five retrieval strategies (BM25, dense, recency-aware, entity graph, hybrids) on the same agent-memory QA set with the same reader LLM does not yet exist in peer-reviewed sources; LongMemEval's retriever ablation (Table 9) is the closest existing partial comparison.
- `57e2ad1e6fc9afd9` LoCoMo's paper is machine-converted HTML; one table cell (Dialog k=10 Multi-Hop R@k = 247.4) is a rendering artifact and was treated as 24.7. PDF-only sections were not read; all cited numbers come from abstracts or HTML-rendered full text.
- `97b9c2be82edddfc` Zep, M3-Embedding, and HippoRAG benchmark numbers are author-reported; Zep is the vendor of the tested product. 2026 preprint headline numbers (AgentIR, TiMem) were verified only at abstract level and could not be independently corroborated; several look implausibly high next to peer-reviewed baselines.
- `38fcbdc4541b046e` GitHub routes code review requests to specific people/teams and gives each reviewer a filterable 'awaiting your review' list; each PR is decided with one of three discrete outcomes: Comment, Approve, Request changes.
- `b3cea14ec5ed51db` GitLab shows an approval-state icon on each merge request in the project/group MR list ('required approvals missing' vs 'approvals satisfied') and a widget on the MR showing per-reviewer state (Awaiting review / In progress / Approved / Commented / Requested changes); an eligible reviewer approves from the widget or via the '/approve' quick action.
- `15a5494102fc23e9` Stripe Radar presents a review queue ('a prioritized list of completed or to-be-captured payments') in two modes: a list view to scan without opening details, and a detailed view with customizable risk context; operators navigate with J/K keys or Previous/Next buttons and act on each item (Approve / Refund / Refund-and-report-fraud).
- `80236c7a54854694` AWS CodePipeline's Manual approval action stops a stage; approvers see it in the console with an optional review URL, comments, and an SNS notification when it is ready.
- `7dc38bcaf329e110` Google Cloud Deploy shows a Review link in the delivery-pipeline visualization when a rollout is pending approval; each pending rollout opens an Approve rollout screen with a Manifest diff tab showing exactly what changed versus the current deploy, then Approve or Reject (console or 'gcloud deploy rollouts approve|reject').
- `bd7d76e295f61ec0` Azure Pipelines' ManualValidation@0 task pauses a YAML pipeline and shows a message bar linking to a Manual validation dialog containing the 'instructions'; users with Queue-builds permission choose resume or reject.
- `fb6ff8fd3ecfaeea` Spinnaker's Manual Judgment pipeline stage waits for the user to click Continue, with optional instructions and user-selectable input options that drive downstream pipeline behavior.
- `5940164f5de5669e` LaunchDarkly delivers approval requests to an Approvals dashboard, email, and in-app inbox, plus per-object 'Pending changes' panels and Slack/Teams notifications.
- `6151e2ec3a082fec` Amazon A2I presents review tasks to a configured work team (internal workforce, Mechanical Turk, or AWS-prescreened vendors) when a flow-definition condition fires; the work team sees items via a human task UI, and results land in S3 with CloudWatch Events on completion.
- `db8bc4fd1714d223` GitHub code review auto assignment picks reviewers by one of two algorithms: round robin (who has received the least recent request) or load balance (counts recent and outstanding reviews, aiming for an equal number per member within any 30-day period); members whose status is 'Busy' are excluded.
- `fa9b089b4e75b0cb` GitLab's Automatic reviewer assignment assigns CODEOWNERS automatically; the GitLab Duo Agent Platform strategy chooses the minimum number of reviewers needed to satisfy each approval rule, weighing availability, review workload (open MRs awaiting their review), local time, and recency of activity.
- `c350ba42c71e1f08` A peer-reviewed field study of 1,112 parole rulings by experienced judges found the share of favorable rulings fell from ≈65% to near zero within each decision session and jumped back to ≈65% after a break; the authors argue the depletion comes from the act of deciding, not elapsed time (Danziger, Levav & Avnaim-Pesso, PNAS 108(17):6889-6892, 2011). The observational result is the fact; the causal 'mental depletion' interpretation is the authors' and is contested in later literature.
- `4c1abdad3bec9cb5` Stripe's stated best practice is to focus reviewer time on payments where human judgment adds insight and let automation decide the majority, and to avoid adding a review step where no inherent fulfillment delay exists because it becomes a bottleneck for good customers (vendor guidance, not measured evidence).
- `6a7cf2b037ce0c6e` AWS A2I human-review loops fire on confidence-range conditions (e.g., review a label when its confidence is below X or above Y) or on random sampling of a percentage of items; thresholds are adjustable over time.
- `026757b2f7b5dc33` Stripe Radar lets operators write rules that automatically place payments into review and provides Smart Refunds recommendations ranked by refund-confidence, so operators triage a ranked list rather than scan everything.
- `af4621ee1fa4b90c` MemGPT (paper arXiv:2310.08560 §2.2) separates main context (LLM prompt tokens: system instructions, working context, FIFO queue) from external context (archival storage, a read/write database of arbitrary-length text objects; and recall storage, the message database); messages evicted from the FIFO queue are stored indefinitely in recall storage and remain readable via MemGPT function calls.
- `90b6a3e4a78524a2` MemGPT (arXiv:2310.08560 §2.2) queue manager enforces a warning token count (e.g. 70% of context window) that inserts a system 'memory pressure' warning so the LLM can move data to working context or archival storage, and a flush token count (e.g. 100%) at which it evicts a count of messages (e.g. 50% of context window) and generates a new recursive summary from the existing recursive summary plus evicted messages; the FIFO queue's first slot holds that recursive summary.
- `d4d8a0005c2c4c6a` MemGPT (arXiv:2310.08560 Table 2) on DMR (500 MSC conversations, gpt-4-turbo) reaches 93.4% LLM-judge accuracy vs 35.3% recursive-summarization baseline, 32.1% raw GPT-4 (92.5% with MemGPT), 38.7% GPT-3.5 Turbo (66.9% with MemGPT).
- `185255b5f360bea5` Letta docs describe 'dreaming' (successor of MemGPT): background subagents review recent conversations, consolidate lessons, and update memory without interrupting active work, runnable after a configured number of user messages or on context-window compaction, plus a /doctor audit for placement, duplication, and system-prompt token usage.
- `c115eb02966062a5` The 70%/100%/50% MemGPT eviction numbers are framed as examples ('e.g.') in the paper, not a tuned optimum; no ablation of eviction fraction was found in reviewed sources.
- `9807eb38133c8148` Generative Agents (arXiv:2304.03442 §4.1) retrieval score = alpha_recency*recency + alpha_importance*importance + alpha_relevance*relevance with all alpha = 1; recency is an exponential decay over sandbox game hours since last retrieval (decay factor 0.995); importance is an LLM-assigned 1-10 integer at creation; every memory object carries creation and most-recent-access timestamps; the memory stream is unbounded and decay only re-ranks at retrieval.
- `c730526e5529262e` Mem0 memory decay is a soft ranking bias, never a filter — at worst it scales a candidate's score by 0.3x; scaling band 0.3x-1.5x (approx 1.5x just-accessed, 0.3x floor for months-idle); opt-in per project and off by default; widens candidate pool to top_k x 3 (floor 50); reinforcement per memory capped at most recent 20 touches.
- `7162d35800fc45bf` Mem0 publishes decay mechanics (scaling band, lifecycle stages) but no measured retrieval-quality number (e.g. recall/accuracy delta with decay on/off); effectiveness is asserted, not benchmarked on that page.
- `d75547aec46b3741` Decay-as-ranking-bias (Mem0) and decay-as-recency-weight (Generative Agents) are the only decay mechanisms found in reviewed sources; neither removes data.
- `6ed4eb78bc0729b3` Mem0 default add path is ADD-only extraction: a new fact is stored without silently rewriting an older one; explicit update or delete operations are used to correct or remove a memory.
- `7912bae6582bb109` LangGraph InMemoryStore is a plain dict-backed store with no TTL, eviction, or expiry logic; its docstring says data is lost when the process exits and recommends a database-backed store for persistence; retention policy is delegated to the backing database or application.
- `5f22c03352903571` Mem0 extraction pipeline: step 1 context lookup (checks related existing memories to avoid storing the same fact again), step 3 deduplication and embedding (redundant facts removed, then each memory embedded).
- `a1d9c54ea313be45` Mem0 Dream merge folds duplicates into one canonical memory; the folded record is hidden from reads by default (the canonical memory is returned), retained rather than deleted, and surfaced with include_merged=true; runs as memories are added.
- `2e0933213df8db7b` Zep (arXiv:2501.13956 §2.2) two-stage dedup during ingestion: entity resolution (embedding cosine + full-text candidates then LLM duplicate check) and edge/fact dedup, where hybrid search for relevant edges is constrained to edges between the same entity pairs as the proposed new edge, significantly reducing dedup computational complexity.
- `179f80e4011804dd` A-MEM (arXiv:2502.12110 §3.3) merges new memories into semantically near existing notes instead of deduplicating by deletion: for each near neighbor the LLM decides whether to update its context, keywords, and tags, and the evolved note replaces the original.
- `43f4539098e18753` Dedup happens at write time across systems as a way to keep the store from growing with near-identical facts; the dedup rate (how much growth is prevented) is not reported by any reviewed source.
- `70d2e0f66bef8b38` Zep/Graphiti (arXiv:2501.13956 §2.2.3) uses a bi-temporal model: timeline T (when a fact holds) and T' (transactional ingestion), each edge storing t_valid, t_invalid, t'_created, t'_expired; an LLM compares new edges against semantically related existing edges for contradictions and invalidates temporally overlapping affected edges by setting t_invalid to the invalidating edge's t_valid; Graphiti prioritizes new information; retrieval returns the t_valid/t_invalid range so stale-but-historical facts remain representable.
- `15dffb87cdfc3f1c` Mem0 Dream supersede marks an older memory as superseded and links it to the newer fact; superseded memories are not deleted and not hidden by default (normal search/get still returns them, badged as superseded); latest_only=true filters them out; runs as memories are added on every plan.
- `dca21576c4c8bae6` Zep (arXiv:2501.13956 Table 3) time-aware invalidation improves LongMemEval knowledge-update and temporal-reasoning categories vs full-context reading: with gpt-4o, knowledge-update 78.2% to 83.3% and temporal-reasoning 45.1% to 62.4%.
- `1cebb877f366157c` Invalidation/supersede is the near-universal answer to 'outdated fact' in reviewed systems: keep the history, hide or timestamp the old truth, never destroy it.
- `bd7bc6f9009fc641` Mem0 per-memory expiration: an expiration_date (YYYY-MM-DD, UTC, date-inclusive) stops the memory surfacing in search once the date passes; nothing is deleted — search() and get_all() skip it, fetching by ID still returns it, clearing the date restores it; no date means never expires (default); malformed dates fail open.
- `825009a503cb6034` Mem0 distinguishes three retention policies: Expiration (hides, data kept, reversible), Decay (re-ranks, data kept, never filters), Delete (permanent, irreversible).
- `eee720517a9475e3` Generative Agents (arXiv:2304.03442 §4.2) reflection recursively synthesizes observations into higher-level 'reflections' appended to the memory stream; the stream is compacted in meaning, not in size.
- `06b55f283657c3cb` Mem0 Dream synthesis distills memories into higher-order pattern memories written alongside existing memories, never in place of them, gated on at least 20 memories per user and a cadence (7 days Pro, daily Enterprise).
- `76d2c318013e0da9` Zep (arXiv:2501.13956 §2.3) community subgraphs are periodic summaries of entity clusters (label propagation, dynamically extended); the paper concedes communities gradually diverge from a complete label propagation run, so periodic community refreshes remain necessary — even the summarization tier requires scheduled re-runs as facts accumulate.
- `08ab1305a972ff38` No reviewed source reports a measured compression ratio (bytes or fact-count reduction) for consolidation tiers.
- `ca79380b2a16a6e8` Zep vs MemGPT vs full-context on DMR (500 MSC conversations, gpt-4-turbo): MemGPT 93.4%, Zep 94.8%, full-conversation baseline 94.4%, session-summary baseline 78.6%, recursive summarization 35.3%.
- `91f447d80e6aa73b` Zep on LongMemEvalS (avg ~115k-token histories, retrieving 10-20 facts/entities per query): 63.8% (gpt-4o-mini) / 71.2% (gpt-4o) vs full-context 55.4% / 60.2%; latency 3.20s / 2.58s vs 31.3s / 28.9s (~90% lower); average context tokens 1.6k vs 115k; accuracy improvements up to 18.5% (gpt-4o), 15.2% for gpt-4o-mini.
- `b6926ac40cee68e9` Zep paper caveat (arXiv:2501.13956 §4.2): DMR conversations contain only 60 messages each, easily fitting within current LLM context windows, so DMR barely exercises retention.
- `71b0fc0c3cc953e2` LongMemEval (arXiv:2410.10813, ICLR 2025): long-context LLMs lose 30-60% accuracy reading the full LongMemEvalS history vs oracle retrieval; GPT-4o 0.870 oracle to 0.606 full-history (30.3% drop).
- `028e8effd0ed2845` LongMemEval (arXiv:2410.10813 Fig 3a): commercial memory systems on histories ~10x shorter: ChatGPT (GPT-4o) 0.5773 vs offline reading 0.9184 (37% drop); Coze 0.3299 (64% drop).
- `cf94cceed826d20c` LongMemEval design deltas under growth: fact-augmented key expansion improves memory recall@k by 9.4% and QA accuracy by 5.4%; time-aware query expansion improves temporal-reasoning recall by 6.8-11.3%; Chain-of-Note plus structured format improves QA accuracy by up to 10 absolute points.
- `b42bd1037051c802` LongMemEval storage-granularity finding: compressing sessions into individual user facts harms overall performance due to information loss but improves multi-session reasoning accuracy.
- `073a5ff97a268f5f` A-MEM (arXiv:2502.12110 Table 4) retrieval time grows from 0.31 microseconds (1,000 memories) to 3.70 microseconds (1,000,000 memories) with linear O(N) memory (1.46 MB to 1464.84 MB); per-operation cost about 1,200 tokens vs about 16,900 for MemGPT/LoCoMo baselines (85-93% reduction), measured by the authors on their own system.
- `19d6b1929b157a24` No controlled experiment was found isolating any single retention policy (e.g. decay on/off, eviction threshold, supersede vs delete) with retrieval-quality numbers; such numbers are published only for whole systems (Zep, MemGPT) on fixed benchmarks, not for the retention mechanism in isolation.
- `71bfdcb5d6aea1fa` No reviewed source reports dedup rates, compression ratios, or fact-count growth over time; whether retention actually bounds storage is unanswered in every reviewed source.
- `7c0a290e885b0a7d` Agent memory systems almost universally keep the long-term fact store append-only and unbounded, solving 'growth' by bounding only the in-context tier and by re-ranking/superseding at retrieval: MemGPT/Letta evicts the context queue to a recursive summary while archival/recall storage grows indefinitely; decay exists as a retrieval-time ranking bias and never deletes; dedup is write-time; invalidation/supersede is timestamp-based and non-destructive; per-item expiry exists as soft-hide (Mem0 expiration_date) and is absent from LangGraph's core store.
- `e798cc05679df268` Workspace-trust dialogs gate loaded skills/config: Claude project skills take effect for allowed-tools only after the trust dialog is accepted, and a skill can grant itself broad tool access; Codex loads project config only when the project is trusted.
- `0b90b61b3081c07d` Mini AGI makes memory writes human-signed: contested facts and dream-loop results route through memory signoff / a human queue (ADR-0010 signoff); the reviewer gate is human by design.
- `1ab0e2db5aa0ab3d` Skills can restrict their own invocation and tools: disable-model-invocation: true (only the user can trigger, recommended for /deploy-style skills) and allowed-tools/disallowed-tools frontmatter.
- `59816a658ba4962d` Vendors separate human-written memory (CLAUDE.md) from agent-written auto memory; both are loaded at the start of every conversation and treated as context, not enforcement.
- `44f6ae81403776df` Claude Code auto memory is bounded: per-repo directory ~/.claude/projects/<project>/memory/, MEMORY.md index capped at the first 200 lines or first 25KB; writes over the limit return an error to rewrite the index; a modified ISO-8601 timestamp is stamped on frontmatter.
- `52f81a90784ffeec` Codex memories are opt-in generated state: off by default, stored under ~/.codex/memories/, treated as generated state not hand-edited, secrets redacted from generated fields; docs recommend keeping required rules in AGENTS.md and treating memories as a recall layer, not the sole source for rules that must always apply.
- `e18acaa145958ac6` Claude Code ships ConfigChange hooks that can audit or block settings changes during sessions.
- `2c3f7e1718461895` Mini AGI canonical memory is append-only with provenance on every entry: dated, carries a source, every fact id is sha256[:16]; derived views are generated, never hand-edited, and canonical wins on conflict.
- `57a4ba0fa591b0c8` Mini AGI checkpoint journal records BEGIN/VERIFY-PASS/VERIFY-FAIL/CHECKPOINT-ABORT in memory/episodic/checkpoints.log; a red gate hard-resets to the last BEGIN commit (rollback always lands on the last checkpoint, ADR-0004); the journal must be repaired via checkpoint.sh, never through git.
- `dc228d975c9158da` OpenAI Agents SDK guardrails are model-as-judge: input guardrails run before the main model, output guardrails validate final output, tool guardrails check arguments/results, and a tripwire blocks the run. Boundary caveat: input guardrails run only for the first agent in a chain and output guardrails only at the final-output agent, so checks belong next to the side-effecting tool in manager-style workflows.
- `f84e2ab57f514419` Claude Agent SDK: auto-approved tools never reach canUseTool, so for checks that must run on every call the docs prescribe a PreToolUse hook.
- `853315f48c90e499` Mini AGI reviewers must cite canonical fact ids they rely on; a verdict without memory anchors is flagged and gated (ADR-0003). The OWASP mapping attributes memory-poisoning mitigation to 'Append-only canonical with content-hash fact ids' and a provenance fingerprint in the audit (ADR-0014).
- `6a52b10eed388a69` Codex OTel records codex.tool_decision with approved/denied and source (configuration vs user), enabling post-hoc audit of who authorized each action.
- `ddf4bfc7760221e4` MCP spec: MCP itself cannot enforce security principles at the protocol level; hosts must obtain explicit user consent before invoking any tool, tool annotations should be considered untrusted unless obtained from a trusted server, and implementors SHOULD build robust consent and authorization flows.
- `bfe28bd5ddad1fe0` Mini AGI CI gate fails unless the runner identifies itself as isolated (non-root + RUNNER_NAME), so 'gate requires sandbox evidence' is literal (ADR-0009); the local worker runs under Landlock write-containment confined to workdir + its own state dir (ADR-0012).
- `8c15dbef1c8dcdeb` CoALA (arXiv:2309.02427v3) defines agent memory as working memory plus long-term episodic, semantic, and procedural memory; 'learning' = writing to long-term memory, retrieval = reading it back. [fact]
- `a25a4334cdf452aa` CoALA warns learning by writing to procedural memory is significantly riskier than writing to episodic or semantic memory, as it can easily introduce bugs or allow an agent to subvert designers' intentions. [fact]
- `8835fdd3dfe63826` CoALA contains no treatment of shared memory between agents, memory privacy, or isolation — verified against full text; multi-agent work is mentioned only as grounding/debate/collaboration. [fact]
- `75f9d3c529016fb3` LangGraph's Store is key-value data accessible from any thread, isolated only by a user-chosen namespace tuple; reads match by namespace prefix; the Store API (put/get/delete/search/list_namespaces) has no permission or ownership parameter. [fact]
- `1ba70b1569d53382` LangGraph's contamination boundary is convention, not enforcement — safety relies on every writer scoping to the correct namespace and every reader querying within its namespace. [analysis]
- `6a5c72bff4bd6d67` CrewAI MemoryScope restricts all operations to a branch of the tree; the agent/code using it can only see and write within that subtree. [fact]
- `8aba31b1c97b13fb` CrewAI MemorySlice with read_only=True allows recall from multiple scopes but remember() raises PermissionError; read-write slices require an explicit scope on every write. [fact]
- `9f5e02f20f43e4b7` CrewAI private=True memories are visible on recall only when the source matches; include_private=True is an admin escape hatch. [fact]
- `5025c0afbe426e17` CrewAI serializes concurrent writers at the backend: LanceDB operations are serialized with a shared lock and retried automatically on conflict, handling multiple Memory instances pointing at the same database. [fact]
- `abc0df15df514356` OpenAI Agents SDK sessions are history stores keyed by session_id; different sessions maintain separate conversation histories; sharing is explicit; RedisSession exists for shared memory across workers/services; no per-agent ACL exists. [fact]
- `8eefc75285f050fd` Mem0 scopes memory by user_id/agent_id/app_id/run_id on writes and reads to prevent data mixing; unmentioned entities are not constrained — searching {user_id: alice} does not require agent_id to be null, so cross-tagged records still surface (documented subtlety). [fact]
- `7fd17b84308d883d` Mem0's default extraction attributes facts from user messages with user_id set and agent_id null, assistant messages with agent_id set and user_id null; consequently an AND-filter on user+agent returns nothing for normally-created records — a documented misattribution trap. [fact]
- `336453c5de421eee` Mem0 writes are additive-only by default: new memories are added without overwriting or deleting existing memories; update/delete are separate operations. [fact]
- `2eafea4e6eeb8e76` AutoGen (AgentChat): a Memory (e.g. ListMemory, ChromaDBVectorMemory, RedisMemory) is constructed and passed into a specific agent's constructor; the only way two agents share memory is passing the same instance; no framework-level permission scoping, filtering is via metadata you supply. [fact]
- `b1016827b3e68aa6` LangGraph subgraphs manage their own checkpoint namespace, so the parent does not see subgraph state changes; the docs' prescribed escape hatch for cross-boundary sharing is shared state via Store. [fact]
- `9e2e2cacd723d017` CrewAI per-agent view is a scope path (e.g. memory.scope('/agent/researcher')); slices compose several scopes into one view. [fact]
- `93aab397ab2425d6` CrewAI save-time dedup: encoding pipeline compares new content to existing records; above consolidation_threshold (default 0.85) an LLM chooses keep/update/delete/insert_new; near-duplicates within one batch are dropped by pure vector math; agents never decide this themselves. [fact]
- `4bb028f612142623` Mem0 'Dream' runs in the background, synthesizing recurring patterns, superseding outdated facts, and merging duplicates; synthesis is opt-in, Supersede and Merge are always on. [fact]
- `34638b8e92dfd7dd` CoALA notes modifying and deleting (unlearning) are understudied in recent language agents. [fact]
- `9cc983109ba5b4f3` CoALA never uses the term 'consolidation' — verified in full text; the concept maps to three distinct mechanisms: per-agent reflection written back to semantic memory (Reflexion/Generative Agents), save-time pipeline dedup (CrewAI), and background system-owned consolidation (Mem0 Dream). [fact]
- `b2fee6b8038589fb` Anthropic's multi-agent research system avoids a shared store: subagents have distinct tools/prompts/trajectories, write large outputs to a filesystem and pass only references to the coordinator; the shared context is the coordinator's compressed summary, minimizing the 'game of telephone'. [pattern, fact-sourced]
- `b364a47fb85065c7` Anthropic context-engineering guidance: subagents return only a condensed, distilled summary (often 1,000-2,000 tokens); detailed search context remains isolated within sub-agents. [pattern, fact-sourced]
- `5aa99c3328323465` CrewAI scopes/slices are the only sources where read and write privilege are separated at the API level (read-only slices, private/source-matched visibility). [pattern, fact-sourced]
- `df3c5fffc23e7509` Mem0 documents a leakage-by-default rule: unmentioned entities in a search filter are not constrained, so cross-tagged records still surface if the caller is careless. [fact]
- `ca8c6940f3cb0005` RecMem (arXiv:2605.16045, ACL 2026 Findings) frames the core dichotomy in LLM memory consolidation: existing systems invoke LLMs to process every incoming interaction for memory extraction ('eager consolidation'), which leads to substantial token consumption; RecMem's fix triggers LLM consolidation only when semantically similar interactions recur (thresholds theta_sim=0.6-0.7, theta_count=4-5), buffering everything else in an embedding-only 'subconscious' store.
- `27cb143596b061e2` Measured construction-token costs on LoCoMo (GPT-4.1-mini), per conversation: Mem0 1,520.8K / A-Mem 1,459.9K (eager, per-turn) vs RecMem 193.2K (recurrence-based batch) = -87.3% vs Mem0, -86.8% vs A-Mem (RecMem Table 1).
- `13abdc5cbdc4afeb` On LoCoMo with GPT-4o-mini, construction tokens: Mem0 1,233.5K / A-Mem 1,143.3K vs RecMem 202.4K, i.e. up to ~7.8x fewer tokens for RecMem (RecMem Table 1).
- `bcfce75e6e955850` On LongMemEval-S (500 conversations, ~115K tokens avg), GPT-4.1-mini construction tokens: Mem0 1,626.5K / A-Mem 1,264.3K vs RecMem 365.5K = -77.5% / -71.1% (RecMem Table 2).
- `f80a8893ffad1994` RecMem's query-time token cost stays comparable to eager baselines (e.g. LoCoMo GPT-4o-mini: 2.73K vs Mem0 1.99K), so the saving concentrates in construction; the authors argue (opinion, their claim) that these construction-time differences accumulate over time and can dominate total LLM usage in streaming deployments (RecMem §4.2).
- `f0cad0a342fce769` LightMem (arXiv:2510.18866, ICLR 2026) total (online+offline) token usage on LongMemEval-S, GPT-4o-mini: A-MEM 1,605.8K / MemoryOS 2,991.8K / Mem0 1,152.6K vs LightMem(r=0.7,th=512) 28.25K = 10x-38x fewer total tokens (GPT), 6.9x-21.8x (Qwen) vs baselines (LightMem Table 2).
- `26789dfb6ee09b3f` LightMem's online-only cost (soft updates, no offline pass) is up to 105.9x (GPT) / 117.1x (Qwen) token reduction and 159.4x / 309.9x fewer API calls vs baselines (LightMem Abstract, §5.2).
- `fe6a2d93dd65d35c` Measured cost of an explicit batch consolidation pass: LightMem's offline 'sleep-time' OP-update pass raises construction cost vs online-soft-update alone — e.g. LongMemEval-S GPT-4o-mini LightMem(r=0.7,th=512): online 28.25K tokens / 18.4 calls / 283.8s runtime -> with OP-update 83.44K / 125.5 calls / 496.0s; the batch pass roughly triples construction cost but stays ~14x cheaper than Mem0's eager construction (1,152.6K) (LightMem Table 2).
- `6b7f36fab910c326` Mem0 (arXiv:2504.19413) reports >90% token-cost saving and 91% lower p95 latency vs processing the whole 26,031-token conversation each query (Mem0 retrieves ~1,764 tokens avg) (Mem0 Abstract, §1, Table 2).
- `3de772acdc3f3db4` Mem0 latency on LOCOMO: total p95 = 1.44s (Mem0) / 2.59s (Mem0^g) vs full-context 17.12s; search p95 0.20s vs LangMem 59.82s (Mem0 Table 2, §4.3-4.4).
- `acbe5b346b26c040` Zep/Graphiti (arXiv:2501.13956) on LongMemEval-S (~115K-token conversations): response latency 3.20s (gpt-4o-mini) / 2.58s (gpt-4o) vs full-context 31.3s / 28.9s = ~90% latency reduction while avg context drops 115K -> 1.6K tokens (Zep Table 2, §4.3.2).
- `c5fada8b53377287` LightMem runtime (memory-bank construction) speedups over eager baselines: 2.9x-12.4x (GPT), 1.6x-6.3x (Qwen) on LongMemEval-S (LightMem §5.2).
- `3b19ffa7c3f7be90` Sleep-time Compute (Letta, arXiv:2504.13171) targets latency by doing inference offline before queries, claiming you can 'respond at the accuracy of standard test-time compute but with far lower latencies'; it reduces test-time tokens needed to reach the same accuracy by ~5x (Stateful GSM-Symbolic, Stateful AIME) (Abstract, §5.1).
- `19ef42b700d36e05` RecMem quality: selective batch consolidation matches or beats eager systems on accuracy — best overall among memory-based methods on both benchmarks (LoCoMo GPT-4.1-mini: 81.10 vs Mem0 62.92 / A-Mem 68.83; LongMemEval-S: 76.80 vs 71.20 / 71.60, and beats FullContext 66.20); but on short LoCoMo conversations FullContext still slightly outperforms RecMem (76.43 vs 72.47, gpt-4o-mini), i.e. the consolidation saving is not free on small contexts (RecMem Tables 1-2).
- `d3c25848ca456b8e` LightMem reports higher ACC and lower cost than all memory baselines (LongMemEval-S GPT-4o-mini: 68.64 vs Mem0 53.61 / A-Mem 62.60 / FullText 56.80), i.e. no measured quality-cost tradeoff in their eval; ablation shows removing topic segmentation costs -6.3% ACC (GPT) (LightMem Table 2, §5.4).
- `b5e9e0c951b1e6e6` The MCP 'Skills Over MCP' working group is standardizing skills discovery/consumption; current direction is SEP-2640, a Resources-based Extensions-Track skill primitive, coordinated with the agentskills.io spec.
- `0f170eb63104f9a4` OpenAI Agents SDK hooks: `RunHooks`/`AgentHooks` receive `on_tool_start`/`on_tool_end` (immediately before/after a local tool is invoked) plus `on_agent_start/end`, `on_handoff`, `on_llm_start/end`, attached per-run or per-agent; the `@tool` wrapper adds the runtime pipeline (schema validation, context injection, guardrails, timeouts, failure handling, tracing) around the raw callable, and calling `__wrapped__` bypasses it.
- `8eff920971cc7319` Claude Code hooks fire per session (`SessionStart`/`SessionEnd`), per turn (`UserPromptSubmit`, `Stop`), and on every tool call (`PreToolUse`, `PostToolUse`, `PostToolUseFailure`, plus `PermissionRequest`/`PermissionDenied`); handlers can be shell commands, HTTP endpoints, MCP tools, LLM prompts, or subagents; `matcher` (tool name/regex) + `if` (permission-rule syntax) filter them; MCP tools match as `mcp__<server>__<tool>`; hooks can return `permissionDecision: 'deny'` to block a call; hooks live in settings files, managed policy, plugin `hooks/hooks.json`, or skill/agent frontmatter; `allowManagedHooksOnly` lets enterprises suppress user/project/plugin hooks.
- `579985981421cecf` MCP change notification: servers declaring `listChanged` push `notifications/tools/list_changed` over a `subscriptions/listen` stream when the tool list changes — the protocol-level equivalent of a change hook.
- `2207c9782986060d` LangChain tool execution is wrapped by middleware (e.g. `wrap_tool_call` for retry/error-to-message conversion); `ToolNode` runs tools inside the graph.
- `709bb8e0cf1de244` CrewAI tools emit structured events (`ToolFailureDetectedEvent`, `ToolUsageFinishedEvent`) observed by subscribers via an event bus.
- `a7ce42ab7d93d083` OpenAI Agents SDK guardrails have three layers: input (on first-agent input, parallel or blocking), output (on final output), and tool guardrails (input checks before a function-tool runs — can skip, replace output, or raise a tripwire; output checks after); tripwires raise exceptions and halt the run; ordering vs approvals is configurable via `ToolExecutionConfig(pre_approval_tool_input_guardrails=True)`; tool guardrails apply only to `function_tool` tools, not hosted/built-in tools or handoffs.
- `79491f200bb1b2ce` OpenAI `allowed_callers` lets a tool declare whether it may be invoked directly by the model and/or by programmatic tool-calling (`["direct"]`, `["programmatic"]`, or both) — an access gate on the tool itself; approval/HITL interrupts run through `RunState` pause/resume.
- `e232f1c2ce57f2cf` MCP enforced contract: `inputSchema` MUST be a valid JSON Schema object (2020-12 default); `outputSchema` is optional but binding when present (servers MUST provide conforming structured results, clients SHOULD validate); tool annotations from non-trusted servers MUST be considered untrusted; servers MUST validate all inputs, enforce access controls, rate-limit, and sanitize outputs; clients SHOULD validate tool results before handing them to the LLM; there SHOULD be a human able to deny invocations; SEP-1303 routes input-validation failures into the `isError: true` tool-result channel.
- `fe22d29d30fbcaf7` Anthropic enterprise skills gates: pre-deployment approval gates for five dimensions (triggering accuracy, isolation, coexistence, instruction following, output quality); required eval suites of 3–5 queries per skill (should-trigger/should-not-trigger/ambiguous); separation of duties (authors ≠ reviewers); risk-tier vetting (code execution, instruction manipulation, MCP references, network calls, hardcoded credentials); integrity checksums verified at deploy time and signed commits; recall capped at max 8 skills per request to protect trigger accuracy.
- `a2ba28d2061e0152` Claude Code treats hooks as not the enforcement layer: 'Because the `if` filter is best-effort, use the permission system rather than a hook to enforce a hard allow or deny', and a hook 'can deny the call, but staying silent doesn't approve it.'
- `c25768dab2411013` LangChain schema contract is enforced by construction (type hints required, reserved names rejected); error handling is middleware-based with no built-in guardrail/tripwire layer in the tools docs; gating (HITL, interrupts) is a LangGraph graph-level feature, not a tool-level one.
- `d3efc6d2b15b11ba` CrewAI: a tool returns `ToolFailure(code, retryable)` instead of a string — the framework records it and 'never guesses whether a string looks like an error'; `tool_failure_policy` (`ignore`/`warn` default/`raise`) with most-specific-wins precedence (tool → task → agent → crew); `max_usage_count` limits a tool's calls.
- `96875179b47df8ba` Anthropic is the only framework with explicit 'drift' usage found: its enterprise Monitor stage says 'Rerun evaluations periodically to detect drift or regressions' and treats declining trigger accuracy or output quality as a signal to update or deprecate a skill — a manual/review-driven process, not an automated gate.
- `a5be82962e50e626` LangChain drift/issue detection is externalized to LangSmith Engine, which monitors traces, detects issues, proposes fixes, and can open a PR with the fix.
- `886f02d1ed76d192` MCP's nearest protocol-level drift signal is `notifications/tools/list_changed` — contract-change detection (a host detects its cached contract is stale), not behavioral drift.
- `0b8e26b881b8812b` OpenAI (SDK + platform): no behavioral-drift detection appears in any page reached; the only lifecycle primitive is the SDK's observability hooks, and the platform skills guide advises 'inspect … before using', not periodic re-validation.
- `027989abc31d7253` CrewAI: failure recording + event bus provide raw data for drift analysis but no drift detection policy itself.
- `ecd4a6522505f64f` Synthesis opinion: only Anthropic treats skill/tool quality as a reviewable, versioned artifact with a lifecycle and re-evaluation cadence; MCP is the only framework that turns the tool contract (schema) itself into an on-the-wire, cacheable, change-notifiable object; other frameworks put enforcement in the host application rather than the artifact.
- `f799ae94c5278f06` Anthropic's engineering blog 'Effective context engineering for AI agents' (2025) asserts that giving the model less context is generally better: every unneeded token is both a confusion risk and a budget waste. Core techniques are context narrowing (retrieval pulling only relevant snippets), dynamic context assembly per call, entity memory/scratchpads, and summarization (a separate compression category for high-level understanding only). It warns against dumping full call transcripts into subagent calls. Source: https://www.anthropic.com/engineering/effective-context-engineering-for-agents
- `e368dbd927efdfc0` MemGPT (Packer et al., arXiv:2310.08560, 2023) reports handling contexts exceeding the LLM context window by a factor of 10 while maintaining performance on dialogue and document analysis tasks, via OS-inspired tiered memory (main/external context) with selective retrieval ('paging'). The quantitative claim is the 10x context-window multiplier, not a directly measured token-cost reduction; token saving is the implied mechanism. Source: https://arxiv.org/abs/2310.08560
- `582d97a2bb8bd903` HippoRAG (Gutiérrez et al., NeurIPS 2024, arXiv:2405.14831) reports on long-document QA (MuSiQue, 2WikiMultiHopQA, HotpotQA) that full-context baseline accuracy 'drops' as document length grows while the retrieval-based approach keeps accuracy higher at a fraction of the tokens. The reported numbers are accuracy figures (HippoRAG ~12-17% on MuSiQue vs SOTA ~35%+), not token-savings figures; cost reduction is stated qualitatively as 'more efficient'. Source: https://arxiv.org/abs/2405.14831
- `86494d2770f9e1c6` Generative Agents (Park et al., UIST 2023, arXiv:2304.03442) uses a memory stream with retrieval by recency/importance/relevance, and observations summarized on the fly into higher-level reflections stored back into the stream. No token-savings numbers are reported; the mechanism keeps only salient memories in context. Source: https://arxiv.org/abs/2304.03442
- `7d97975df00109c5` LLMLingua (Jiang et al., Microsoft, EMNLP 2023, arXiv:2310.05736) reports coarse-to-fine prompt compression achieving a ratio up to 20x with minimal performance loss (budgets 1x/3x/5x/10x/20x), with measured savings in compressed token count and FLOPs reduction. It targets whole prompts (demonstrations, instructions, chain-of-thought), not agent memory. Source: https://arxiv.org/abs/2310.05736
- `0a3013221a215aa2` No single primary study directly A/Bs selective retrieval vs summary vs compression vs full-context and reports token savings as the outcome metric for long-running agents was found; each source reports a different metric (accuracy at fixed context, context-window multiplier, compression ratio, per-call context bounds). Settling evidence would be a controlled experiment on a long-running agent benchmark (e.g., SWE-bench agentic runs or long-horizon tool use) measuring billed input tokens and task success across the four arms with identical model and task; to the reviewer's knowledge no such published benchmark exists.
- `31eed941a6bf3c22` OpenTelemetry GenAI semantic conventions moved out of the main opentelemetry/semantic-conventions repo into a dedicated repository open-telemetry/semantic-conventions-genai; the old pages at opentelemetry.io/docs/specs/semconv/gen-ai/ are redirect stubs pointing there.
- `211ef594acd91688` The GenAI semantic convention set is NOT yet stable — every span, attribute, and metric carries Development stability (as of fetch from main branch on 2026-08-06).
- `8402661c95f9d43c` The semantic-conventions-genai repo covers GenAI (LLM, agent, embeddings, retrieval) and MCP operations; shared attributes not specific to GenAI (e.g. server.address, error.type, db.*) continue to live in the upstream semantic-conventions repo, pinned at a version (docs reference v1.44.0 / spec v1.56.0).
- `00b76328e304b094` The semantic-conventions-genai repo's Schema URL field is still marked TODO.
- `e7f5d7ddbd0506aa` GenAI spans represent logical operations as observed by the caller, covering the operation duration including automatic retries.
- `69e042353c67161a` The inference span (gen_ai.inference.client) SHOULD be span kind CLIENT (or INTERNAL for same-process models); span name SHOULD be {gen_ai.operation.name} {gen_ai.request.model}.
- `122ebf323a7b873b` Required attributes on the inference span are gen_ai.operation.name and gen_ai.provider.name.
- `4c19d9b8fed90b2c` Conditionally required attributes on the inference span: error.type (if error), gen_ai.conversation.id (when available), gen_ai.output.type (when output format requested), gen_ai.prompt.name/gen_ai.prompt.version (when named template used), gen_ai.request.choice.count, gen_ai.request.model (if available), gen_ai.request.seed, gen_ai.request.stream, gen_ai.request.top_k, server.port (if server.address set).
- `31afd4aa76fb215a` Recommended attributes on the inference span include gen_ai.conversation.compacted, gen_ai.request.frequency_penalty, gen_ai.request.max_tokens, gen_ai.request.presence_penalty, gen_ai.request.previous_response.id, gen_ai.request.reasoning.level, gen_ai.request.stop_sequences, gen_ai.request.temperature, gen_ai.request.top_p, gen_ai.response.finish_reasons, gen_ai.response.id, gen_ai.response.model, gen_ai.response.time_to_first_chunk (streaming), token-usage attributes gen_ai.usage.input_tokens/output_tokens/reasoning.output_tokens/cache_creation.input_tokens/cache_read.input_tokens, plus server.address.
- `5dca42b84576ac81` Opt-in content attributes on GenAI spans (PII warning): gen_ai.input.messages, gen_ai.output.messages, gen_ai.prompt.variable.<key>, gen_ai.system_instructions, gen_ai.tool.definitions; these MUST follow JSON schemas in model/gen-ai/.
- `2e18ebad8d315465` Sampling attributes that SHOULD be set at span creation time: gen_ai.operation.name, gen_ai.provider.name, gen_ai.request.model, server.address, server.port.
- `3bdf61d670b97831` gen_ai.operation.name well-known values (snapshot of main on fetch date; the enumeration will grow over time): chat, create_agent, create_memory, create_memory_store, delete_memory, delete_memory_store, embeddings, execute_tool, fetch_response, generate_content, invoke_agent, invoke_workflow, plan, retrieval, search_memory, text_completion, update_memory, upsert_memory.
- `2eea1cc6e5d5294f` gen_ai.provider.name well-known values (snapshot of main on fetch date; the enumeration will grow over time): anthropic, aws.bedrock, azure.ai.inference, azure.ai.openai, cohere, deepseek, gcp.gemini, gcp.gen_ai, gcp.vertex_ai, groq, ibm.watsonx.ai, mistral_ai, moonshot_ai, openai, perplexity, x_ai.
- `96dfb8000058662e` gen_ai.output.type well-known values: text, json, image, speech.
- `7baa4e0fc447c393` Embeddings span (gen_ai.embeddings.client): operation name = embeddings; required gen_ai.operation.name and gen_ai.provider.name; recommended gen_ai.embeddings.dimension.count, gen_ai.request.encoding_formats, gen_ai.response.model, gen_ai.usage.input_tokens.
- `0a74dadc333efd53` gen_ai.server.request.duration metric: unit s; required gen_ai.operation.name and gen_ai.provider.name; conditional error.type, gen_ai.request.model, server.port; recommended gen_ai.response.model and server.address.
- `a374d61735af78e8` gen_ai.server.time_to_first_token metric: unit s; required gen_ai.operation.name and gen_ai.provider.name; conditional gen_ai.request.model; recommended gen_ai.response.model and server.address; no error.type attribute (successful responses only).
- `0f71428eedb1157f` gen_ai.server.time_per_output_token metric: unit s; time per output token after the first; same attribute set as time_to_first_token.
- `e113f24a5245b284` gen_ai.invoke_workflow.duration metric: unit s; conditional error.type and gen_ai.workflow.name.
- `55a5de088bb8a4cb` gen_ai.invoke_agent.duration metric: unit s; conditional error.type and gen_ai.agent.name; recommended gen_ai.request.model.
- `f640281634a3ed98` gen_ai.invoke_agent.inference_calls metric: unit {inference_call}; recommended gen_ai.agent.name.
- `34b2aac2c52f66d1` gen_ai.invoke_agent.tool_calls metric: unit {tool_call}; recommended gen_ai.agent.name.
- `176d8ed99f2c76c6` gen_ai.execute_tool.duration metric: unit s; required gen_ai.tool.name; conditional error.type and gen_ai.agent.name; recommended gen_ai.tool.type.
- `9fd0d62f3cea36ac` DMR (Deep Memory Retrieval) is a task introduced in the MemGPT paper, built on the Multi-Session Chat (MSC) dataset; the agent is asked a question referring back to a prior conversation with a very narrow expected answer range, with QA pairs generated by a separate LLM instructed to write questions answerable only from the old chat log and not contained in persona information.
- `8de41cd967fdda17` DMR/MSC setup: MSC conversations have five sessions of ~12 messages, plus a new session 6 containing one QA pair; Zep reports DMR as a 500-conversation subset of MSC. Metrics are ROUGE-L plus an LLM judge scoring CORRECT/WRONG. Reported accuracies: GPT-3.5 Turbo 38.7% -> MemGPT 66.9%; GPT-4 32.1% -> 92.5%; GPT-4 Turbo 35.3% -> 93.4%.
- `672b4912c42144b2` DMR measures single-session-hop fact recall (opinion grounded in source): whether an agent can pull one specific fact out of a prior session; it tests retrieval/consistency, not updating, temporal reasoning, or abstention. Zep's primary text criticizes it: 'The evaluation relies exclusively on single-turn, fact-retrieval questions... Each conversation contains only 60 messages, easily fitting within current LLM context windows.'
- `94dcf0d91a61e711` LoCoMo is a dataset of very long-term conversations, each ~300 turns and ~9K tokens on avg. over up to 35 sessions, 50 dialogues, generated via a machine-human pipeline grounded in personas and temporal event graphs, including multimodal (image-sharing) turns.
- `1052dd5a927ed18a` LoCoMo has three tasks: (1) QA in five categories — single-hop, multi-hop, temporal reasoning, open-domain (commonsense/world) knowledge, and adversarial (designed to trick the agent into providing wrong answers, with expectation the agent identifies them as unanswerable); (2) event-graph summarization scored with FactScore against the ground-truth temporal event graph; (3) multi-modal dialogue generation scored with MMRelevance.
- `a6e2c69032154967` LoCoMo findings: long-context LLMs/RAG improve QA 22–66% but significantly lag human levels (by 56%), especially in temporal reasoning (by 73%); long-context models are especially prone to misassigning dialogs or events to the wrong speaker; gpt-3.5-turbo-16k scored 2.1% on adversarial questions. RoMem later classifies LoCoMo as the 'hybrid' benchmark: a mixture of dynamic temporal updates and general knowledge queries.
- `9cd6a6c67868558e` LongMemEval: 500 manually curated questions over task-oriented user-assistant histories evaluating five abilities — information extraction (IE), multi-session reasoning (MR), temporal reasoning (TR), knowledge updates (KU), and abstention (ABS); seven question types; two standard settings: LongMemEval_S (~115k tokens/question) and LongMemEval_M (500 sessions, ~1.5M tokens).
- `045bcab59e18db4a` LongMemEval KU definition: 'Ability to recognize the changes in the user's personal information and update the knowledge of the user dynamically over time.' KU error analysis states the retriever was only able to identify the updated knowledge but failed to retrieve the previous information before update, counted as retrieval failure.
- `0e8c9801a605c550` LongMemEval findings: long-context LLMs drop 30–60% vs. oracle retrieval on LongMemEval_S; commercial ChatGPT and Coze dropped 37% and 64% vs. offline reading; ChatGPT tended to overwrite crucial information as chat continues, while Coze often failed to record indirectly provided user information.
- `348e2784f9115e96` On knowledge-update and temporal reasoning: LongMemEval authors report 'Naive time-agnostic memory designs perform poorly on temporal reasoning questions'; time-aware indexing/query expansion improves temporal-reasoning recall by 6.8–11.3%; KU questions require retrieving BOTH pre-update and post-update evidence, with a store that drops the older fact counted as failed retrieval.
- `e286837b45d310e8` Static-dynamic dilemma (RoMem primary text): 'most existing approaches model time as discrete metadata, either sorting by recency (burying old-yet-permanent knowledge), simply overwriting outdated facts, or requiring an expensive LLM call at every ingestion step, leaving them unable to distinguish persistent facts from evolving ones.' Failure directions: recency sorting silently buries static knowledge (decades-old fact ranked below fresher irrelevant entries); disabling recency bias leaves temporal conflicts unresolved, confusing the downstream LLM. RoMem asserts its append-only store works only because contradictions are resolved geometrically at query time, and claims all existing baselines (Mem0, Zep, LicoMemory, HippoRAG) treat memory as a static snapshot, causing contradictory facts to cluster together in retrieval space and confuse the LLM.
- `9fd23bfe4eb2af7f` RoMem cross-benchmark numbers (single-paper, author-reported): LoCoMo Recall@10 (GPT-5-mini impl.) — Mem0 temporal 0.690, HippoRAG 0.645, Zep 0.553, A-Mem 0.691, RoMem 0.726; DMR-MSC MRR — Mem0 0.847, HippoRAG 0.848, RoMem 0.856, with naive-recency handling claimed to be precisely the behaviour naive recency-based approaches would destroy for static fact retrieval.
- `f350dbda4620ffdd` Detail-level extraction: fact-extraction compression trades recall of detail — 'While further compression into individual user facts harms overall performance due to information loss, it improves the multi-session reasoning accuracy.' LoCoMo likewise: session-summary RAG does not improve QA, likely due to loss of information during conversion of dialogs to summaries.
- `dd8a402cb34e9585` Adversarial/abstention failures: facts never stored can't be abstained on — Coze often failed to record indirectly provided user information (implicitly-stated facts never enter the store), and compressed/overwriting stores corrupt prior facts over time (ChatGPT); LoCoMo's adversarial category (2.1% for a long-context model) is the same failure surface.
- `d32f5cbbb9dfef2a` DMR is the task where a static fact store succeeds: it has no update, temporal, or abstention categories, and Zep shows a plain full-conversation baseline already matches MemGPT (94.4% vs 93.4%, both gpt-4-turbo; Zep 94.8%). Hence high DMR scores co-exist with failures on LongMemEval/LoCoMo.
- `3b3bae3fd783ee40` Uncertain/estimated: RoMem's cross-benchmark numbers use a different metric than the original LoCoMo paper (Recall@10 over ~1,986 QA pairs vs. the original paper's F1 answer prediction over 50 dialogues); LoCoMo question count differs across sources (1,813 in one 2026 paper vs. 1,986 in RoMem). Zep's LongMemEval improvements (up to +18.5% accuracy, −90% latency) are vendor self-reported. No primary source reached gives a controlled head-to-head of a 'pure' append-only store vs. an updating store on all three benchmarks with an identical LLM judge.
- `2bc3ff3548b14a6f` Verdict synthesis: DMR (MemGPT, 2023) measures single-session-hop fact recall on short 5-session MSC chats and is near-saturated (full-context baseline matches the memory system); LoCoMo (2024) measures long-horizon QA across single/multi-hop, temporal, open-domain, and adversarial questions plus event-summarization, with temporal reasoning and abstention the hardest axes (human 87.9 F1 vs. best model ~37.8; long-context adversarial 2.1%); LongMemEval (ICLR 2025) explicitly measures IE, MR, TR, KU, ABS at 115k–1.5M-token scales, documenting 30–60% drops for long-context LLMs and 37–64% drops for commercial memory assistants.
- `d71aa36a9e81bc8c` Seccomp is a syscall filter: a BPF program evaluated on struct seccomp_data (syscall number, arch, instruction_pointer, up to 6 raw register arguments) deciding KILL/TRAP/ERRNO/USER_NOTIF/TRACE/LOG/ALLOW. BPF cannot dereference pointers, which kernel docs credit with making the filter itself immune to the TOCTOU problems of syscall interposition frameworks (source: seccomp_filter.rst, seccomp(2)).
- `c7f2552aa3ba9e0c` Landlock is a stackable LSM restricting operations on objects (file hierarchies via filesystem access rights; TCP/UDP ports via network access rights since ABI v4/v10; IPC via scope flags since ABI v6), enforced on a thread and its descendants (source: landlock.rst, landlock(7)).
- `980038e5090eec8b` Kernel docs state: 'System call filtering isn't a sandbox... It is meant to be a tool for sandbox developers to use. Beyond that, policy for logical behavior and information flow should be managed with... potentially, an LSM of your choosing.' Direct corollary: seccomp alone is insufficient as a worker sandbox; an LSM layer (Landlock is one) is the documented complement (opinion derived directly from that statement).
- `a68feba40bd78382` Seccomp reduces the kernel syscall surface but cannot express path/object policy (no pointer deref); Landlock expresses path-based filesystem and port-based network policy but does not filter syscalls at all.
- `3f11178717f2bfc4` Capability gating: installing a seccomp filter requires prctl(PR_SET_NO_NEW_PRIVS, 1) or CAP_SYS_ADMIN; Landlock is documented as usable by unprivileged processes ('Landlock empowers any process, including unprivileged ones, to securely restrict themselves').
- `9e867896987f3592` Both seccomp and Landlock are one-way/irreversible: seccomp filters survive execve and can only be added to; Landlock: 'Once a thread is landlocked, there is no way to remove its security policy; only adding more restrictions is allowed.'
- `554ea35e5b4867b0` Landlock is kernel 5.13+ and opt-in at boot (lsm=...landlock...); its API is ABI-versioned (v1..v8+ as of 7.0), and features silently shrink per kernel unless the sandbox layer degrades explicitly. Seccomp BPF is CONFIG-gated (CONFIG_SECCOMP_FILTER) and has per-arch and kernel-version surface (e.g. the x32 nr fix in 5.4).
- `7c6f0eb74c7ce6fc` This repo's ADR-0012 chose Landlock write-containment for the worker (read+execute on the tree, write confined to workdir/state) over a full namespace sandbox, citing 'no root, no user namespaces, no external binary' and the binding risk being filesystem writes rather than network egress. The threat-model ranking ('writes are the real risk') is repo opinion.
- `1711f8ebaaa6ff1c` Landlock unmediatable operations: 'It is currently not possible to restrict some file-related actions accessible through these syscall families: chdir(2), stat(2), flock(2), chmod(2), chown(2), setxattr(2), utime(2), fcntl(2), access(2).' A worker can still stat/read metadata, chdir, lock, and chmod what it can reach.
- `d6f4e1893008deef` Landlock pre-existing FD escape: 'Files or directories opened before the sandboxing are not subject to these restrictions,' and such FDs keep their Landlock properties when passed between processes 'even when some of the involved processes do not have an enforced Landlock ruleset.' A worker that inherits pre-opened writable FDs from its parent retains write access.
- `9663caa7535ed43a` Landlock OverlayFS divergence: a policy restricting an OverlayFS layer 'will not restrict the resulted merged hierarchy, and vice versa' — an OverlayFS upper/lower/merge structure is outside the restricted hierarchy from Landlock's point of view.
- `dd98058876169ac0` Landlock Erratum 3 (documented privilege-escalation-class bug): 'it was possible to widen access rights through rename or link actions involving disconnected directories, potentially bypassing LANDLOCK_ACCESS_FS_REFER restrictions... could allow privilege escalation in complex mount scenarios.' Fixed only on kernels reporting the errata bit.
- `356b94ef83a50a9a` Landlock network support is late and partial: no network restriction before ABI v4 (kernel 6.7, TCP bind/connect only); UDP added in ABI v10. On older/backported kernels a best-effort policy silently drops network control. IPv4/IPv6 stream sockets that are not TCP (SMC/MPTCP/SCTP) were mis-gated by TCP rights before Erratum 1.
- `8926e976facd2be8` Landlock thread semantics: landlock_restrict_self restricts the calling thread and descendants; below ABI v8 (and at v8 without LANDLOCK_RESTRICT_SELF_TSYNC) a policy 'would only be enforced for the calling thread and its children (and not for all threads, including parents and siblings).' A multithreaded worker that restricts one thread can leave sibling threads unconfined.
- `7390cd3a09477737` Landlock truncate/ioctl subtleties: truncate is decoupled from WRITE_FILE (e.g. open(O_RDONLY|O_TRUNC) truncates without the write right); creat() needs the truncate right; fallocate(FALLOC_FL_COLLAPSE_RANGE) can shorten a writable file 'sidestepping the LANDLOCK_ACCESS_FS_TRUNCATE right'; several ioctls (FIOCLEX, FIONBIO, FICLONE, FIDEDUPERANGE...) are exempt from IOCTL_DEV.
- `b773707e3c794a0a` Landlock scope limits: LANDLOCK_SCOPE_* does not cover everything — a sendto() on an already-connected socket is not restricted, and IPC scoping 'does not support exceptions.'
- `ef358e65af2c75f9` Landlock does not and cannot filter syscalls or deny syscall-level primitives (e.g. ptrace of a peer is mediated via domain-subset rules, but no generic syscall kill).
- `d1df44ba19d24fa5` Seccomp: BPF 'may not dereference pointers,' so pathnames, buffers, and most argument content are invisible; only register values are visible.
- `d83a963660e3cc87` Seccomp deny-list bypass: an allow-list is strongly recommended 'because it is often possible to alter the representation of a value without altering its meaning, leading to a deny-list bypass.'
- `a0ae947f3627d7ca` Seccomp architecture/ABI confusion vectors: always check arch; on x86-64, the x32 ABI shares AUDIT_ARCH_X86_64 and a policy that does not account for __X32_SYSCALL_BIT 'can be bypassed by a malicious program that sets __X32_SYSCALL_BIT'; kernels before 5.4 permitted nr 512-547 plus x32-ORed forms that could invoke ptrace with confused x32-vs-x86_64 semantics.
- `7b032b76d2592172` Seccomp ptrace escape: 'seccomp-based sandboxes MUST NOT allow use of ptrace, even of other sandboxed processes, without extreme care; ptracers can use this mechanism to escape' (SECCOMP_RET_TRACE lets a tracer rewrite the syscall, and before 4.8 the check was not re-run after tracer notification).
- `59e0420458d34317` Seccomp no_new_privs requirement documents the setuid attack: the filter may be malicious against a setuid program — e.g. making setuid() appear to succeed without doing it, tricking a privileged program into keeping superuser privileges — which is why PR_SET_NO_NEW_PRIVS (or CAP_SYS_ADMIN) is mandatory.
- `76649269e1ba035d` Seccomp userspace notification (SECCOMP_RET_USER_NOTIF) reintroduces TOCTOU and is explicitly not a security mechanism: 'the seccomp user-space notification mechanism can not be used to implement a security policy'; SECCOMP_USER_NOTIF_FLAG_CONTINUE has a time-of-check/time-of-use race because the target can rewrite syscall arguments while blocked; a notifier 'can be bypassed if the existing filters allow the use of seccomp(2) or prctl(2) to install a filter' with higher-precedence action.
- `b917c9b0c660aa39` Seccomp PID-reuse race in the supervisor path: the pid in seccomp_notif can be recycled after the target dies; the documented mitigation is SECCOMP_IOCTL_NOTIF_ID_VALID between reading /proc/tid/mem and using the bytes, and 'a write by the supervisor into the target's memory can never be considered safe.'
- `6a6de58f8e958687` Seccomp vDSO/vsyscall bypass and quirks: syscalls served in userspace (e.g. clock_gettime, gettimeofday, time) never reach seccomp; vsyscall emulation honors seccomp but with documented TRAP/TRACE oddities.
- `7323ccdc175dbfdf` Seccomp argument truncation/visibility mismatches: seccomp sees the full 64-bit registers while the kernel may only consume the low 32 bits (i386-on-x86-64), so filters can misjudge actual semantics.
- `f63bce506848d83d` Seccomp unknown actions default to kill (kernel treats unknown action as SECCOMP_RET_KILL_PROCESS), so forward-compatibility mistakes tend to fail-safe; filter size caps: BPF_MAXINSNS 4096 per program, MAX_INSNS_PER_PATH 32768 total.
- `9a5b5080bf5a63c4` Landlock is an LSM layer stacked over DAC/other LSMs; seccomp constrains the syscall surface; kernel docs place both as composable hardening layers, and inheritance across clone/execve is analogous for both. Nothing in the primary sources documents a way to combine them into one primitive; each keeps its separate gaps.
- `2cd8c890524c508b` Unverified: whether a given distribution kernel exposes Landlock at boot (LSM ordering) is host-specific, not answered by the documented sources. Kernel-doc 'future Landlock evolutions' claims (e.g. restricting chdir/stat/...) are direction statements, not commitments.
- `eb55e129103e8a6f` Estimate: snapshot frequency bounds recovery time (replay length) and storage cost (archived WAL/old snapshots); no source gives universal numbers — the etcd example snapshot of 2.1 MB for 7 keys/rev 10 is illustrative, not a rule; choose checkpoint interval per write volume and RTO/RPO.
- `ecca3611145516e6` Fowler's Event Sourcing: all changes to application state are stored as a sequence of events, enabling complete rebuild by re-running events on an empty application, plus temporal query and event replay; explicitly paired with snapshots (replay from an overnight snapshot on crash).
- `1c641abef370327a` Event sourcing replay re-runs side effects, so external systems must be wrapped with a Gateway that can detect replay mode and suppress outbound calls — the same problem durable-execution platforms solve with idempotency.
- `a40757229890299e` Temporal's durable execution is event sourcing in disguise: the workflow sends Commands mapped to Events persisted to a complete durable log; on worker crash, the worker uses the Event History to replay the code and recreate the workflow state as it was immediately before the crash.
- `a66abf4c0c1f97cf` Temporal Durable Execution: a workflow maintains its state and progress across failures, crashes, or server outages because the Event History records state at each step and the execution resumes from the last recorded event.
- `dbc7cbf9a748d51b` LangGraph uses a checkpointer that saves a snapshot of graph state at each super-step, organized into threads; if nodes fail at a super-step, the graph can be restarted from the last successful step.
- `b556d77f45006ee2` LangGraph durability is tunable: "exit" persists only when execution exits (mid-run crash loses state), "async" persists while the next step executes (small risk of no checkpoint if the process crashes), "sync" persists synchronously before the next step starts — an explicit durability-vs-throughput trade mirroring WAL synchronous settings.
- `7fcceed280fcc7b0` LangGraph separates full snapshots from finer-grained writes: per-node pending writes are written durably within a super-step so successful nodes are not re-run on resume, while time-travel/replay resumes only at full super-step checkpoints — snapshot-restore plus per-task delta.
- `a495d4c500ec01b5` LangGraph documents an unbounded-growth failure mode: over long conversations checkpoints accumulate, increasing latency and storage costs; the fix is pruning or a retention policy (equivalent to compaction), and DeltaChannel stores deltas instead of full values to bound checkpoint size.
- `8b0d2fb3cb59a7d9` AWS Step Functions: Standard workflows are durable and exactly-once with execution state persisting between transitions and full history retrievable; Express workflows are at-least-once (async) or at-most-once (sync) with no state persistence between transitions; retrying on at-least-once requires idempotent actions.
- `6665ad2250b0bf6a` The three approaches (WAL+checkpoint, snapshot-restore, event sourcing) are one family, not alternatives: every credible system (PostgreSQL, SQLite, WiredTiger/MongoDB, RocksDB, etcd/Raft, Temporal, LangGraph, AWS Step Functions) combines an append-only, checksummed, order-preserving log with periodic state snapshots that bound replay.
- `8baa3b2f82d7faa6` Opinion (sourced inference, not a measured study): the selector for agents is which failure you must survive and at what cost — WAL+checkpoint gives strongest crash consistency and minimal replay but needs a storage-engine contract and is overkill for small agent state; snapshot-restore is simplest with a bounded recovery window but loses everything since the last snapshot unless combined with a log (RPO = snapshot interval); event sourcing enables exact resume, audit, time travel, and human-in-the-loop review at the cost of side-effect duplication, unbounded history requiring compaction/pruning, and schema/version evolution of events.
- `649f2dd5755ff2c1` Uncertain, not established from these sources: no primary source quantifies end-to-end failure-recovery success rates, RTO/RPO for arbitrary agent workloads, or a cost model for choosing among the three approaches; the WAL-reset bug shows latent-corruption risk exists even in the most battle-tested implementation, and nothing in these docs proves any engine's recovery is correct under every crash point.
- `467e731dc67aed05` A claim that a failure was recovered is only as good as its verifier: this repo's AGENTS.md requires run verify to actually execute the declared verify_command before an outcome is trusted — no external source substitutes for executing a recovery path on your own system.
- `af81d4d2d8f1a247` What would settle the open questions: (a) a crash-injection fault-injection harness run against each candidate with asserted post-crash invariants; (b) measured RPO per configuration; (c) replay-cost curves for agent-sized state; (d) a verifier that executes a restored snapshot plus replay and asserts the agent's state equals a reference trajectory.
- `1eb6cac2ee3c0b55` FrugalGPT defines three cost-reduction strategies: prompt adaptation, LLM approximation, and LLM cascade (query a list of LLMs sequentially; a scoring function g(q, answer) in [0,1] and a learned per-model threshold decide when to stop and return). The cascade is a length-3 list scored via a DistilBERT regressor. (FrugalGPT, arXiv 2305.05176, §3-4)
- `53b44257c13294e1` Hybrid LLM routes each query to exactly one of two models (large vs small), never calling multiple LLMs per query. The router is a BERT-style encoder (DeBERTa-v3-large) trained to predict the quality gap H(x)=q(S(x))−q(L(x)) where q is BART score; a test-time threshold on the router score trades quality for cost. (Hybrid LLM, arXiv 2404.14618v1, §2.2, §3.1-3.3)
- `9cc229d3928af9fd` RouteLLM trains a binary 'win prediction' router P_θ(wins|q) from Chatbot Arena human preference data and routes to the strong model iff P(wins|q) >= cost threshold α. Four parameterizations: similarity-weighted (SW) Bradley–Terry ranking, matrix factorization, BERT classifier, causal LLM classifier (Llama 3 8B). (RouteLLM, arXiv 2406.18665v4, §3.1, §4.2)
- `c576cfdd5214f4fd` 'Task-classification' routers categorize queries into pre-defined categories and pick the best-scoring model — e.g., vLLM Semantic Router uses a ModernBERT categorizer. (RouterArena, arXiv 2510.00202v3, §6.1)
- `300485f9871ece7c` FrugalGPT cascade (3 LLMs, learned confidence thresholds) achieves cost savings to match the best individual LLM's accuracy: 98.3% on HEADLINES (GPT-4 best), 73.3% on OVERRULING (GPT-4), 59.2% on COQA (GPT-3 best). (FrugalGPT, arXiv 2305.05176, Table 3)
- `faa50827c8944f4b` FrugalGPT at the same cost improves accuracy by up to 5% (Fig 5 caption); the abstract states 4%. (FrugalGPT, arXiv 2305.05176, §4, Fig 5, Abstract)
- `9846b295906aa283` On OVERRULING, FrugalGPT simultaneously gains 1% accuracy and cuts cost 73% vs GPT-4. (FrugalGPT, arXiv 2305.05176, §4)
- `f1536221b75a8013` FrugalGPT example learned thresholds (HEADLINES, budget $6.5 = 1/5 of GPT-4 cost): accept GPT-J's answer if score > 0.96; else query J1-L, accept if score > 0.37; else GPT-4. Result: 80% cost cut and 1.5% accuracy gain over GPT-4. (FrugalGPT, arXiv 2305.05176, §4)
- `ef0073bcd67cc7dd` Cheap models are complementary: GPT-4 is wrong while GPT-J/J1-L/GPT-Curie are right on ~6% of HEADLINES queries; GPT-4 is wrong but GPT-3 right on 13% of COQA (upper bounds, not always achieved). (FrugalGPT, arXiv 2305.05176, §4, Fig 4)
- `8d7738014c5ebe99` API price spread justifies routing: costs across 12 APIs from 5 providers differ by ~2 orders of magnitude (GPT-4 $30 vs GPT-J $0.20 per 10M input tokens, March 2023 pricing). (FrugalGPT, arXiv 2305.05176, Table 1)
- `cef70c90b6220520` Hybrid LLM headline: up to 40% fewer calls to the large model with no drop in response quality (abstract); 22% fewer GPT-3.5-turbo calls at <1% BART-score drop (intro, Fig 1c). (Hybrid LLM, arXiv 2404.14618v1)
- `6977145a51d83491` Hybrid LLM routing value depends sharply on the model-pair quality gap: at a 20% cost advantage the small-gap pair (Llama-2 7b/13b) loses <=0.1% BART quality, the large-gap pair (FLAN-t5 800m/Llama-2 13b) loses up to 5.8%. When the gap is large, training on Pr[H>=0] labels is near-useless ('0 for almost 90% of queries'); a label-relaxation transformation (Pr[H>=−t]) recovers a usable signal. (Hybrid LLM, arXiv 2404.14618v1, Table 1, §3.3, Fig 4a)
- `e151279bfa369f9a` Hybrid LLM with thresholds tuned to keep BART drop <1%, achievable cost advantage is pair-dependent: Llama-2 7b/13b → >96% cost advantage; Llama-2 13b/GPT-3.5-turbo → only ~15%; FLAN-t5 800m/Llama-2 13b → ~5%. (Hybrid LLM, arXiv 2404.14618v1, §4.5, Table 3)
- `135c69977d75322e` Hybrid LLM router latency overhead is negligible relative to generation: DeBERTa router 0.036±0.002 s/query vs Llama-2 13b generation 14.61±0.27 s (router ~10× faster than the fastest LLM, FLAN-t5 800m at 0.46 s). (Hybrid LLM, arXiv 2404.14618v1, §4.4, Table 2)
- `feeed923077d1e1f` Hybrid LLM router quality transfer to new model pairs degrades with quality-gap correlation: at high correlation (r=0.76) 20% cost advantage costs <=1.6% quality, 40% costs <=4.1%; at low correlation (r=0.06) it fails. (Hybrid LLM, arXiv 2404.14618v1, §4.7, Fig 8)
- `74a333f06b0e1b1b` Recurring across independent LLM-routing papers: (1) the achievable tradeoff is dominated by the size of the weak/strong quality gap and the training-data distribution match (augmentation and low-data regimes matter more than router architecture); (2) routers over-deploy the strong model in practice (RouterArena normalized deferral curve), so the frontier is far from the oracle; (3) router overhead itself is measurable and non-trivial (latency, embedding API calls, per-request cost) but small vs generation cost.
- `e9faf4c799927644` Cross-paper comparison of LLM routers is apples-to-oranges: different model pools, different quality metrics (BART score vs accuracy vs LLM-judge), different cost assumptions (e.g., RouteLLM's GPT-4/Mixtral ~100× ratio), and 2023–2025 price baselines. Confidence-threshold cascade papers (FrugalGPT, 2502.11021) report large savings but on few datasets and with authors' own thresholds.
- `8479b0749854fe46` MAST (Cemri et al., UC Berkeley; arXiv 2503.13657) defines 14 LLM-agent failure modes in 3 categories: FC1 System Design Issues (disobey task spec 11.8%, disobey role spec 1.5%, step repetition 15.7%, loss of conversation history 2.8%, unaware of stopping conditions 12.4%), FC2 Inter-Agent Misalignment (conversation reset 2.2%, fail to ask for clarification 6.8%, task derailment 7.4%, information withholding 0.85%, ignored other agent's input 1.9%, reasoning-action mismatch 13.2%), FC3 Task Verification (premature termination 6.2%, no/incomplete verification 8.2%, incorrect verification 9.1%).
- `973e990075c8a7fd` MAST (arXiv 2503.13657) was derived by Grounded Theory analysis of 150 traces across 5 frameworks, refined via 3 inter-annotator rounds (kappa=0.88), then LLM-as-judge annotation of 1,642 traces (kappa=0.77 vs. humans).
- `aa17692c22a78738` Interaction-Centric Taxonomy (Raj, Gupta, Mahmoud et al., Scale AI; arXiv 2607.28802, July 2026) defines 41 LLM-agent failure modes, each assigned to an interaction edge between two components and a fault side (model vs. other); components are Model, Owner, Grader, Third party, Context, Memory, Tool, Local env., External env. grouped into three families (User, Harness, Environment); 36 modes are model-side, 5 are component-side.
- `f7a62764ca74d0e9` Interaction-Centric Taxonomy (arXiv 2607.28802) was grounded in 40 worked examples (E1-E40) drawn from public benchmarks, model system cards, published reports, and logged agent trajectories; labels assigned by tracing the earliest unrecoverable failure ('root-cause principle'); strongest independent reasoning judge reaches Cohen's kappa=0.76 vs. human category labels, highest pairwise judge agreement kappa=0.84.
- `38956b8103d9e760` Interaction-Centric Taxonomy (arXiv 2607.28802) names modes including Over-initiative, Under-initiative, Satisficing, Instruction-Following Failure, Reasoning Failure, Unauthorized Irreversible Action, Sycophancy, Domain Knowledge Deficit, Value Misalignment, Instruction-Grader Mismatch, Specification Gaming, Evaluation Awareness, Indirect Prompt Injection, Contextual Sycophancy, State Tracking Failure, Goal Drift, Context Rationale Erosion, Missed Write, State Staleness, Overgeneralization, Memory Rationale Erosion, Pollution, Redundancy, Missed Read, Memory Following Failure, Incorrect Tool Selection, Tool Hallucination, Tool Feedback Neglect, Tool Recovery Failure, Malformed Arguments, Suboptimal Arguments, Mistranslation, Delegation Failure, Communication Failure, Recovery Failure, Service Failure, Stale State Delivery, Observation Failure.
- `f4b23532d8f51878` Longitudinal silent-failure taxonomy (Wu; arXiv 2606.14589, 'When Errors Become Narratives') defines five mechanism-oriented classes of agent silent failures: (A) environment and platform quirks, (B) design-assumption mismatches, (C) error swallowing and dilution, (D) chained hallucination and fabrication (described as the unique-to-LLM 'fail-plausible' pattern), (E) operational omission and forensic blind spots.
- `984129939642011a` Longitudinal silent-failure study (Wu; arXiv 2606.14589) covered an 8-week study of one production personal-assistant runtime (~40 scheduled jobs, 8 LLM providers) with 22 documented root-caused incidents; reported ~70% of silent failures caught by human user-view observation, 0% ex-ante prevention vs. 87% regression-blocking in retrospective audit, incident latency 13 hours to 60 days.
- `02a02d256e8ffada` Aegis taxonomy (Song et al., U. Toronto/Vector; arXiv 2508.19504) defines 6 agent-environment failure modes in 3 categories: Exploration Failures (State-space Navigation Failure, State Awareness Failure), Exploitation Failures (Tool Output Processing Failure split into comparison/calculation/retrieval/sorting, Domain Rule Violation distinguishing invalid action vs. lack of correct action, User Instruction Following Failure), plus Resource Exhaustion (turn/token limit).
- `d0a70761a296b570` Aegis (arXiv 2508.19504) was measured on 142 failed traces / 3,656 turns across 5 benchmarks (TauBench airline & retail, BFCL file system, CRM-Arena, MedAgentBench) and 3 models (GPT-4.1, GPT-4.1 mini, o3); failures localized to the first unsuccessful subtask using an HTN-inspired subtask abstraction; resource exhaustion accounted for 45%/83% of retail/CRM failures under GPT-4.1.
- `d897024c3f240a4b` DEFT deep-research failure taxonomy (Zhang et al., OPPO AI Agent Team; arXiv 2512.01948) defines 14 failure modes under 3 core categories: Reasoning (Failure to Understand Requirements 10.55%, Lack of Analytical Depth 11.09%, Limited Analytical Scope 0.90%, Rigid Planning Strategy 5.60%), Retrieval (Insufficient External Information Acquisition 16.30%, Information Handling Deficiency 2.26%, Information Integration Failure 2.91%, Information Representation Misalignment 2.91%, Verification Mechanism Failure 8.72%), Generation (Redundant Content Piling 2.51%, Structural Organization Dysfunction 2.26%, Content Specification Deviation 10.73%, Deficient Analytical Rigor 4.31%, Strategic Content Fabrication 18.95%).
- `57c6cc3485ade4ed` DEFT (arXiv 2512.01948) was derived by grounded theory on ~1,000 reports from 10+ deep research agents: open coding by 5 LLM coders (Claude Opus 4.1, Gemini 2.5 Pro, Grok 4, DeepSeek-V3.1, Qwen3-Max-Preview) yielding 51 concepts, axial coding in 3 ICR rounds with 3 domain experts sampling 24-54 records with Krippendorff's alpha, selective coding to 3 core categories, theoretical-saturation check on unseen agents (WebThinker, OpenManus), human-LLM inter-coder reliability alpha~0.80-0.90.
- `554b0217976296b3` DeepVerifier DRA failure taxonomy (Wan et al., CUHK/Tencent; arXiv 2601.15808, ACL 2026 Findings) defines 5 major classes and 13 sub-classes built to drive rubric-based verification: Problem Understanding, Finding Sources (dominant — e.g., consulting wrong evidence, generic searches), Reasoning (premature conclusions, misinterpretation, hallucinated/overconfident claims), Action Errors, Max Step Reached; 13 sub-class names shown in Fig. 3 were not all reproduced in the fetched text.
- `bacb0e234ea35e04` DeepVerifier (arXiv 2601.15808) was measured on 2,997 agent actions across 90 tasks on WebAggregatorQA (CK-Pro agent, Claude-3.7-Sonnet backbone); 555 error points annotated by two staff annotators against human reference solutions with 63% cross-annotator overlap; taxonomy built by iterative clustering/labeling of error points.
- `7e96686c5bcb8461` Three-tier task-phase taxonomy (Lu, Li, Huo; arXiv 2508.13143, ASE 2025 NIER) aligns agent failures to task phases: planning errors, task execution issues, incorrect response generation; benchmarked on 34 programmable tasks with 3 open-source agent frameworks x 2 LLM backbones, ~50% observed task completion, causes induced from in-depth failure analysis.
- `9ef58d49f849ae77` Tool-agent parameter-filling taxonomy (Xiong et al.; arXiv 2507.15296, 'Butterfly Effects in Toolchains') defines five failure categories derived from the invocation chain of a mainstream tool agent; parameter-name-hallucination failures reportedly stem chiefly from LLM limits, other categories trace to input sources; measured with 15 input perturbation methods correlated against failure categories. Individual category names are not enumerated in the abstract and could not be verified (arXiv HTML 404, PDF unreadable).
- `0e16f2f530c68f78` AgentEval failure taxonomy (Guo, Wu, Yiu; arXiv 2604.23581, ACL 2026 Industry) is a hierarchical failure taxonomy with 3 levels and 21 subcategories used for DAG-structured step-level evaluation with root-cause attribution; category names not enumerated in the abstract.
- `7135e88581ab3411` AgentEval (arXiv 2604.23581) was measured on 450 test cases across three production workflows and two agent model families: Cohen's kappa=0.84 vs. human experts, 72% root-cause accuracy, failure-detection recall 0.89 vs. 0.41 for end-to-end; transfer shown on tau-bench and SWE-bench traces.
- `284dea68d04d19f5` Tool-invocation reliability taxonomy (Huang, Malwe, Wang; arXiv 2601.16280, ICAIBD 2026) is a 12-category error taxonomy spanning tool initialization, parameter handling, execution, and result interpretation; category names not enumerated in the abstract; measured on 1,980 deterministic test instances across open-weight (Qwen2.5 series, Functionary) and proprietary models (GPT-4, Claude 3.5/3.7), with tool-initialization failures identified as the primary bottleneck for smaller models.
- `bdfabd828b164b23` SpreadsheetBench 2 (Zhu et al.; arXiv 2606.29955) provides a trajectory-analysis failure taxonomy; dominant bottlenecks reported as insufficient spreadsheet inspection and incorrect target-cell selection.
- `f9a27846604a85ce` NOMAD (Giannouris & Ananiadou; arXiv 2511.22409) presents an error taxonomy for LLM-generated UML diagrams covering structural, relationship, and semantic/logical errors.
- `249cc166e0a084fe` Harness-sensitivity taxonomy (Cho et al.; arXiv 2605.26731) is a six-label taxonomy from a 432-run controlled experiment; format_violation dominates capable models while wrong_file dominates low-capability models.
- `89d1caf3454ccaea` There is no single canonical LLM-agent failure taxonomy — the space is fragmented by scope (multi-agent, agent-environment, deep research, tool-use, production runtime) and by whether categories name the responsible component or the observable symptom; no survey synthesizing them was located in the search.
- `c1a0b768075c2467` Verdict of the research: besides MAST there is a substantial, actively growing body of empirically derived LLM-agent failure taxonomies, all reporting explicit measurement corpora (trace counts, annotator counts, agreement statistics, task/turn counts); the strongest-documented are the Interaction-Centric Taxonomy (41 modes; 40 grounded worked examples; judge reproducibility kappa=0.76), DEFT (14 modes; grounded theory over ~1,000 DRA reports with Krippendorff's-alpha validation), Aegis (6 modes; 142 annotated failed traces), DeepVerifier (5 classes/13 sub-classes; 555 annotated error points), and the longitudinal silent-failure taxonomy (5 classes; 22 production incidents).
- `7d3033022fab8078` Reflexion (Shinn et al., 2023) reports pass@1 on HumanEval of 91% vs 80% for the then-SOTA GPT-4 baseline, a +11pp gain from the trial-error-reflection loop (arXiv:2303.11366v4).
- `691b74f6eacc0b29` On ALFWorld (134 text-world tasks), ReAct+Reflexion completed 130/134 tasks vs ReAct-only which stalled; the paper reports an absolute +22% improvement over strong baselines within 12 iterative trials (Reflexion §4.1).
- `5016a8902d5d98e8` On HotpotQA, Reflexion improved over baselines by +20%; with ground-truth context (CoT-GT) it lifted accuracy by 14% without access to ground-truth answers (Reflexion §4.2).
- `ccbdd85316bb9ecd` Reflexion's ablation on the 50 hardest HumanEval Rust problems (GPT-4 base): base 0.60; self-reflection without test execution 0.52; test execution without natural-language reflection 0.60; full Reflexion (tests+reflection) 0.68. The reflection pass added +8pp only when paired with an external evaluator; 'reflect but no tests' performed worse than the base (Reflexion §4.3, Table 3).
- `3e53d4e63a90eb15` On HotpotQA, an episodic-memory-only ablation (recent trajectory, no reflection) gained less than the full reflection step, which added an 8% absolute boost — the verbal reflection step, not just feeding back the failed trace, carried the gain (Reflexion §4.2).
- `4ab190f11c08150f` Reflexion hurt on MBPP (Python): 77.1 vs GPT-4 baseline's 80.1 pass@1, attributed to flaky self-generated unit tests — the internal test suite's false-positive rate was 16.3% on MBPP vs 1.4% on HumanEval, so the agent prematurely accepted wrong submissions (Reflexion §4.3).
- `83f1964c6217fb0d` ReAct (Yao et al., 2023), interleaving sparse 'thought' traces with environment actions, outperformed imitation and RL baselines by +34% (ALFWorld) and +10% (WebShop) absolute success rate; on WebShop ReAct's 40.0% SR beat Act (30.1%), IL (29.1%), and IL+RL (28.7%). Grounding reasoning in a Wikipedia API cut hallucination: among failed HotpotQA trajectories, hallucination was the cause in 56% of CoT cases vs 0% for ReAct (arXiv:2210.03629).
- `e5e61f2516f62242` ReAct's ablation against Inner-Monologue-style prompts (ReAct-IM, replaying dense external feedback with no internal goal reasoning) shows internal sparse reasoning beats pure external-feedback replay: 71% vs 53% overall ALFWorld success — direct evidence about which reflection content adds value (goal-level re-planning) vs not (regurgitating observations) (ReAct §4).
- `af2b1630bfe751ef` CRITIC (Gou et al., 2023) has the model validate its own output against external tools (search engine, code interpreter) and revise; the authors report it 'consistently enhances the performance of LLMs' across free-form QA, mathematical program synthesis, and toxicity reduction, concluding external feedback is 'crucially important' for self-improvement. Caveat: only the abstract was verifiable; per-benchmark numbers were not verified (arXiv:2305.11738, ICLR 2024).
- `b66006c1f5cda644` Tyen et al. (2024, ACL Findings): LLMs cannot reliably find their own reasoning errors (even in objective, unambiguous cases), but can correct them when given the error location; feeding ground-truth mistake-location boosts performance across 5 reasoning tasks. A small classifier trained on out-of-domain data beat prompting a large model at mistake-finding — the bottleneck is error detection, and external/structured detection signals make correction reliable (arXiv:2311.08516).
- `a375ddea979ace49` Training reflection into weights (rather than prompting it) shows large gains: SPOC (2025, preprint) — single-pass interleaved solution+verification model via SFT + online RL improved Llama-3.1-8B/70B by +8.8/+11.6pp on MATH500, +10.0/+20.0pp on AMC23, +3.3/+6.7pp on AIME24. S2R (2025, preprint) — teaching self-verification + self-correction via SFT and outcome/process RL lifted Qwen2.5-Math-7B from 51.0% to 81.6% with only 3.1k seed samples (arXiv:2506.06923; arXiv:2502.12853; both arXiv preprints).
- `6729b6c2eba5c724` Retroformer (Yao et al., 2024) argues Reflexion-style verbal feedback 'does not reason and plan in ways compatible with gradient-based learning'; its learned retrospective model tuned by policy gradient improves agents over time and outperforms baselines that do not use environment rewards — the optimization of the reflection itself is the value-adding successor step (arXiv:2308.02151).
- `ab49a330f50c49ef` Huang et al. (2024, ICLR) tested intrinsic self-correction (no external feedback) on reasoning: models 'struggle to self-correct their responses without external feedback, and at times, their performance even degrades after self-correction' (arXiv:2310.01798).
- `f3583d648963c6ab` Self-Refine (Madaan et al., 2023) reports ~20% absolute average gain across 7 tasks, but on GSM8K the self-feedback loop gained ≈0pp (GPT-3.5 64.1→64.1, ChatGPT 74.8→75.0, GPT-4 92.9→93.1); the paper's explanation is that the model cannot detect its own math errors — ChatGPT's feedback was 'everything looks good' in 94% of instances — while with oracle (external) error feedback the gain became 5%+ (arXiv:2303.17651v2).
- `3900b567dfe90746` Self-Refine's feedback-quality ablations: actionable feedback >> generic >> none (Code Optimization 27.5 vs 26.0 vs 24.8; Sentiment Reversal 43.2 vs 31.2 vs 0); on a weak model (Vicuna-13B) the refinement step fails entirely — the model cannot even emit feedback in the required format (Self-Refine §4, Table 2).
- `1051969642545360` Liang et al. (EMNLP 2024) document 'Degeneration-of-Thought': once an LLM has established confidence, iterative self-reflection 'is unable to generate novel thoughts,' so reflection alone stagnates; their multi-agent debate outperformed it on commonsense translation and counter-intuitive arithmetic, and they find an LLM is 'not a fair judge' across different models (arXiv:2305.19118).
- `0b60fb3ea9f2562a` A 2026 arXiv preprint (not yet peer-reviewed) recasts iterative self-correction as feedback control with a stability threshold (correct when error-correction-rate/error-introduction-rate exceeds a bound); across 7 models × {GSM8K, MATH, StrategyQA}, only o3-mini (+3.4pp), Claude Opus 4.6 (+0.6pp), and o4-mini (≈0) were non-degrading, while GPT-5 and four others lost accuracy; a verify-first prompt intervention turned GPT-4o-mini's −6.2pp degradation into +0.2pp (arXiv:2604.22273).
- `399d42c5feedc012` A 2026 preprint (arXiv:2605.27559) finds the conditional miscorrection rate dominant (53–94%) across model families and benchmarks in a 'detection-without-correction' decomposition; another 2026 preprint (arXiv:2606.02646) finds a noise placebo matches self-correction on free-form math at 4× scale — suggesting much of the gain attributed to debate/reflection may be plain re-evaluation — and that 30 dense debating agents produce no more answer diversity than one. Both are arXiv preprints.
- `0cae2c2bdf2ef1dc` A 2026 arXiv preprint (arXiv:2606.05976) shows the format/role of reflection matters to the point that the 'models can't self-correct' result may be partly an artifact: keeping an erroneous claim byte-identical but relabeling it from the model's own <thought> to an external role (user message, tool response, <memory>) raised explicit-correction rates by 23–93 percentage points (significant in 10/12 settings).
- `888b4004f537880f` Renze & Guven (FLLM 2024) tested 9 LLMs × 8 self-reflection agent designs on a 1,000-question multi-domain MCQA exam and found all reflection types significantly improved re-answer accuracy (p<0.001) across all models, with information-rich reflections (Instructions, Explanation, Solution) beating minimal ones (Retry, Keywords, Advice), and even the plain 'Retry' agent improving. Protocol caveats from the paper itself: reflections were generated with the correct answer given as an external feedback signal (answer labels redacted afterwards), the task is single-step re-answering rather than an agent loop, the LSAT-AR exam dominated the gains, and strong models near the 100% ceiling compress the effect (arXiv:2405.06682).
- `9240d1f949e33e15` Established synthesis: reflection loops produce large, replicated gains only when a reliable external evaluator grounds the reflection (unit tests/environment/exact-match/tools) — Reflexion +11pp HumanEval, 130/134 ALFWorld, +20% HotpotQA; ReAct +34pp/+10pp over IL baselines; CRITIC consistent gains; the Reflexion ablations pin this down (+8pp with tests, −8pp without).
- `6b4813b0dee8f914` Established synthesis: pure intrinsic self-correction (no external feedback) on reasoning adds ≈0 or negative value — Huang et al., Self-Refine's GSM8K ≈0pp, Reflexion's test-omission ablation (0.52 < 0.60 base), and the 2026 stability-threshold preprint (only 3 of 7 tested frontier models non-degrading).
- `8fe5600483b3642b` Established synthesis: the failure point in self-correction is error detection, not correction — Tyen et al. (location signals restore correction), Self-Refine's '94% everything looks good,' and the detection/miscorrection-decomposition preprint converge here; designs that externalize the critic (verifier model, tools, classifiers, verify-first prompting, RL-trained self-verification) improve reliability, while designs asking the generator to judge its own ungrounded reasoning add little or hurt.
- `33f44ff98f2913eb` Uncertain/contested: whether 'self-reflection' per se (vs plain retry/re-sampling) contributes beyond the verification gate — Renze & Guven found even 'Retry' helped, a 2026 preprint finds a noise placebo matches self-correction on math, and another finds the 'can't self-correct' result is partly a chat-template role-labeling artifact; these are not yet reconciled. Renze & Guven's positive MCQA result and Huang et al.'s negative reasoning result are not comparable protocols (correct-answer-conditioned reflection + re-answer vs intrinsic correction).
- `2142573ef60a16f5` Coverage caveats: CRITIC was verified from its abstract only (no accessible full text); the 2025–2026 papers are arXiv preprints, not peer-reviewed; no claim was verified that could not be read in a primary source.
- `9a04dce90972235e` Trust in automation should be calibrated: a person's trust should match the automation's actual capabilities; both overtrust (uncertainty ignored) and distrust (valid outputs ignored) degrade joint performance, and the design goal is 'appropriate reliance.' (Lee & See 2004, Human Factors 46(1):50-80; verified via metadata, full text paywalled)
- `095902827fb17ae8` In AI-assisted decision making, calibrating trust case-by-case is a key to success: knowing when to trust or distrust the model lets an expert apply their knowledge, improving outcomes in cases where the model is likely to perform poorly. (Zhang, Liao, Bellamy 2020, ACM FAT*)
- `862b7661a840e726` People avoid algorithms after seeing them err even when the algorithm outperforms them, while being more forgiving of equivalent human errors (algorithm aversion). (Dietvorst, Simmons, Massey 2015, JEPG; verified at title/metadata level, full text not fetched)
- `79281ff089bb0d1f` Modern neural networks are miscalibrated (overconfident), and temperature scaling — a single-parameter post-hoc step — is 'surprisingly effective at calibrating predictions' on most datasets; the standard cheap, model-agnostic production fix. (Guo et al. 2017, ICML)
- `fb43f89f710315c4` Conformal prediction lets any pre-trained model produce prediction sets/intervals with a user-specified coverage guarantee (e.g. 90% coverage), valid distribution-free and non-asymptotically. (Angelopoulos & Bates, arXiv 2107.07511)
- `e88f28106f9d0f13` Conformal prediction for generative outputs: a calibrated stopping+rejection rule over LM sampling returns a small set of candidate answers containing at least one acceptable answer with high probability, and can flag individual spans (phrases/sentences) as non-hallucinated with statistical guarantees. (Quach et al., Conformal Language Modeling, ICLR 2024)
- `6d07ba987c2ea213` Verbalized confidence: a GPT-3 model trained to express '90% confidence'/'high confidence' in words produces well-calibrated probabilities without using model logits and stays 'moderately calibrated under distribution shift.' First-party OpenAI result; basis for LLM verbal-confidence features. (Lin, Hilton, Evans 2022, arXiv 2205.14334; verified abstract)
- `67ab1b96cc2d1128` P(True)/self-evaluation: larger LMs are well-calibrated on multiple-choice and true/false questions 'when provided in the right format'; asking the model to evaluate the probability its own answer is correct yields encouraging calibration, improving when the model first samples many of its own answers. (Kadavath et al., Anthropic, arXiv 2207.05221; verified abstract)
- `1473b45f4850c8dd` Semantic entropy (sampling-based uncertainty at the meaning level) detects hallucinations/confabulations across datasets and tasks without a priori task knowledge and without task-specific data, and helps users understand when they must take extra care with LLMs. (Farquhar et al., Nature 630:625-630, 2024)
- `998dde220cf1e31b` A supervised estimator trained on labeled data and hidden activations beats entropy baselines for detecting wrong answers in LLMs, works in black/grey/white-box settings, and better uncertainty estimation leads to better calibration performance. (Liu et al., arXiv 2404.15993; verified abstract)
- `3c233462b12abfbd` Sampling multiple reasoning paths and taking the most consistent answer (self-consistency) improves accuracy on arithmetic/commonsense benchmarks (GSM8K +17.9%). (Wang et al., ICLR 2023; verified abstract)
- `ddfc343faef6bd2c` A statistically calibrated generative LM must hallucinate on 'arbitrary' facts appearing once in the training data, at a rate bounded below by a Good-Turing estimate, independent of architecture or data quality; mitigation requires post-training or selective behavior, not better calibration alone. (Kalai & Vempala, STOC 2024; verified abstract)
- `ac4908a0f4b2b020` LLM uncertainty-quantification methods (predictive-distribution, representation, ensemble-based) 'perform well under the restrictive assumption of no ambiguity [but] degrade to close-to-random performance on ambiguous data' — they conflate epistemic and aleatoric (ambiguous) uncertainty. (Tomov et al., arXiv 2511.04418; verified abstract)
- `f7b7247ebe13d8e0` Showing a model's confidence score alone calibrated people's trust and improved their performance in a mortgage-lending decision task; local explanations did not reliably add benefit, and improved trust calibration was not sufficient for accuracy gains — experts still had to bring knowledge complementary to the AI's errors. (Zhang, Liao, Bellamy 2020, FAT*)
- `fcfc5df953031f06` In large-scale image classification (N=1,152,000 observations), machine confidence scores and human confidence ratings each independently and significantly improved hybrid human+AI accuracy (machine confidence log-odds effect 0.257, human confidence 0.104, class error model 0.101; all p<.001); human confidence ratings were as effective as an explicit error model. (Steyvers et al., PNAS 119(11), 2022; full text verified)
- `0ad6075069f5864a` Complementarity has narrow preconditions: a hybrid human+AI pair beats both human-only and machine-only ensembles only when the two classifiers perform within a bounded accuracy band, and the key limiting factor is the latent correlation between human and machine confidence (observed ~0.4 for human+AI vs ~0.7 human-human); effective AI advice should be accurate and statistically independent of human judgment. (Steyvers et al., PNAS 2022)
- `66da2477aa7e8e3f` In a 199-participant study, adding explanations to AI recommendations did not reduce acceptance of wrong advice; forcing deliberative engagement ('cognitive forcing') did reduce overreliance but was the least-liked design and helped high 'Need for Cognition' users more. (Buçinca et al., CSCW 2021; verified abstract)
- `a0d0a9a6f35b7d72` Explanations only engender appropriate reliance and complementary performance insofar as they let the operator check the AI prediction's correctness; most explanations and most tasks do not enable this verification. (Fok & Weld, arXiv 2305.07722; verified abstract)
- `e6515e624c072972` Uncertainty-based abstention measurably improves deployed-quality outcomes: filtering out semantically uncertain questions in a clinical radiology VLM raised answer accuracy on retained questions from 51.7% to 76.3% (GPT-4o) and 54.8% to 63.8% (GPT-4.1), both p<.001. (Wienholt et al., Eur Radiol 2026, arXiv 2510.09256; verified abstract)
- `c0fb76a443969f8b` An adaptive conformal semantic-entropy score with an accept/abstain decision rule carries a finite-sample, distribution-free bound on error rate among accepted answers (TriviaQA AUROC 0.88 vs 0.65 for token entropy) — conformal-guaranteed abstention is deployable. (Karimi et al., IJCAI 2026, arXiv 2605.04295; verified abstract)
- `154306422881beb8` No first-party product documentation describing deployed operator-facing confidence UIs (e.g., what ChatGPT/Claude/Vertex actually render for confidence in production) was located; the production picture is reconstructed from methods papers, first-party lab research (OpenAI Lin 2022, Anthropic Kadavath 2022), and deployment-style clinical studies — an absence-of-evidence claim, not proof of non-existence (unverified).
- `6e30b9e0905d102a` Established multi-source finding: showing a calibrated confidence score helps operators when the score is the operative mechanism — it improved trust calibration (Zhang 2020), machine and human confidence each significantly improved hybrid human+AI accuracy within a narrow accuracy band (Steyvers 2022), and uncertainty-based abstention improved deployed clinical accuracy ~20-25 points (Wienholt 2026).
- `b7ab00766ba80fd0` Established multi-source finding: explanations are a weak lever on their own — they do not reduce overreliance (Buçinca 2021) and only help when they enable verification (Fok & Weld 2024).
- `6cf5f705aaedf39d` Established multi-source finding: hard limits — calibrated LMs must hallucinate single-occurrence facts (Kalai & Vempala 2024) and UQ degrades under genuine ambiguity (Tomov 2025) — so confidence display must be paired with abstention/verification.
- `1b0ebc092cb2ebe8` Unverified from sources reached: how and whether specific deployed vendors (ChatGPT, Claude, Vertex) present confidence to operators in production; several key claims (F7 verbalized confidence, F8 P(True), F11 self-consistency, F12 calibrated-must-hallucinate) rest on author abstracts rather than full texts; full texts of Lee & See 2004, Dietvorst 2015, and Yáñez 2026 were not read (title/metadata level only).
- `551c6fb883620cdf` AWS CloudTrail records every request's principal in userIdentity.type with an explicit enumeration: Root, IAMUser, AssumedRole, Role, FederatedUser, Directory, AWSAccount, AWSService, IdentityCenterUser, Unknown; federation paths add SAMLUser / WebIdentityUser; userName, principalId, arn, and accessKeyId sit under the same element.
- `276b963419de5d5e` AWS defines AWSService as 'The request was made by an AWS account that belongs to an AWS service' (e.g. Elastic Beanstalk assuming a role to call other services) — a dedicated kind for automated processes, distinct from human identity kinds.
- `adbc0cde1ae47dc7` Google Cloud splits audit logs into four named classes: Admin Activity (user-driven API calls or actions modifying resource configuration/metadata), Data Access, System Event (log entries written by Google Cloud systems that modify resource configuration and aren't driven by direct user action, e.g. autoscaling adding/removing VMs), and Policy Denied — a first-order channel filter encoding whether a human drove an entry.
- `3aee4552498f51e4` GCP audit logs carry the caller identity in AuditLog.authenticationInfo (principalEmail) and network origin in RequestMetadata.callerIp; for service-to-service calls inside Google's production network callerIp is redacted to 'private', and this includes calls made by Google-owned service accounts (service agents) even when initiated by a user.
- `832663c967f96aa0` Kubernetes audit events (audit.k8s.io/v1 Event) record user ('Authenticated user information'), verb, sourceIPs, requestURI, requestReceivedTimestamp, responseStatus, userAgent, and annotations.
- `87e145dc13717659` Kubernetes distinguishes principals by a username convention; audit policy rules match on users and userGroups; documented examples include username system:kube-proxy, group system:authenticated, and impersonation example system:serviceaccount:default:my-controller.
- `b85e02376e6436d9` Kubernetes treats userAgent as explicitly untrusted: 'the UserAgent is provided by the client, and must not be trusted.'
- `e4d1d79629285352` AWS sessionContext.sourceIdentity identifies the original user identity making the request across role assumption and role chaining (the field survives AssumeRole → service API calls); sessionContext.sessionIssuer records how temporary credentials were obtained; onBehalfOf carries the IAM Identity Center user a call was made for.
- `1696dd59343bd054` Kubernetes impersonation records both identities in the audit event: the authenticated caller in user and the impersonated identity in impersonatedUser, e.g. user system:serviceaccount:default:my-controller with impersonatedUser jane.doe@example.com; an audit event is logged for each impersonation request.
- `00aadbc4be76ab80` GCP attributes a human's actions executed through Google-managed service agents with the originating sign-in context, and notes that in multi-service flows, unless explicit identity delegation is in place, the service might propagate the immediate caller's origin.
- `79da1f19e3565dad` GitHub's audit log records hashed_token (SHA-256 hash of the token used for authentication), programmatic_access_type, and token_scopes; covered authentication methods are personal access token, OAuth token, GitHub Apps (as an app installation or on behalf of a user), deploy key, and SSH key — any event attributable to a compromised token can be found by its hash.
- `97588acadbb60f33` AWS records accessKeyId (the specific credential that signed the request) and credentialId for bearer-token callers.
- `f0389e3a1b973eae` The OpenAI Agents SDK traces every run as a trace (workflow_name, trace_id, optional group_id to link multiple traces from the same conversation, metadata) composed of spans linked by trace_id/parent_id; agent runs are wrapped in agent_span(), handoffs in handoff_span(), tool calls in function_span(), LLM calls in generation_span(), guardrails in guardrail_span().
- `4ac0d73a111f7e22` In a multi-agent framework the attribution unit is which agent performed which step in which workflow, on behalf of which conversation (group_id); linking the human/end-user at the trace level rather than the span level is a synthesis/opinion, not SDK-stated.
- `bdf2aeb421403f14` Google Cloud MCP servers — the standard way agent frameworks invoke external tools — write Data Access audit logs with the service name format SERVICE_NAME.googleapis.com/mcp, enabling audit of agent-orchestrated tool calls inside the platform's audit log.
- `51d27a5241433608` Google Cloud states 'Log entries written by Cloud Audit Logs are immutable.'
- `17e10ae638f998a8` Kubernetes audit records begin their lifecycle inside the kube-apiserver, are pre-processed according to a policy, and are written to a backend; current backend implementations include log files and webhooks.
- `69618f21014c3ad5` A single cross-vendor 'agent identity' standard for web audit trails is not verifiable from the sources reached: the proposed 'Agent ID' specification (agent-id GitHub org and agent-id.github.io) returned 404/empty; its existence/status is unknown — not verifiable.
- `5ba555c707cfab81` How LLM-native audit trails (prompt → tool → action) get rendered into host-platform audit logs beyond the GCP MCP example is not covered by any fetched source; that mapping is opinion.
- `6bc015d1e12cd076` Whether a log's userAgent or token fields genuinely identify an LLM agent vs. a deterministic script is not specified by the sources; k8s explicitly flags userAgent as client-supplied and untrusted.
- `ae9c67ba8d9f53fa` Compaction is defined as 'taking a conversation nearing the context window limit, summarizing its contents, and reinitiating a new context window with the summary'; in Claude Code the model is passed history to 'summarize and compress the most critical details... preserving architectural decisions, unresolved bugs, and implementation details while discarding redundant tool outputs or messages', continuing with compressed context plus the five most recently accessed files. (Anthropic Engineering, effective-context-engineering, 2025-09-29)
- `dfb60f28dd6c7c53` Claude Code operational definition of compaction: 'Older tool outputs are cleared first, then the conversation is summarized.' (Claude Code Glossary)
- `76474e76517a8afc` Summarization = LLM-written prose condensation of messages; in Anthropic stack it is the engine inside compaction; OpenAI offers it as a distinct cookbook pattern.
- `2658adb1b6119494` Pruning/truncation = dropping raw transcript items (tool results, old turns, unplayed audio) without replacing them with a summary.
- `27864aab0d2d7442` Claude Agent SDK auto-compacts when context window approaches limit: emits a compact_boundary message; a PreCompact hook allows archiving the full transcript before summarizing — the documented way to keep evidence. (Claude Agent SDK, agent-loop)
- `f33c569f53a96c52` Claude Code structured summary keeps: requests and intent, key technical concepts, files examined/modified with important code snippets, errors and how they were fixed, pending tasks, current work; it replaces verbatim conversation — full tool outputs and intermediate reasoning are gone. Detailed instructions from early in conversation may be lost; persistent rules belong in CLAUDE.md. (Claude Code docs)
- `2d27b81edd2936f7` After Claude Code compaction, project CLAUDE.md, auto memory, and MCP tools reload; the skill listing is the one startup item not re-injected.
- `e763351d80a7f4c0` Claude Code context-window simulation models the summary at ~12% of the summarized token count — illustrative, not a measurement.
- `2c0287690e80db51` Evidence is not destroyed, only removed from the model-visible window: every Claude Code session transcript is written to a JSONL file under ~/.claude/projects/; the SDK can mirror transcripts to S3/Redis so any host can resume.
- `7099cb2b4578c725` Prompt-cache economics: compaction invalidates the cached conversation prefix by design; a warm mid-session /compact reads the prefix from cache so it 'costs a fraction of what the context size suggests'; /rewind reuses an already-cached prefix; after long breaks Claude Code offers 'resume from a summary'. (Claude Code prompt-caching docs)
- `adda7097ca47a201` OpenAI compaction is server-side (context_management: [{type:'compaction', compact_threshold:200000}] or /responses/compact endpoint) and returns an 'encrypted compaction item that carries forward key prior state and reasoning into the next run using fewer tokens... opaque and not intended to be human-interpretable' plus retained items; you must not prune the output, and with previous_response_id chaining 'do not manually prune'. (OpenAI Compaction guide)
- `84a95bae5288ece5` Contrast: Anthropic's compaction output is a readable structured summary; OpenAI's is an opaque encrypted state item whose fidelity is unquantified in the doc.
- `1c1bd4db44906d9f` Anthropic warns 'overly aggressive compaction can result in the loss of subtle but critical context'; guidance is to 'Start by maximizing recall to ensure your compaction prompt captures every relevant piece of information from the trace, then iterate to improve precision.' (Effective context engineering)
- `31742f4c5353a826` Anthropic treats tool-result clearing ('context editing') as 'one of the safest lightest touch forms of compaction'.
- `6b3158691054e2b1` LoCoMo found session summaries as context do not significantly improve performance despite high recall accuracies, 'likely due to loss of information during the conversion of dialogs to summaries'; retrieval of raw observations did help (+5% for top-5 retrieved); incremental summarization was strongest for event summarization. (Maharana et al., arXiv:2402.17753)
- `a9602827037b1026` LoCoMo long-context QA: gpt-4-turbo scored 32.4 vs human 87.9; a 16k-context model fell to 2.1% on adversarial questions while a 4k-context model got 70.2%. (arXiv:2402.17753)
- `880cc6d2318d69e3` Claude platform context editing 'automatically clears stale tool calls and results from within the context window when approaching token limits... removing stale content while preserving the conversation flow.' (claude.com/blog/context-management)
- `c55bac41f7ac5dee` OpenAI Realtime conversation.item.truncate cuts audio and removes the text transcript for the unplayed portion; OpenAI explicitly states it does not produce a truncated transcript.
- `3d1604aaeeb8b61b` Lost in the Middle (Liu et al., TACL 2023): models use beginning and end of context best; 'performance is often highest when relevant information occurs at the beginning or end... and significantly degrades when models must access relevant information in the middle.' Supports pruning the middle over pruning oldest turns.
- `692f03b97bba1e0f` Structured note-taking/agentic memory: the agent regularly writes notes persisted to memory outside the context window, pulled back into the window at later times; memory tool keeps key findings and project state across sessions out of the window. (Effective context engineering; context-management blog)
- `1694c889dc5b5cdf` Subagent architectures bound context growth to a summary: a subagent 'returns only a condensed, distilled summary of its work (often 1,000–2,000 tokens)' and the main agent's context grows by that summary, not the full subtask transcript.
- `ce2aee7d1f5ece13` MemGPT formalized OS-style virtual context management: 'intelligently manages different memory tiers... providing extended context within the LLM's limited context window.' (Packer et al., arXiv:2310.08560)
- `15116661844082ce` As reported by a second research group (vendor evaluation, not MemGPT's own paper): 'The MemGPT framework currently leads performance metrics with 93.4% accuracy using gpt-4-turbo, a significant improvement over the 35.3% baseline achieved through recursive summarization' on Deep Memory Retrieval; the same paper reports its own system at 94.8% on DMR and gains 'up to 18.5%' with ~90% latency reduction on LongMemEval using a non-lossy episodic store. (Rasmussen et al., Zep, arXiv:2501.13956)
- `21ee0cd54dbd7b92` Anthropic vendor-internal evals (published in launch post, methodology internal and not reproducible — treat as claims): combining memory tool with context editing improved performance by 39% over baseline; context editing alone 29%; in a 100-turn web-search eval context editing finished workflows that would otherwise fail due to context exhaustion while reducing token consumption by 84%. (claude.com/blog/context-management)
- `fe75beb9da3fa6ba` Chroma context rot (18 models, Jul 2025): significantly higher performance on focused prompts (~300 tokens) vs full prompts (~113k tokens) on LongMemEval; distractor effects amplify with length; even simple tasks degrade as input grows. Supports pruning irrelevant context rather than growing the window.
- `0006a8ef037932a1` No public apples-to-apples benchmark runs the same agent traces through naive truncation vs LLM summarization vs compaction vs retrieval and reports fact-recall side by side; vendor numbers are directional evidence, not a ranking. What would settle it: a neutral benchmark with fixed transcripts, identical inputs across methods, and both downstream task success and fact-recall metrics.
- `fc0733f9714e89f6` MCP's trust model: clients trust servers they connect to; local MCP servers are trusted like any other installed software and run with the same privileges as the client; the connection decision rests with the user/administrator (SECURITY.md).
- `3f8680deb624fd36` In MCP, command execution to launch STDIO servers, server side effects (filesystem, git, DB, network, system commands), resource access, and LLM-driven tool invocation are declared intended behaviors, not reportable vulnerabilities (SECURITY.md).
- `24fb2af3014fffe6` MCP's stdio transport is not a sandbox; 'one stdio peer can crash, hang, exhaust resources of, or otherwise deny service to the other' is out of scope, and deployments running stdio at reduced privilege own isolation enforcement (SECURITY.md).
- `15216a4151961b81` MCP in-scope security categories: protocol-level flaws, auth/authz bypasses, SDK implementation bugs, sandbox escapes, session hijacking, token theft/leakage, cross-tenant access (SECURITY.md).
- `58d35d9cec3463a7` MCP clientInfo/serverInfo are self-reported, unverified, and MUST NOT be used for security decisions (Spec 2026-07-28).
- `728334f20b68d83d` MCP tool annotations MUST be considered untrusted unless they come from trusted servers (Spec 2026-07-28 Tools).
- `c81957cf3aac4c06` Confused Deputy (OAuth proxy) attack: a proxy with static client ID + dynamic client registration + consent cookies lets an attacker steal an authorization code; mitigations MUST include per-client server-side consent, exact-string redirect_uri validation, cryptographically random single-use state set only after consent, __Host- prefixed Secure/HttpOnly/SameSite=Lax consent cookies, and frame-ancestors CSP / X-Frame-Options DENY (Security Best Practices).
- `af0d9ca54cec9c18` Token passthrough: MCP servers MUST NOT accept tokens not explicitly issued for the MCP server, nor forward them downstream; clients MUST send the RFC 8707 resource parameter and servers MUST validate audience (Security Best Practices, Authorization Security Considerations).
- `57ed22f30f0c6760` SSRF during OAuth metadata discovery: servers can point resource_metadata/authorization_servers/token endpoints at cloud metadata (169.254.169.254), localhost, or private ranges; mitigations are HTTPS-only (except loopback in dev), blocking private/link-local/loopback ranges (RFC 9728 7.7), egress proxies, and DNS-pinning (Security Best Practices).
- `9eba1c8e9d2d65b7` State handle hijacking: MCP is stateless and servers mint state handles returned as tool arguments; possession of a state handle MUST NOT be treated as authentication — per-request authorization, user-bound handles (<user_id>:<handle>), secure non-deterministic handles, and expiry are required (Security Best Practices).
- `c3484bbb30701da2` Local server compromise mitigations: mandatory pre-execution consent showing the exact untruncated command, sandboxing, least privilege, warning on dangerous patterns (sudo, rm -rf, network ops); prefer stdio or gate HTTP transports behind tokens / restricted unix sockets (Security Best Practices).
- `e4344628bee35e4c` OAuth authorization URL validation: servers MUST only allow http/https (https in production), reject dangerous schemes (javascript:/data:/file:), never open URLs via shell, and apply CSP script-src 'self' (Security Best Practices).
- `0d9ab0996bcbca80` Mix-up attacks: iss-based Authorization Response Validation mitigates them; PKCE alone does not prevent the attack because the client transmits the verifier to the attacker's token endpoint (Security Best Practices).
- `9a9c35587bb37962` Scope minimization: use progressive least-privilege scope sets, incremental WWW-Authenticate scope step-up challenges, avoid omnibus scopes, and never trust claimed scopes alone — server-side authorization logic required (Security Best Practices).
- `26a851d4bc8775f8` MCP auth applies to HTTP transports (server SHOULD conform); STDIO implementations SHOULD NOT use the OAuth spec and instead take credentials from the environment; all authorization-server endpoints must be HTTPS, redirect URIs must be localhost or HTTPS, PKCE S256 is MUST, and clients must refuse to proceed if code_challenge_methods_supported is absent (Spec 2026-07-28).
- `854c0680bf360524` MCP tools: servers MUST validate all tool inputs, implement access controls, rate-limit tool invocations, and sanitize tool outputs; clients SHOULD prompt for confirmation on sensitive operations, show inputs before calling, validate results before passing to the LLM, implement timeouts, and log usage (Spec 2026-07-28 Tools).
- `fb854e84fe169d26` MCP human-in-the-loop: for trust & safety and security there SHOULD always be a human in the loop able to deny tool invocations, with visible tools and confirmation prompts (Spec 2026-07-28 Tools).
- `f085f8338af5bf85` MCP tools list MUST NOT vary per-connection or as a side effect of other requests, but MAY vary by the authorization presented on the request (scope-filtered capability exposure) (Spec 2026-07-28 Tools).
- `425a6e7f9fed9c50` MCP JSON Schema $ref resolution must not auto-dereference network URIs by default; composition keywords can be a DoS vector needing depth/sub-schema/time bounds (Spec 2026-07-28).
- `7e6334fcd675c07a` MCP x-mcp-header (tool params mirrored into HTTP headers) must use token-safe names and primitive types and be rejected if malformed; servers SHOULD NOT mark secrets/PII since header values are visible to intermediaries (Spec 2026-07-28 Tools).
- `5a487b22cf615c84` MCP icon URIs/bytes are untrusted: reject unsafe schemes, fetch without credentials, same-origin checks, magic-byte MIME validation, resource-exhaustion limits (SVG can contain embedded JS) (Spec 2026-07-28).
- `d04b401a1b7e08c0` MCP design principle: 'Servers should not be able to read the whole conversation, nor see into other servers'; the host enforces security boundaries and isolation between servers (Spec 2026-07-28 Architecture).
- `31c35c7e43c70949` TS SDK CVE-2026-25536 (GHSA-345p-7cg4-v4c7, high, CVSS 7.1): reusing a StreamableHTTPServerTransport or single McpServer across clients causes JSON-RPC ID-collision response misrouting / cross-client data leak (CWE-362); patched in 1.26.0.
- `5b21d8e8e738cd1d` TS SDK CVE-2026-0621 (GHSA-cqwc-fm46-7fff, high): ReDoS in UriTemplate.partToRegExp() on exploded array patterns ({/id*}, {?tags*}) -> 100% CPU / DoS via resources/read (CWE-1333); patched in 1.25.2.
- `1f78b4ce03e423e2` TS SDK CVE-2025-66414 (GHSA-w48q-cv73-mx4w, high): DNS-rebinding protection disabled by default for HTTP servers on localhost (CWE-1188); patched in 1.24.0.
- `e0dea4f23e7e0578` Python SDK CVE-2026-52870 (GHSA-hvrp-rf83-w775, CVSS 7.6): experimental tasks feature lets any client read/cancel other clients' tasks (CWE-862); patched in 1.27.2.
- `ea5482b19c31dba4` Python SDK CVE-2026-52869 (GHSA-jpw9-pfvf-9f58, CVSS 7.1): SSE/Streamable HTTP session routing without principal verification — anyone knowing a session ID can inject on it (CWE-639); patched in 1.27.2.
- `84257652468055af` Python SDK CVE-2026-59950 (GHSA-vj7q-gjh5-988w): deprecated WebSocket transport had no Host/Origin validation (CWE-346/CWE-1385), so cross-origin pages could drive the server; patched in 1.28.1.
- `494ee4991d2c9420` Python SDK CVE-2025-66416 (GHSA-9h52-p55h-vw2f): DNS-rebinding protection disabled by default (CWE-1188); patched in 1.23.0.
- `89cb87ce94fb35b0` Python SDK CVE-2025-53366 (GHSA-3qhf-m339-9g5v, CVSS 8.7) and CVE-2025-53365 (GHSA-j975-95f5-7wqh, CVSS 8.7): unhandled exceptions lead to server crash / DoS; patched in 1.9.4 and 1.10.0.
- `4d80268c0ca32fc1` Rust SDK (rmcp) CVE-2026-42559 (GHSA-89vp-x53w-74fx, CVSS 8.8): Streamable HTTP server did not validate Host -> DNS-rebinding attack against loopback servers (CWE-346/CWE-350); patched in 1.4.0.
- `f5ece5bdef4f8055` Rust SDK (rmcp) CVE-2026-63128 (GHSA-9pj6-vhgr-3mwh, CVSS 7.5): unauthenticated session-table memory leak -> permanent DoS (~75 GB/day at 2k req/s) (CWE-400/401/772); patched in 2.0.0.
- `3286897269792642` Rust SDK (rmcp) CVE-2026-63127 (GHSA-33f5-2c5q-wgwj, CVSS 8.2): missing OAuth Protected Resource resource validation lets a malicious server redirect the flow to a legitimate AS and steal tokens; patched in 2.0.0.
- `ab24a97564500905` Rust SDK (rmcp) CVE-2026-64684 (GHSA-9g45-5xwm-f3wc): custom headers (e.g., X-API-Key) forwarded to cross-origin redirect targets (CWE-200); patched in 2.1.0.
- `6d2f6996dd28f2d0` Rust SDK (rmcp) GHSA-c9xm-49cp-xcr9 (no CVE listed): OAuth client fetches server-controlled resource_metadata= URLs without origin/private-network validation -> SSRF (CWE-918); patched in 2.0.0.
- `6acc0b1c65c48fd1` Official MCP Registry moderation is permissively scoped: it removes only illegal content, malware, spam, and non-functioning servers; it won't remove servers with security vulnerabilities, and consumers should assume minimal-to-no moderation (registry moderation-policy).
- `78da7b52131a9cf4` Reference servers in modelcontextprotocol/servers are not production-ready solutions; developers should evaluate their own security requirements (servers README).
- `c83ae8c7cde41572` SEP-1024 (Final, Standards Track) mandates for one-click local MCP server installation: a consent dialog showing the exact untruncated command, explicit affirmative approval, and a cancel path (motivated by silent command execution, lack of visibility, arbitrary code execution via crafted configs).
- `5c3186bd4f4808cf` 'Breaking the Protocol' (arXiv 2601.17549) reports three architectural MCP weaknesses — absence of capability attestation, bidirectional sampling without origin authentication (enables server-side prompt injection), implicit trust propagation across multi-server configs — and measures that MCP's design amplifies attack success by 23–41% versus non-MCP integrations; a proposed MCPSec extension cuts measured success from 52.8% to 12.4% (study-produced estimates).
- `293aa0c6db3e1be3` Two studies of 7 MCP clients (Claude Desktop, Claude Code, Cursor, Cline, Continue, Gemini CLI, Langflow) identify tool poisoning (malicious instructions embedded in tool metadata/descriptions) as the most prevalent client-side vector, with wide disparities (Claude Desktop 'implements strong guardrails', Cursor 'exhibits high susceptibility to cross-tool poisoning') (reported; arXiv 2603.21642, 2603.22489).
- `9bdd40c2ea5c9354` MPMA (arXiv 2505.11154) documents a Preference Manipulation Attack: a customized MCP server inserts manipulative words into tool names/descriptions to make the LLM prefer it; a genetic-algorithm variant (GAPMA) adds stealth.
- `b7ce46afba9f4ef1` MCP-38 (arXiv 2603.18063) defines a 38-category MCP threat taxonomy (tool-description poisoning, indirect prompt injection, parasitic tool chaining, dynamic trust violations) mapped to STRIDE and OWASP LLM/Agentic frameworks; a companion SoK (arXiv 2512.08290) systematizes indirect prompt injection and tool poisoning and surveys defenses (cryptographic provenance, runtime intent verification).
- `7153a23984e13764` MCP-Guard (arXiv 2508.10991) reports a multi-stage detection pipeline with 96.01% accuracy on adversarial prompts and the MCP-ATTACKBENCH benchmark of 70,448 samples (study-reported).
- `0fe733e6d264cd16` A measurement study of 6 MCP marketplaces (arXiv 2509.25292, 8,060 servers) found more than half of listed projects invalid or low-value, plus dependency-monoculture and maintenance risks (study-reported).
- `0a125f2cbc31901b` An empirical MCPApps study (arXiv 2607.25635, 1,723 apps) found only 37.2% gate tool execution behind a blocking approval step, leaving the LLM able to invoke any enabled tool unconditionally in most apps (study-reported).
- `26963aa692752e78` Default agent run state is in-memory in all major agent frameworks; persistence is an added layer. LangGraph splits it into checkpointers (thread-scoped short-term state) and stores (cross-thread long-term memory), and says persistence 'matters when an agent needs to continue a conversation, resume after an interruption, recover from a failure, or remember information across interactions'.
- `cacdebebd3626767` OpenAI Agents SDK's built-in session memory (SQLiteSession) exists to avoid manually re-sending .to_input_list() history between runs.
- `b93d7d79db9d851e` Microsoft's Agent Framework treats durability as an extension: 'An ordinary Agent Framework agent runs in-process: its conversation history lives in memory and is lost when the process ends.'
- `2b319322a0428380` When frameworks add persistence, they route production durability to databases, not flat files. LangGraph's checkpointer table: MemorySaver/InMemorySaver = RAM, lost on restart; SqliteSaver = 'Ideal for experimentation and local workflows… local file-based storage for development'; PostgresSaver = 'used in LangSmith. Ideal for using in production.'
- `7b02843dca2475f6` OpenAI Agents SDK built-in session backends: SQLiteSession is the default for 'local development and simple apps'; documented shared/multi-process paths are RedisSession ('shared memory across workers… low-latency distributed deployments'), SQLAlchemySession (Postgres etc.), MongoDBSession ('horizontally-scalable, multi-process session storage'), and DaprSession. Operational guidance: in-memory SQLite for temporary, file-based SQLite for persistent, Redis for shared low-latency, MongoDB for multi-process horizontally-scalable storage.
- `12fe0bcb03d20370` Microsoft durable agents persist session state in external storage (Durable Task / Azure Storage) specifically to get multi-instance scale-out (any worker can resume a session), crash recovery, and human-in-the-loop waits of days/weeks with zero compute.
- `0f28c5715a50722b` Checkpoint-style run state has a documented scaling cost: 'Over long conversations, checkpoints accumulate. This can increase latency and storage costs. Fix: prune old checkpoints or set a retention policy.'
- `e268c955dcf1bd2f` By default LangGraph checkpoints write the full value of every state channel at each super-step; DeltaChannel stores only deltas, making checkpoint blobs 'O(1) per step instead of O(N)… at the cost of… storage-vs-latency tradeoff.'
- `330187df00ed1779` LangGraph custom-checkpointer guidance: get-by-id must be 'O(1) or close to it'; 'A scan with a value filter on every lookup works but does not scale'; index design is correctness+performance-critical.
- `8c506f6dd07767f9` LangGraph offers durability modes (exit/async/sync): 'exit' is fastest but 'you cannot recover from system failures (like process crashes) mid-execution'; 'sync' persists every checkpoint before continuing 'at the cost of some performance overhead.'
- `b7d8338a5de669c3` Filesystem-as-state is used where human readability, git, and zero-infra win, with explicit first-party limits. Claude Code: the first 200 lines of MEMORY.md, or the first 25KB, are loaded at the start of every conversation; topic files are not loaded at startup but read on demand; auto memory is machine-local and files are not shared across machines or cloud environments; CLAUDE.md should target under 200 lines per file because longer files 'consume more context and reduce adherence.'
- `bf3e747ec837d4b4` The MCP reference Knowledge Graph Memory Server persists the whole knowledge graph to a single JSONL file (MEMORY_FILE_PATH, default memory.jsonl) — a file-backed, single-process memory with no server-side DB.
- `4e221bfa0c5d7bc3` OPINION (local first-party experience, not a benchmark): mini-agi is a filesystem-as-state agent kernel (canonical memory + a checkpoint journal); its AGENTS.md records file-based operational hazards — never restore the checkpoint journal through git (it destroys working-tree journal lines and orphans BEGINs; repair through checkpoint.sh only) and never chain a grep -c with && (a zero-match exits 1 and silently breaks the chain). This is documented pain from the flat-file model, not a measured comparison.
- `8d8f35ea68bf6ccc` SQLite is an embedded database that is itself a file, and its own docs bound its scaling: 'SQLite does not compete with client/server databases. SQLite competes with fopen().'
- `2766f5af4ab026e3` SQLite supports an unlimited number of simultaneous readers but only one writer at any instant in time.
- `3e5e18d029069f90` Over a network filesystem, SQLite warns that file-locking logic is buggy in many network filesystem implementations, two or more clients might modify the same part of the same database at the same time resulting in corruption, and there is nothing SQLite can do to prevent it.
- `7c86cd916f6513f8` SQLite lists 'very large datasets' and 'many concurrent writers' as cases where a client/server RDBMS 'may work better.'
- `73649fc34b71a4f6` ESTIMATE (vendor design guidance, no published numbers in the sources reached): the scaling curve implied by LangGraph's and OpenAI's backend menus is roughly in-memory → SQLite file → Postgres/Redis/MongoDB, but neither publishes a benchmark for agent state at scale in the cited pages.
- `c65a4ce6cdb624b8` Long-term cross-thread agent memory has a different shape than run state and is DB- or vector-store-backed in the frameworks. LangGraph's long-term memory is a Store abstraction (namespace + key + JSON value) with production backends PostgresStore, MongoDBStore, RedisStore, UpstashStore and optional semantic/vector search; the reference SQL schema uses TEXT[] namespace + JSONB value + GIN index.
- `d0090b3e0a85c898` Microsoft Agent Framework ships a separate 'vector-stores-and-embeddings' feature area, i.e. durable memory is treated as a database/vector-store concern rather than a filesystem one.
- `5d0c374aed68c1f0` Run state durability is not free in any architecture: default agent run state is in-memory and lost on process end, and even file-backed checkpoints grow unboundedly and need pruning/delta encoding.
- `28a317b7a861c29c` All three major frameworks route production durability to databases, not flat files: LangGraph to Postgres ('Ideal for using in production'), OpenAI Agents SDK to Redis/SQLAlchemy/MongoDB/Dapr for 'shared', 'distributed', 'horizontally-scalable' memory, Microsoft to Durable Task external storage for 'multi-instance scale-out.'
- `ebcb42396b17bd81` Flat-file memory is chosen for readability/git/zero-infra (Claude Code, MCP memory server) and its costs are explicit in primary docs: context-window limits (~200 lines/25KB), on-demand loading, and machine-locality (not shared across machines/cloud).
- `86bb11459302600d` File/SQLite concurrency is bounded: one writer at a time, and network-filesystem file-locking bugs can corrupt state (sqlite.org); 'scale-out' and 'concurrent writers' are exactly the cases the primary docs route to client/server DBs.
- `f1bbc7cc63d0bf3b` Uncertain: 'which scales better' is only answered as vendor design guidance, not measurement, in the sources reached; no published benchmark was found comparing flat-file vs SQLite vs Postgres for agent run state at load.
- `180a819281a6e18b` Unverified: scale-out behavior of a flat-file model across many worker processes — both Claude Code and the MCP memory server are explicitly single-machine/single-process.
- `1f4b9fc62486cff6` PostgreSQL REFRESH MATERIALIZED VIEW completely replaces the view contents by re-executing the backing query; stock PostgreSQL has no incremental refresh path, only full recomputation.
- `8097fca3fceebe45` PostgreSQL REFRESH MATERIALIZED VIEW CONCURRENTLY still re-executes the full backing query; the option only avoids locking concurrent readers, requires a plain UNIQUE index covering all rows, and works only when the view is already populated.
- `e1f2e874ff8f4e1d` Oracle fast refresh applies to the materialized view only the changes to master tables recorded in materialized view logs; COMPLETE refresh re-executes the defining query; FORCE (default) does fast refresh if possible else complete refresh.
- `6268ad4cbce610b0` Oracle fast refresh requires materialized view logs to exist on the master tables at CREATE time, and eligibility for fast refresh is verified when the definition is created.
- `19fdc143f081ed4f` Oracle offers three refresh timings: ON COMMIT (refresh as part of commit on a master table, may increase commit time), ON DEMAND (manual via DBMS_MVIEW, default), and ON STATEMENT (per-DML; only for fast-refreshable star/snowflake views that include the fact table ROWID).
- `cfddc08e47e6dcf7` Oracle real-time materialized views avoid refreshing entirely: the optimizer writes a query combining existing MV rows with changes recorded in log files since the last refresh (on-query computation).
- `266b8325088b1614` pg_ivm (PostgreSQL extension) computes and applies only incremental changes to materialized views, updated immediately in AFTER triggers when a base table is modified, instead of recomputing from scratch as REFRESH MATERIALIZED VIEW does.
- `3faec3e0b1d80803` pg_ivm keeps incremental maintenance efficient via an automatically-created unique index (on GROUP BY columns, DISTINCT target list, or base-table primary keys) used as scan keys to locate affected rows.
- `60fdbe45190d2e94` pg_ivm README shows a doc example where a single-row update costs ~20.6 s via full refresh vs ~15 ms as a base-table write for an incremental MV (doc-reported example timings).
- `1a17006009583ba8` pg_ivm documents that IVM is not effective when a base table is modified frequently, or when a large part is modified or large data is inserted — maintenance cost can exceed refresh from scratch.
- `f481b98eb5ba6799` CouchDB runs the map function over all documents only once, on first query; when a document is changed, the map function runs only once for that single document.
- `9b9296bb8b2fc470` CouchDB invalidates per-document view rows via back-references and applies changes since the last query as a batch operation, since usually multiple documents change between queries.
- `d58edf0e65fb0f8f` CouchDB caches reduce results in B-tree interior nodes, so range-aggregate queries are answered from cached partial reductions, recalculating only a minimum number of nodes.
- `8c38fd0e61beee75` Changing a CouchDB view definition invalidates its index; views that change often and share a design document with others force those other views' indexes to rebuild from scratch when the design document is written.
- `3c52730566932ab6` SQL Server indexed views are views with a unique clustered index; the optimizer can consider the view for substitution without it being referenced in the query, and DML on base tables must update referenced indexed views, so DML query performance can degrade significantly.
- `c6f1f175eb359cd1` SQL Server indexed views are gated on determinism and precision: only precise deterministic expressions can participate in key columns and WHERE/GROUP BY clauses; plus fixed SET options, WITH SCHEMABINDING, and a restricted dialect (AVG must be expressed via COUNT_BIG + SUM; LEFT/RIGHT/FULL OUTER JOIN, subqueries, SELECT DISTINCT, window functions, and set operators are excluded).
- `33aa2b6ddca3a89e` dbt incremental models build the table from all source rows on first run and transform only the rows the user filters for on subsequent runs; is_incremental() is true only when the target table exists, full-refresh is not passed, and the model is materialized='incremental'.
- `7b3058140a429de1` dbt's standard incremental filter is a high-watermark against the derived table itself: where event_time >= (select coalesce(max(event_time),'1900-01-01') from {{ this }}); new-rows-only semantics suit append-only sources directly.
- `c436a95d905a7aa0` dbt unique_key turns pure append into upsert semantics (update-or-ignore on match, insert on new), required to avoid bringing modified records in as duplicates; without it most adapters result in append-only behavior. incremental_predicates push predicates into the merge to limit scans of the existing table.
- `15355f27b297c061` When dbt incremental logic changes, incremental results diverge from historical rows stored in the target table, so the model must be rebuilt via dbt run --full-refresh; the default on_schema_change policy is 'ignore', meaning a removed column makes the run fail.
- `ed80831515a47d10` Materialize distinguishes plain views (results recomputed from scratch each access) from indexed and materialized views, whose results are persisted in durable storage and incrementally updated as new data arrives; indexed views keep the incrementally-maintained result in cluster-local memory.
- `855675ab421a6d0b` Materialize rehydrates materialized views on restart from its own storage layer, not from upstream sources: hydration reconstructs in-memory state by reading Materialize's storage layer and does not require reading the upstream system.
- `ae4ee7c4c7369b1f` BigQuery has two materialized-view classes: incremental views, which BigQuery precomputes/updates in the background from base-table changes, and non-incremental views, where every refresh runs the full query; freshness is served by reading rest of the data from the MV and only the changes from base tables.
- `5d32c1280761f55f` BigQuery incremental eligibility is strict: incremental views support a limited feature set; AVG, ARRAY_AGG, and APPROX_COUNT_DISTINCT results are stored as intermediate sketches rather than final values; a deletion in a base table can force a full refresh (notably for BigLake sources) because the partition layout is what permits update-in-place.
- `6dbd271b7ce0e25e` Incremental aggregate maintenance requires auxiliary state beyond the visible columns: pg_ivm adds hidden __ivm_count__/__ivm_count_avg__/__ivm_sum_avg__ columns and recomputes min/max from base tables when the extreme tuple is deleted (a documented slow path); BigQuery stores AVG/APPROX_COUNT_DISTINCT as sketches; CouchDB caches partial reduce values in B-tree nodes.
- `1a44ed072b912b06` Consistency has a price on the write path: SQL Server DML query performance can degrade significantly on tables referenced by indexed views; pg_ivm immediate maintenance slows base-table writes and raises errors in REPEATABLE READ/SERIALIZABLE for multi-base-table views under concurrent updates; Oracle ON COMMIT may increase commit time.
- `16f2c921fa480440` Staleness is an explicit knob: BigQuery max_staleness bounds lag at 30 minutes to 3 days (doc-stated, not measured); Materialize's reaction time is framed as freshness + latency (doc-reported, page not fetched directly); Oracle real-time MVs trade stored freshness for on-query computation; dbt's default is batch staleness bounded by run cadence.
- `8ab263a3dc129e33` Five established mechanisms avoid full recomputation, all from primary docs: (1) delta-capture + replay via change logs/triggers (Oracle fast refresh + MV logs, pg_ivm AFTER triggers); (2) index-structured materialization updated per changed document (CouchDB B-tree views); (3) view-as-physical-index maintained by DML (SQL Server indexed views); (4) batch incremental pipelines with high-watermark + upsert keys (dbt); (5) continuous/streaming maintenance with durable or in-memory storage (Materialize, BigQuery incremental MVs).
- `53591a59c549819a` All incremental-maintenance mechanisms impose eligibility constraints on the derivation (deterministic, restricted SQL), keep auxiliary state for incremental correctness, and expose a full-rebuild escape hatch and an explicit staleness/write-cost tradeoff.
- `e761f79662ed051f` Transferable patterns for regenerating derived documents from append-only sources (source constraints are fact; the generalization is opinion): pure/deterministic derivation, a durable change/append log plus high-watermark position, keyed upsert for idempotent replay, incremental state stored adjacent to the output, an explicit full-rebuild path for when logic changes, and bounded staleness with in-place swap or merge.
- `f89a7953b2d236cf` Kafka's log-compaction and EventStoreDB projections as mechanisms for derived state are unverified: the Kafka documentation site served only a JS redirect and EventStoreDB's projections docs were unreachable, so compacted-topic-as-derived-state and projection-over-event-stream claims must not be attributed to primary sources until fetched directly.
- `00d9b00db4beee22` Oracle and BigQuery performance figures (refresh times, cost claims) are vendor-reported and unbenchmarked; the pg_ivm timing example is doc-reported; Materialize's reaction-time formula and BigQuery's max_staleness numeric range come from doc statements, not measured data.
- `ce69ffc93f18ed5b` Anthropic eval guidance defines good success criteria as Specific, Measurable, Achievable, Relevant, measured on a 'held-out test set' (e.g., 'F1 >= 0.85 on a held-out test set of 10,000 diverse Twitter posts').
- `bf6ba5ce46a474d0` Anthropic eval design principles: be task-specific, 'automate when possible' (multiple-choice, string match, code-graded, LLM-graded), and 'prioritize volume over quality' (many automated-graded questions over fewer human-graded ones).
- `0950282a69d5217e` Anthropic agent-eval practice: a good task is one 'where two domain experts would independently reach the same pass/fail verdict'; each task should be passable given a reference solution; 'a 0% pass rate across many trials (0% pass@100) is most often a signal of a broken task, not an incapable agent'.
- `33379f8028c507ae` SWE-bench Verified (OpenAI, 93 professional annotators, each sample labeled 3x, ensembled by max severity) filtered out 68.3% of the original SWE-bench test set for underspecified problem statements (38.3%), unit tests that unfairly reject valid solutions (61.1%), or other issues.
- `4a75ebc1387e623f` As-reported (Anthropic citing external sources): Opus 4.5 initially scored 42% on CORE-Bench and jumped to 95% after fixing rigid grading (e.g., penalizing '96.12' for expected '96.124991...'), ambiguous specs, and unreproducible stochastic tasks; METR likewise found misconfigured time-horizon tasks whose graders required exceeding the score threshold stated in the task.
- `fda4c8c9837bba04` Anthropic grader taxonomy: code-based (fast, objective, reproducible, but 'brittle to valid variations'), model-based (flexible but 'non-deterministic', requires calibration with human graders), human (gold standard, calibrates LLM graders); recommendation is deterministic graders where possible and grading outcomes rather than tool-call sequences.
- `251b28fe96799da2` LiveBench was designed to avoid test-set contamination and LLM-judge/crowdsourcing biases by scoring 'answers automatically according to objective ground-truth values'.
- `99ec949f854d2660` EleutherAI lm-evaluation-harness gives every task a VERSION field with stability enforced by unit tests (so metric changes from old buggy implementations are knowable) and offers --check_integrity for data verification.
- `1ee6755802152b1d` OpenAI collaborated with SWE-bench authors to build a Docker-containerized harness because SWE-bench environments were 'difficult to reliably set up... inadvertently causing unit tests to fail regardless of the solution'.
- `21114a6bac5e12fc` Anthropic harness guidance: each trial must start from a clean, isolated environment; 'unnecessary shared state between runs... can cause correlated failures due to infrastructure flakiness rather than agent performance' (Claude was observed gaining an unfair advantage by reading git history from prior trials).
- `adc8bcda2c13dd44` Anthropic: 'An eval suite is a living artifact that needs ongoing attention and clear ownership'; effective structure was dedicated evals teams owning infrastructure while domain/product teams contribute tasks; automated evals require ongoing maintenance as product and model evolve to avoid drift.
- `a7da537959884446` Anthropic separates capability evals (low initial pass rate, a 'hill to climb') from regression evals (~100% pass rate, protect against backsliding); once capabilities plateau, high-pass-rate capability evals 'graduate' into a continuously run regression suite.
- `8313bde3b0ba0e0b` 'Test set contamination, wherein test data from a benchmark ends up in a newer model's training set, is a well-documented obstacle for fair LLM evaluation and can quickly render benchmarks obsolete.' (LiveBench)
- `30e358a2763714ce` Magar & Schwartz (ACL 2022): pretrained models are trained on web corpora 'often contaminated' with downstream test sets; controlled experiments show exploitation 'exists in some cases, but in others the models memorize the contaminated data, but do not exploit it'; memorization vs exploitation is affected by duplication count and model size.
- `ea85a2da072f7a6a` LiveBench mitigates contamination by (1) frequently updated questions drawn from recent math competitions, arXiv papers, news, and datasets ('questions are added and updated on a monthly basis'), and (2) harder 'contamination-limited' versions of prior benchmarks.
- `f718f7e999afdd74` EleutherAI harness ships a 'Test Set Decontamination' utility scoring only 'data points not found in the model training set', via precomputed 13-gram exact-match indices against The Pile.
- `515eceaeb6949b62` MLE-bench (agent benchmark from 75 Kaggle competitions) explicitly investigates 'the impact of contamination from pre-training' alongside its main results.
- `3cc8e5c15b13c683` OpenAI states SWE-bench, being scraped from public GitHub repos, is 'likely to be contaminated' for models pre-trained on internet text, and that static datasets are 'inherently limited' — so they must be supplemented by other evals.
- `469930fe1449fcbf` OpenAI Evals offers 'private evals' on a company's own data so teams represent common LLM patterns in their workflow without exposing that data publicly.
- `aa56393d93a19846` Anthropic: because model outputs vary between runs, multiple trials are run for more consistent results; a task 'that passed on one eval run might fail on the next'; complementary metrics pass@k (at least one of k trials succeeds) and pass^k (all k trials succeed); for a 75% per-trial success rate, pass^3 is about 42%.
- `ef026a1a1cae7b2e` OpenAI ran SWE-bench Verified scaffolds with 'a single seed', noting results 'may differ from what is reported in the official leaderboards' — an acknowledgment that single-seed agent runs are noisy.
- `beb33e0d0a29d161` SWE-bench Verified identified three reliability problems: overly-specific/unrelated unit tests (false rejections), underspecified issue descriptions, and unreliable dev-environment setup; addressed via human annotation plus a containerized (Docker) harness.
- `b01301fb9acbfab8` Anthropic failure-triage guidance: read transcripts to distinguish 'a genuine mistake' from 'graders rejected a valid solution'; design graders 'resistant to bypasses or hacks'; build partial credit for multi-component tasks.
- `30f149934a3231a5` METR (2026-03-10): repo maintainers merged only ~68% of original human-written ('golden') patches, so METR normalizes all agent scores against this golden baseline rather than treating any single grader as exact.
- `531ebba427c1608f` Chen, Zaharia, Zou (2023): GPT-4 (March 2023) identified primes with 84% accuracy; GPT-4 (June 2023) scored 51% on the same questions; instruction-following decreased over that window; the 'same' LLM service can change substantially in a short time, 'highlighting the need for continuous monitoring of LLMs'.
- `01c89c6dfe6fb3b6` OpenAI regression-eval recipe: define one eval (data source + grader), run a baseline run against the current prompt, then a candidate ('regression-run') after a prompt change; the lower score on the same dataset flags the regression before shipping.
- `48b751132c157a46` OpenAI eval run API returns per-criteria pass/fail counts and supports webhook events (eval.run.succeeded/failed/canceled), enabling continuous or automated regression runs.
- `5bcc69c7f6435d17` Anthropic: regression evals 'should have a nearly 100% pass rate... a decline in score signals that something is broken'.
- `208b7ba338e48067` Anthropic: 'eval saturation occurs when an agent passes all of the solvable tasks, leaving no room for improvement'; SWE-bench Verified went from ~30% to >80% for frontier models in about a year and 'large capability improvements then appear as small increases in scores'; Anthropic's rule is to not trust scores until transcripts are read.
- `738eaa4b67e49046` METR notes that as its time-horizon task suite saturates, 'the results are becoming more sensitive to analysis choices'.
- `f653848c6e81eaf3` OpenAI reports GPT-4 on SWE-bench Lite scoring between 2.7% (early RAG-based scaffold) and 28.3% (CodeR) depending on scaffolding, and therefore runs evaluations 'continually and as often as needed... before, during, and even after training'.
- `952e1ad776c0a506` LiveBench's monthly refresh of questions is explicitly a response to benchmarks becoming obsolete via contamination.
- `d3703dbf4777d91d` Anthropic: LLM-as-judge graders 'should be closely calibrated with human experts', graded per-dimension with isolated judges and an 'Unknown' escape to reduce hallucinated verdicts; 'once the system is robust, it's sufficient to use human review only occasionally'.
- `a2648dcfb37a9a2f` Anthropic: for subjective output, constraining output format and using structured outputs for guaranteed schema compliance reduce grader-side parsing flakiness.
- `dcdc4ac6fa86ae47` Citation-by-recollection is unreliable: a same-titled arXiv entry (2305.10348) returned by search was an unrelated laser-physics paper, while the correctly identified contamination paper (Magar & Schwartz, arXiv:2203.08242) was found via its exact title through the arXiv API — evidence supporting versioned, URL-anchored eval evidence.
- `804ffb8a7c26c4aa` Bazel's `flaky` test attribute executes a test up to three times, marking it failed only if it fails every time; default False (single execution). Its use is generally discouraged. (Bazel Build Encyclopedia)
- `f647744ca1f24b4f` Bazel `--flaky_test_attempts` retries each failing test up to the specified count; tests needing more than one attempt are marked 'FLAKY' in the test summary. Default is one attempt for regular tests and three for tests marked flaky (flaky=1). Supports per-pattern targeting, e.g. --flaky_test_attempts=//foo/.*,-//foo/bar/.*@3.
- `fac277b31fa74875` Bazel `--runs_per_test=N` runs every test N times and fails the test if any attempt fails; `--runs_per_test_detects_flakes` (default false) marks a shard FLAKY if at least one run passes and at least one fails.
- `3880083953a8f0d3` Gradle Test Retry plugin retries failed tests within the same task up to maxRetries (default 0 = off); defaults: failOnPassedAfterRetry=false (pass-on-retry does not fail the task), failOnSkippedAfterRetry=true, maxFailures=0 (circuit-breaker cap off). Supports include/exclude filters by class name and annotations, plus whole-class retry (classRetry) for Spock @Stepwise/TestNG dependsOn. The README warns retrying tests alone is not a viable flaky-test mitigation strategy and should accompany tracking/fixing.
- `45d823a2f58dba76` pytest-rerunfailures supports --reruns N, --reruns-delay with exponential backoff (delay for n-th re-run = reruns_delay * factor**(n-1), default factor 1.0), --only-rerun/--rerun-except regex filters, per-test @pytest.mark.flaky(reruns=5), --force-reruns overrides, --reruns-mode=append (additive marker+CLI counts), --max-suite-reruns suite-wide cap. Priority order: marker > CLI > ini.
- `bb634c81a4c4c097` JUnit Pioneer @RetryingTest: maxAttempts is required and caps executions; minSuccess (default 1) requires the test to succeed that many times within maxAttempts; suspendForMs pauses between retries; onExceptions retries only on listed exception types. Assumption failures (aborts) are not retried; failed-then-passed executions are reported as aborted/ignored.
- `e6b84c294456ecd4` Google denotes a test flaky so it reports failure only if it fails 3 times in a row, reducing false positives but encouraging developers to ignore flakiness; Google also supports re-running only failing tests and automatic re-run when tests fail. (John Micco, Google Testing Blog, 2016)
- `fb857bcd7b9b0020` Google runs an automated quarantine tool that monitors test flakiness and, when flakiness is 'too high' (threshold not specified in the source), automatically removes the test from the critical path and files a bug to reduce flakiness; this could mask a real race condition or other bug. (Google Testing Blog, 2016)
- `24f290b647ef095e` Kubernetes quarantines a single test by adding [Flaky] to its test name; most CI jobs exclude these tests, which run only in explicitly flaky jobs (e.g. the gci-gce-flaky job). Quarantining a presubmit test requires a release-milestone issue labeled priority/critical-urgent, lifecycle/frozen, and kind/flake, with the owning SIG expected to fix and reintroduce or delete the test. Whole-suite quarantine uses [Feature:Foo] names plus dedicated jobs, which merge-blocking suites avoid unless proven non-flaky.
- `a02663aaf66927cd` Kubernetes has a 'zero-flake' policy: test jobs must not automatically retry on test failures. Effective 2019-12-13 (ginkgo.flakeAttempts=2 removed for e2e), confirmed as policy in 2023.
- `af500e05f86d5f38` pytest.mark.xfail with strict=False can be used as a manual quarantine so a test's failure does not break the whole build, though it is rather dangerous to use permanently; the pytest API reference says strict=False is particularly useful to mark flaky tests (random failures) to be tackled later.
- `56702ea84de54b43` Develocity/Gradle warns retry-only flake handling is not a complete solution: flaky tests go unnoticed and you will inevitably accrue more flaky tests. (gradle.com/blog/flaky-tests/)
- `d6354b5974db8518` Develocity considers a test flaky if it fails and then succeeds within the same Gradle task or Maven goal execution, marking such tests FLAKY in build scans; retry is required to detect this, requires no code changes, applies to the whole suite, and enables proactive detection of newly introduced flaky tests. The Tests Dashboard ranks the most severe flaky tests and plots trends over time to confirm fixes.
- `d2375864da7ee620` Google defines a flaky test result as a test exhibiting both a passing and a failing result with the same code, and reports a continual rate of about 1.5% of all test runs being 'flaky', almost 16% of tests having some flakiness, and about 84% of observed pass-to-fail transitions involving a flaky test; these figures are single-company 2016 self-reports. A second Google tool detects changes in a test's flakiness level and works to identify the change that caused it.
- `f8e399d3786cdd54` Kubernetes flakiness detection tooling: flakes-latest.json (top 10 flakes per week across PR jobs), go.k8s.io/triage (interactive 2-week failure drill-down), testgrid's sort-by-flakiness view, the kind/flake GitHub label, and unit-test stress reproduction via golang.org/x/tools/cmd/stress with -race -count=1.
- `3158dabb8476e655` No primary source was found publishing a quantitative quarantine threshold (the Google post's 'too high' is unspecified) or an optimal retry-count trade-off; the verdict lists a published flakiness-rate threshold for auto-quarantine or a CI cost/retry study such as Leinen et al. 'Cost of Flaky Tests in Continuous Integration' (TUM/CQSE 2023) as evidence that would settle the thresholds.
