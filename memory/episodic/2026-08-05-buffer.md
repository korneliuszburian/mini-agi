# episodic buffer — 2026-08-05

APPEND-ONLY. Session state for the compact; nothing below is deleted.

## Session overview (goals closed this session)

1. AFK v1 (41d8f1b..1605185): `mini-agi loop run` — AFK verified-iteration
   supervisor (Matt Pocock AFK/Ralph pattern + kernel verified-iteration).
   Spec resolution (case -> its verifier, P0-3; ad-hoc -> generated spec),
   progress.md per attempt, REPORT.md, on-done hook ($1 report, $2 outcome),
   two-phase liveness (idle timeout via out-file mtime + completion grace),
   codex integration (.codex/config.toml 36->39 tools, approvals), self-
   hosting proof #1 (afk-max-idle: the kernel built its own --max-idle flag,
   verifier pass attempt 1, loop verify closed 0.8409).
2. AFK v2 (1605185..c5cee85): session resume (content-marker ownership
   SESS-OWN-<hash>, --no-resume, resume ON for iterate>1), sequential-
   reviewer template (independent read-only review pass, fix attempt via
   resume, resolve_final_outcome), self-hosting proof #2 (verify-gate-full-
   output: the kernel fixed its own hidden-failure-output gap; 0.6076).
3. AFK v3 (c5cee85..12f0b3f): MCP bridge — loop_run/run_status/run_report
   tools, loop run --detach (bg.rs, stdin nulled — the inherited MCP pipe
   hang), identity-aware liveness (run.start, state!=Z), one-run-per-workdir
   lock (claim_lock, read-back), handle authority (single validated parse).
   Review: 5 rounds to APPROVE 7/8. Codex sessions can launch/poll/read
   runs through the kernel.
4. Repo hygiene (12f0b3f..9872ee7): docs/README.md map (single entry
   point), runtime gitignore (.supervisor/, .worker-*, *.blind-hidden,
   then codex.log/progress.md/run.json + !evals/cases/**/run.json),
   root strays -> artifacts/, dedup, load-bearing list.
5. AFK v4 (9872ee7..41ea8b3): parallel-planner — loop parallel (planner
   pass = read-only codex emitting strict JSON manifest; typed
   deny_unknown_fields validation; worktree provisioning with pre-flight +
   vacuity; dispatch with admission; atomic scratch-branch merge; protected
   final gate; MINIAGI_SESSION_TAG). e2e: REAL planner decomposed a goal,
   2 parallel workers passed, merged, FINAL GATE passed. Review: 5 rounds
   to APPROVE 8/8. Blind-worker NOT used in v1 (rename race).
6. Standards polish (41ea8b3..7db4c88): 62-finding docs audit (CRITICAL:
   bare run.json gitignore was swallowing evals/cases/*/run.json),
   AGENTS.md Process rules (hard lessons: never fuse pkill with edits —
   INCIDENT #1 repeated once; never git checkout the journal — orphan
   BEGIN; grep -c chain trap; edit scripts write at the end — lost fixes
   twice; devils-advocate reviewers; per-domain human-review gate with
   frontend = mandatory HITL), docs/INCIDENTS.md (5 postmortems),
   SKILLS-RESEARCH.md (deep skill research), review loop 10 rounds to
   APPROVE 8/8 no findings.
7. Skill-layer perfection (7db4c88..f70dded): definition of done ENFORCED
   — 14 skill hooks run in verify.sh's skills step (skill verify --all),
   structural lint (criteria section + artifact anchors), version/source
   frontmatter (missing = gate fail), one-owner (any dual registration
   fails; 5 global duplicates removed), install never destroys (backup
   rename, symlink-safe), wayfinder/to-tickets route to kernel ticket
   tools. Review loop: REWORK 2/8 -> APPROVE 7/8 no findings.
8. CI RED root-cause (en route): GitHub runners lack git user identity —
   all fixture git closures now carry GIT_AUTHOR_*/COMMITTER_* env;
   finalize commits carry kernel identity (mini-agi
   <kernel@mini-agi.local> — kernel-authored by design). CI green again.

## Research delivered this session (docs/)

- docs/MEMORY-RESEARCH.md (deep memory+skills pass): Anthropic context
  engineering / building agents / CC best practices / writing tools /
  agent skills; OpenAI compaction + new tools; Codex build-skills +
  AGENTS.md; CoALA + MemGPT; Karpathy append-and-review. Gaps found:
  fact merge/supersede, selective token-budgeted retrieval, directed
  consolidation with preservation list + event triggers. Top-10 changes
  with sources + effort.
- docs/SKILLS-RESEARCH.md: 7 skill-design defects, routing gaps, dogfood
  seam, 3-goal roadmap (A shipped, B kernel seams + new skills, C routing
  + evaluation — prompt matrix + telemetry DEFERRED to Goal C), DoD
  status updated (items 1-3,5-6 enforced; 4+7 = Goal C).

## AGI WAYFINDER IN PROGRESS (mid-flight — RESUME HERE)

- User direction: FULL AGI with a brain/memory layer so good it all ties
  into our system; UI/UX (web or desktop) wrapping AFK/Sandcastle agents;
  auto-researcher layer; opencode-based (deepseek v4 flash = absurdly
  cheap + quality). Frontend work itself = user-only (HITL).
- Method: wayfinder skill (map + decision tickets). .scratch/wayfinder-
  map.md + .scratch/wayfinder/ per the skill. Checkpoint agi-s1 OPEN
  (BEGIN agi-s1 -> 65a9670; must close with a verify).
- Research tracks: TRACK 1 DONE (brain/memory: CoALA, MemGPT/Letta,
  Generative Agents (recency×importance×relevance + reflection w/
  citations), Voyager (skills commit-after-verify), HippoRAG (KG+PPR
  hippocampal indexing), Mem0 (ADD/UPDATE/DELETE/NOOP auditor on cheap
  model, soft-delete), André Lund's production "dreaming" (single-writer,
  token budget, cluster->reflect->score->gate->judge->promote, >=3
  supporting events, human queue, git-commit audit, degrade-not-abort),
  Letta dreaming (worktrees, N-messages/compaction triggers), OpenAI
  agentic chapter (verifier/critic + memory hygiene + immutable audit
  trail), E-mem (against aggressive compression — uncompressed episodic
  under master planner), D-MEM (RPE-gated async), MemoryBank (Ebbinghaus
  decay), sleep-time compute (pre-indexing, != dreaming)). Design
  questions 1-11 formulated with options+evidence (cadence, what
  survives, conflict resolution, graph vs store, decay, retrieval,
  write path, distiller/auditor/linker authority separation, per-agent
  scratch + shared semantic + one consolidator, verifiability/
  provenance-by-construction, cost tiering: cheap model for extraction,
  strong for judgment — Mem0 runs all ops on gpt-4o-mini).
  TRACK 2 DONE (UI/UX: terminal TUI reference (opencode/CC/codex),
  Claude Code agent view (minimal honest status surface), Codex cloud
  run board, scheduled-as-inbox + notifications (needs-you only),
  LangSmith/Langfuse (after-the-fact debug), OpenWebUI, editor-native,
  Prefect (read-only MCP supervision), GitHub Actions runs, Vibe Kanban
  (sunsetting — board alone failed), Sandcastle (no UI by design),
  OpenClaw (chat-first + companion app), Letta/Mem0/Graphiti memory
  viewers (list-first, graph only where provenance is the point),
  Obsidian/Neo4j graph viz, deep-research UIs (report + sources +
  sidebar progress + notification). RECOMMENDATION: local web app
  served by the kernel binary, browser as shell, read-mostly mirror
  (7 panels: run board, run detail, memory brain, dream/consolidation
  log, tickets, report viewer, health/journal), writes stay
  terminal/MCP (HITL); data files-first (no new kernel deps; kernel is
  std-only per ADR-0012 — HTTP needs an ADR); attention model:
  needs-you pings (verify FAIL, signoff pending -> --on-done -> ntfy),
  completed = badge, running/dreaming = silent. Tauri later.
  TRACK 3 PENDING (orchestration & economics: multi-model orchestration,
  MCP as integration spine, opencode/deepseek economics, voice pipeline
  worth?, knowledge graphs, mapping our kernel onto the brain).
- NEW SOURCES (user-provided, to integrate):
  * Prime Agent (primeintellect.ai/blog/prime-agent, Aug 05 2026,
    open-source): two abstractions — RLM (Recursive Language Model:
    persistent IPython kernel as the ONLY tool; context as a variable;
    subagent delegation as function calls in a REPL; agent writes
    language-model programs over its own context; survives compaction
    via variables) + Continual Harness (the harness's own state —
    prompts/skills/memory/sub-agents — is CRUD-able by the agent from
    its own trajectory; agent-to-agent messaging through a background
    daemon, limited to the "nuclear family": parent/sibling/child;
    sub-agents are prime-agent instances; daemon owns sessions over a
    local socket, attach/detach, recoverable workers from JSONL+kernel
    state snapshot; Agents View lists live/idle/inactive sessions).
    ADAPTABILITY: our kernel's worker abstraction + MCP surface map
    onto the RLM idea (kernel = the persistent "kernel"); the Continual
    Harness CRUD idea extends our skill/memory seams (skills already
    have version+install; memory is append-only — CRUD is a decision to
    weigh against our one-owner/append-only guarantees); A2A via the
    MCP server (sessions messaging each other) is a natural extension
    of loop parallel + the daemon pattern resembles our bg.rs detached
    runs (survives parent exit, recoverable from run.json+out).
  * Mastra Observational Memory (mastra.ai/docs/memory/observational-
    memory, @mastra/memory@1.1.0): background agents — an Observer and
    a Reflector — watch conversations and maintain a DENSE OBSERVATION
    LOG that REPLACES raw message history as it grows (activation
    conditions trigger the replacement); cheap model by default
    (gemini-2.5-flash; deepseek-reasoner configurable); storage
    provider required (LibSQL); resource+thread identifies
    user/conversation. ADAPTABILITY: this is the write-through +
    continuous-async consolidation pattern (D-MEM-style); our episodic
    buffer + canonical pipeline is the same shape without the
    "replace older messages with observations" step — a candidate for
    the distiller's cadence semantics.

## Key decisions + rejected options (this session)

- REJECTED: web dashboard for solo kernel (AFK-SUPERVISOR v2 doc) —
  SUPERSEDED by the UI/UX track finding (every current solo-supervision
  reference is a personal surface; the rejection premise no longer
  holds — revisit with an ADR).
- REJECTED: parallel-planner "needs a second consumer" deferral —
  the consumer is the kernel's OWN planner layer (user's scenario:
  reviewer generating 20 tickets from one review); shipped.
- DECIDED: reviewers = devils-advocate (roast, evidence-first);
  disposition must disprove with evidence; a finding surviving
  current-code verification is accepted (F3 v3-review was wrongly
  rejected once).
- DECIDED: per-domain human-review gate — frontend = mandatory HITL
  (user zero-trust rule); system-side review default.
- DECIDED: memory consolidation = fact merge/supersede + selective
  retrieval + directed consolidation (preservation list) — from
  MEMORY-RESEARCH; NOT YET BUILT.
- DECIDED: skills = contracts with hooks in the gate (built);
  prompt matrix + routing telemetry = Goal C (deferred).
- DECIDED: opencode as the worker harness (deepseek v4 flash economics)
  — multi-worker worker_name exists; codex->opencode swap is a
  candidate first move for the AGI program.

## Next actions (ordered)

1. CLOSE agi-s1 checkpoint after this compact (checkpoint verify).
2. AGI wayfinder: run TRACK 3 (orchestration & economics), then write
   the wayfinder map (.scratch/wayfinder-map.md) + decision tickets
   (.scratch/wayfinder/<name>.md) synthesizing all three tracks +
   prime-agent + Mastra + MEMORY-RESEARCH + SKILLS-RESEARCH.
3. First build candidates (from research): opencode worker swap (cost
   economics), the dream-loop (distiller/auditor/linker consolidation —
   event triggers + nightly gated promotion, cheap model extraction,
   strong model judgment, provenance-by-construction, soft-delete,
   human queue), the thin web UI (files-first read mirror, 7 panels).
4. Memory quality goal: merge/supersede + dedup gate, selective
   token-budgeted retrieval, directed consolidation + event triggers.
5. Skills-in-codex goal: frontmatter normalization + agents/openai.yaml
   policy + context budget + dynamic injection + fork isolation.
