# Episodic buffer 2026-08-03 — session decision log (append-only)

## Decisions (with reasons)

- D1: Tool parity treats `write`/`edit` as one family (ADR-0006).
  Reason: harness never emits `edit`; measured 0/32 pure write<->edit
  pairs on existing cases — prospective comparability fix, no score
  changed. Rejected: leaving the metric as-is (unfixable artifact).
- D2: Tool-mismatch signal closed as a loop (Phase 6.2): gate
  `TOOL REGRESSION` beyond tolerance + mismatch register surfaced via
  `resume`. Reason: ADR-0005 — the signal must change the next run.
- D3: Work graph (ADR-0008): tickets carry `blocked_by` edges; claims
  registry `tickets/claims.md` with lease semantics; `validate-graph`
  checks dangling + cycles. Reason: Yegge Beads pattern — parallel
  agents must not collide; claim fails on open dependency (--force).
- D4: Sandbox attestation (ADR-0009): verify.sh `sandbox` target —
  skipped locally, mandatory in CI (non-root + RUNNER_NAME).
  Reason: "gate requires sandbox evidence" literal; CI ran in-sandbox
  since Phase 5 but never attested it.
- D5: Proactive composition (Phase 6.4): `mini-agi loop status|dispatch|
  verify`. dispatch picks worst open case (below 0.5, unclaimed, no
  passing rerun), ensures ticket, claims it, writes spec slice; verify
  scores + ingests + releases lease at >= 0.5. Reason: gap->ticket->
  slice->rerun without human routing (Wish Factory).
- D6: Rerun semantics: a passing `<case>-rerun` (>= 0.5) closes the gap;
  the original stays as historical fixture; insights capability gaps
  exclude rerun-closed cases. Reason: regression evidence is not deleted.
- D7: Fixture policy (ADR-0010): insights reports
  `composite_avg_effective` (passing rerun overrides its original, each
  case once) beside the plain history mean; Phase 6 acceptance measured
  on the effective metric. Reason: history stays recorded, capability
  mean measures now. Rejected: deleting fixtures (hides regression
  evidence), re-scoring history (append-only).
- D8: Gate baseline is refreshed via `eval gate --write-baseline` after
  semantics/reruns change (never silently). Reason: gate compares
  against the committed baseline.
- D9: Claims/leases recorded only by CLI commands, never hand-edited;
  ticket status parsed from frontmatter/bullet/plain `Status:` line.

## Rejected options (with reasons)

- R1: Scorer semantics changes without ADR — rejected by contract
  (v1-frozen tests); ADR-0006/0010 were the authorized paths.
- R2: Land Rush megabatch + swarm diagnosis (Yegge CI/CD) — deferred;
  serial gates are correct at current scale.
- R3: Local agent sandboxing (bwrap/firejail) — deferred to Phase 6.4+
  dispatch of untrusted work (kernel std-only).
- R4: Stamps / portable reputation — deferred; composite is the current
  single-dimension stamp.
- R5: Deleting or moving failing fixtures out of evals/cases/ — rejected
  (gate regression evidence must stay).

## Session facts (measured)

- Phase 6 complete: 6.1 failure register, 6.2 mismatch loop, 6.3
  sandbox, 6.4 loop; acceptance: effective avg 0.7431 >= 0.60, zero
  gaps, gate ALL GREEN.
- 8 reruns created: reactive-loop (0.7225), real-ticket-001..007-v2
  (0.6141-1.0000; 007 = perfect 1.0000), flailing (0.8500).
- 142 tests, 20 baseline cases, 42 canonical facts, 20 runs ingested.
- Operational incidents: OOM pressure from 500 agent-browser processes
  (killed, RAM restored); checkpoint journal deadlock repaired earlier
  (rollback re-journaling, same-second audit fix).
- Dogfood evidence: scratch checkpoint.sh rolled back the scratch repo
  on VERIFY-FAIL (rollback branch works); pick_target now reports
  "nothing left to dispatch" (falsifier updated).
